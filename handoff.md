## Summary
검색 단계를 따로 재는 평가층(`eval_retrieval.py`, 44문항)을 붙여 검색 노브를 숫자로 판정할 수 있게 했고, 그 측정으로 오지급을 단정하던 버그 2개와 키워드 파일 필터를 제거했다. 이어서 그 평가층으로 `VECTOR_WEIGHT`를 0.5→0.8로 올렸다. 커밋 9개(`eaf402e`..`a8257d2`), main 대비 미push. 회귀 `test_rules.py` 22/22, 검색 `eval_retrieval.py` hit@1 63.6%·hit@20 100%·MRR 0.744.

## Key Decisions
- 답변 가능 여부를 검색 점수 대신 LLM 근거 판정으로 — BM25·코사인 둘 다 "답이 들어 있는가"를 구분 못 했다. 평가셋에서 정답 없는 질문이 더 높은 점수를 받았다.
- 규칙 트리거를 키워드에서 LLM 추출 의도로 — 한국어 부분문자열 매칭은 구조적으로 충돌한다(상→증상, 형→유형, 이사→이사회). 판정 호출에 의도 추출을 합쳐 호출 수는 늘리지 않았다.
- 리랭커 대신 후보 상한 20 — 평가에서 hit@20이 100%다. 판정기는 항상 정답 조항을 보고 있다. 112청크라 가능한 선택이며 문서가 늘면 리랭커가 필요해진다.
- 후보를 **지우는** 키워드 규칙은 해롭고 **더하는** 확장은 값을 한다 — 파일 배제 필터 제거는 전 지표 개선(hit@20 88.6→100%), 검색어 주입 블록 제거는 악화(hit@3 79.5→72.7%). 같은 "하드코딩"으로 묶어 처리하면 안 된다.
- 제도 섞임 방지는 `TOP_FILE_SCORE_RATIO`(0.6)의 점수 기반 파일 컷이 담당 — 낱말이 아니라 점수로 가리므로 진짜 모호한 질문만 여러 규정이 섞여 판정으로 넘어간다.
- 테스트 기대값을 답변 형태·낱말이 아닌 오지급 단정 금지로 — 규칙 삭제 후 이관·되묻기·조건부 안내가 갈리고, 생성 답변은 "최장"/"최대"를 번갈아 쓴다.

## Traps to Avoid
- **평가셋이 재려는 노브에 유리하게 편향됐는지 먼저 볼 것.** 초기 38문항은 전부 "질문이 속한 제도에 정답이 있는" 형태여서 파일 배제 필터가 무조건 좋게 보였다. 경계 문항 6건을 넣자 5건이 미검색(정답 파일이 후보에 아예 없음)으로 드러났다.
- **검색 점수로 임계값을 만들려 하지 말 것.** 프로브 3개로 "변별력이 생겼다"고 판단했으나 8건으로 늘리자 분리가 음수로 뒤집혔다. BIGRAM_WEIGHT 0.3/0.6/1.0 전부 음수였다.
- **BIGRAM_WEIGHT를 낮추지 말 것.** 1.0→0.3에서 상위 3건 정확도가 6/6→4/6으로 떨어져 되돌렸다. 44문항 스윕에서도 0.5·1.5·2.0 모두 1.0보다 나빴다.
- **`test_rules.py`는 회차마다 1~2건 흔들린다.** 생성 답변이 LLM이라 A-11(15일 경계)·B-03(이사회) 같은 케이스가 간헐 실패한다. 실패를 보면 회귀로 단정하기 전에 그 케이스만 3회 다시 태워볼 것 — 실제로 두 건 다 6/6으로 통과했다.
- **VECTOR_WEIGHT를 0.8 위로 올리지 말 것.** 0.9~1.0이 MRR은 더 높지만(0.78) 키워드가 지키던 문항 2건이 1위→9~11위로 밀린다. 평가셋이 패러프레이즈 위주라 벡터 쪽으로 편향돼 있어 그 이득은 과대평가다.
- **고정 문구 규칙은 인접 사안에서 조용히 틀린다.** `build_housing_move_answer`가 재부임(5.1-6: 1년, 합산 3년 이하일 때만 3년)을 모른 채 신규 부임 표를 하드코딩해 2년 초과 지급을 단정했다. 규칙이 답을 반환하면 `choose_after_rules`가 그래프를 끝내므로 판정 경로로 내려갈 기회도 없다.
- **판정 스키마의 빈 필드를 "해당함"으로 처리하지 말 것.** `sibling_case`가 relation=null에도 참이어서 "유형"의 형에 20만원 지급을 확정했다. relation이 '본인'인 회차엔 통과하고 null인 회차엔 실패해 같은 질문이 회차마다 갈렸다.
- **`starts_new_policy_topic`으로 후속 질문을 판별하지 말 것.** 후속 답변이 주제어를 포함하면 새 주제로 오판한다. 검색 단계는 이력이 있으면 무조건 직전 질문을 붙인다. 규칙·생성 단계엔 아직 남아 있다.
- **판정 프롬프트에 오늘 날짜를 넣어도 신청기한 케이스는 안 바뀐다.** 원인이 날짜가 아니라 5.2-5의 예외 조항(주관부서장 승인)이었고, 되묻는 것이 규정상 맞았다.
- **`구미` 케이스를 선별 로직 문제로 진단한 것은 틀렸다.** 융합 점수가 정직하게 밀어낸 것이다. 벡터가 순위를 망친 사례지만 전체로는 이득이라 VECTOR_WEIGHT 0.5를 유지했다.

## Relevant Files
- app.py:211-213 — `BIGRAM_WEIGHT`(1.0), `TOP_FILE_SCORE_RATIO`(0.6). 후자가 제도 섞임을 막는 유일한 장치다.
- app.py:216-266 — `load_policy_index`(idf·임베딩 캐시), `score_chunk`(BM25). 경로+본문을 함께 임베딩(`searchable`). 임베딩 실패 시 키워드 단독 폴백.
- app.py:320-390 — `retrieve`. 하이브리드 융합, 파일별 상위 3건 확보 후 남는 자리 채움. 파일 배제 필터는 제거됐고 검색어 확장은 잔존한다.
- app.py:944-1038 — `GROUNDEDNESS_INSTRUCTIONS`, `judge_groundedness`. verdict/intent/relation/finding을 한 번에 뽑는다.
- app.py:1052-1120 — `analyze_question_node`, `rule_applies`, `apply_policy_rules_node`. 의도·관계로 규칙을 가른다.
- app.py:1123-1190 — `build_clarify_answer`/`build_escalation_answer`/`generate_answer_node`. 3단계 분기.
- test_rules.py — 답변 회귀 22건(A 18 보호 + B 4 오탐). 실행에 `SSL_CERT_FILE=$(python3 -m certifi)` 필요.
- eval_retrieval.py — 검색 회귀 44문항. 정답 조항 경로 라벨로 hit@k·MRR을 낸다. 라벨은 실행 시 청크와 대조 검증되어 규정이 바뀌면 실행이 멈춘다. `-v`로 전 문항 순위.
- 규정/숙소지원금 이슈 샘플.txt — 실제 상담 로그 약 30건. SOURCE_FILES에 없어 검색 대상이 아니며, 평가 문항의 출처다.

## Open Work
- `retrieve`의 `limit=20`을 줄일 수 있는지 미확인 상태다. hit@10이 93.2%라 20→10은 손실이 있고, 판정 호출의 토큰 비용이 걸리는 지점이다.
- hit@1은 63.6%까지 올랐지만 hit@10이 93.2%로 정체다. 상위권을 더 올리려면 이제 노브가 아니라 청킹·라벨 쪽을 봐야 한다.
- 날짜 계산 도구는 필요하다는 근거가 없다. 미커버로 지적됐던 유형 4개 중 3개(휴직 8개월 지원기간·15일 경계·신청기한 6개월 역산)는 맞게 답하고, 틀린 재부임은 원인이 고정 문구 규칙이었다.
- 추출 스키마 팽창은 `receiving_support` 제거로 한 칸 되돌아갔다(relation → finding). 규정에서 사실 스키마를 한 번에 도출하는 대안은 미착수다.
- LH 전세임대 문의는 clarify에 머물고 이관까지 가지 않는다. 규정이 재임대 형태를 다루지 않는다.
- 실제 이슈 로그는 숙소지원금 영역에만 존재한다. 경조금·여비·동호회 검증은 합성 질문에 의존한다.
- `eval_retrieval.py`는 AGENTS.md의 "구현 코드는 파일 하나" 규칙 밖에 있는 평가 파일이다(test_rules.py와 같은 위치).
- 브랜치는 push되지 않았고 main은 그대로다. 수강생 배포 시작점을 main으로 유지하려는 의도였다.
- 이 Python 설치에 CA 인증서가 없어 HTTPS 호출이 실패한다. 영구 수정은 `/Applications/Python 3.13/Install Certificates.command` 실행이다.

## Prompt for New Chat
handoff.md를 읽고, 여기 나열된 파일들을 실제로 Read 도구로 열어서 문서 내용을 코드와 대조 검증한 뒤, 검증되면 이어서 작업해줘. 이 문서는 사실이 아니라 가설로 취급해.
