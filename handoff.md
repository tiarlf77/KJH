## Summary
`refactor-rules-trial` 브랜치(로컬 전용, main 대비 +576/−322)에서 규정 검색을 BM25+임베딩 하이브리드로 바꾸고, 답변 가능 여부 판정을 LLM에 맡기는 3단계 라우팅(answerable/clarify/escalate)을 도입했다. 고정 문구만 반환하던 규칙을 단계적으로 삭제했고, 검색 단계를 따로 재는 평가셋(`eval_retrieval.py`, 44문항)을 붙여 검색 노브를 숫자로 판단할 수 있게 했다.

현재 상태: 회귀 `test_rules.py` (A) 18 + (B) 4 = 22건 전부 통과(연속 3회 동일). 검색 `eval_retrieval.py` 44문항 hit@20 100% / MRR 0.639.

## Key Decisions
- 답변 가능 여부를 검색 점수가 아닌 LLM 근거 판정으로 결정 — BM25와 코사인 유사도 **둘 다** 답변 가능/불가를 구분하지 못했다. 평가셋에서 정답 없는 질문이 정답 있는 질문보다 높은 점수를 받았다. 유사도는 "관련 있는가"를 재지 "답이 들어 있는가"를 재지 않는다.
- 규칙 트리거를 키워드에서 LLM 추출 의도로 교체 — 한국어는 조사·합성어 때문에 부분문자열 매칭이 구조적으로 충돌한다("상"→이상·증상, "형"→유형, "이사"→이사회). 판정 호출에 의도 추출을 합쳐 호출 수는 늘리지 않았다.
- 별도 리랭커 대신 검색 후보 상한을 12→20으로 확대 — 판정기가 모든 근거를 읽고 추론하므로 후보를 넓게 주면 그 단계에서 실질적 재정렬이 일어난다. **검색 평가에서 hit@20이 100%로 확인됐다**: 판정기는 항상 정답 조항을 보고 있다. 코퍼스가 112청크·19,359자라 가능한 선택이며, 문서가 늘면 별도 리랭커가 필요해진다.
- 테스트 기대값을 답변 형태가 아닌 오지급 단정 금지로 전환 — 규칙 삭제 후 답변이 이관·되묻기·조건부 안내 사이에서 갈린다. 셋 다 규정에 맞으므로 형태를 고정하면 테스트가 흔들린다. 낱말 단위도 마찬가지다(A-09의 "최장"/"최대").
- **후보를 지우는 키워드 규칙은 해롭고, 더하는 확장은 값을 한다.** 같은 "하드코딩"으로 묶여 있지만 방향이 반대다. 파일 배제 필터를 지우니 전 지표가 개선됐고(hit@20 88.6%→100%), 검색어 주입 블록을 지우니 악화됐다(hit@3 79.5%→72.7%). 측정 없이 "하드코딩 제거"로 함께 묶으면 안 된다.
- 제도 섞임 방지는 `TOP_FILE_SCORE_RATIO`(0.6)의 점수 기반 파일 컷이 담당한다. 낱말이 아니라 점수로 가리므로 진짜 모호한 질문만 여러 규정이 섞여 판정 단계로 넘어간다.

## Traps to Avoid
- **검색 점수로 임계값을 만들려 하지 말 것.** 초기에 "변별력이 생겼다"고 판단했으나 프로브 3개만 본 결과였고, 평가셋을 8건으로 늘리자 분리가 음수로 뒤집혔다. BIGRAM_WEIGHT 0.3/0.6/1.0 전부 음수였다.
- **BIGRAM_WEIGHT를 낮추지 말 것.** 2-gram 노이즈를 우려해 1.0→0.3으로 낮췄더니 상위 3건 정확도가 6/6→4/6으로 떨어져 되돌렸다.
- **평가셋이 필터에 유리하게 편향되지 않았는지 먼저 볼 것.** 초기 38문항은 전부 "질문이 속한 제도에 정답이 있는" 형태였다. 그런 문항만 재면 파일 배제 필터는 경쟁 청크를 지워주는 셈이라 무조건 좋게 보인다. 경계 문항 6건을 넣자 5건이 **미검색**(정답 파일이 후보에 아예 없음)으로 드러났다. 노브를 재기 전에 그 노브가 실패하는 문항이 평가셋에 있는지 확인할 것.
- **`starts_new_policy_topic`으로 후속 질문을 판별하지 말 것.** 후속 답변이 주제어를 포함하면("...출장 일정에도 포함되지 않았어") 새 주제로 오판한다. 검색 단계는 이력이 있으면 무조건 직전 질문을 붙이는 방식으로 처리했다(`retrieve_policy_node`). 규칙·생성 단계에는 아직 남아 있다.
- **판정 프롬프트에 오늘 날짜를 넣어도 신청기한 케이스는 안 바뀐다.** 원인이 날짜가 아니라 규정 5.2-5의 예외 조항(주관부서장 승인)이었다. 되묻는 것이 규정상 맞았다. 날짜 주입은 무해해서 남겨뒀다.
- **`구미` 케이스를 선별 로직 문제로 진단했으나 틀렸다.** 융합 점수가 정직하게 밀어낸 것이다. 벡터가 순위를 망친 사례지만 전체적으로는 벡터가 이득이라 VECTOR_WEIGHT 0.5를 유지했다.
- **고정 문구 규칙은 인접 사안에서 조용히 틀린다.** `build_housing_move_answer`가 재부임(5.1-6: 1년, 합산 3년 이하일 때만 3년)을 모른 채 신규 부임 표를 하드코딩해 2년 초과 지급을 단정했다. 규칙이 답을 반환하면 `choose_after_rules`가 그래프를 끝내므로 판정 경로로 내려갈 기회도 없다. 규칙을 추가할 때는 그 규칙이 가로막을 인접 사안을 먼저 볼 것.
- **판정 스키마의 필드가 비었을 때를 "해당함"으로 처리하지 말 것.** `sibling_case`가 relation이 null일 때도 참이어서 "유형"의 '형'에 20만원 지급을 확정했다. relation이 '본인'으로 나오는 회차엔 통과하고 null인 회차엔 실패해 같은 질문이 회차마다 갈렸다.
- **PROTECTED_CASES에 케이스를 추가할 때 리스트 닫는 `]` 위치를 확인할 것.**

## Relevant Files
- app.py:151-172 — `strip_particle`/`tokens`/`bigrams`. 조사 제거와 음절 2-gram.
- app.py:211-213 — `BIGRAM_WEIGHT`(1.0), `TOP_FILE_SCORE_RATIO`(0.6). 후자가 제도 섞임을 막는 유일한 장치다.
- app.py:216-266 — `load_policy_index`(idf·임베딩 캐시), `score_chunk`(BM25). 경로+본문을 함께 임베딩한다(`searchable`). 임베딩 실패 시 키워드 단독으로 폴백.
- app.py:269 — `VECTOR_WEIGHT`(0.5). 44문항으로 재측정한 적 없다.
- app.py:320-390 — `retrieve`. 하이브리드 융합, 파일별 상위 3건 확보 후 남는 자리 채움. 파일 배제 필터는 제거됐고 검색어 확장(`CANONICAL_QUERY_TERMS`, 숙소 토큰 주입, `QUERY_SYNONYMS`)은 남아 있다.
- app.py:944-1038 — `GROUNDEDNESS_INSTRUCTIONS`와 `judge_groundedness`. verdict/intent/relation/finding을 한 번에 뽑는다.
- app.py:1052-1120 — `analyze_question_node`, `rule_applies`, `apply_policy_rules_node`. 의도·관계로 규칙을 가른다.
- app.py:1123-1190 — `build_clarify_answer`/`build_escalation_answer`/`generate_answer_node`. 3단계 분기.
- test_rules.py — 답변 회귀 22건((A) 18 보호 동작 + (B) 4 오탐). 실행에 `SSL_CERT_FILE=$(python3 -m certifi)` 필요.
- eval_retrieval.py — 검색 회귀 44문항. 질문마다 정답 조항의 계층 경로를 라벨로 달아 hit@k·MRR을 낸다. 라벨은 실행 시 실제 청크와 대조 검증하므로 규정이 바뀌면 실행이 멈춘다. 질문 임베딩도 캐시를 타서 재실행 무료. `-v`로 전 문항 순위.
- 규정/숙소지원금 이슈 샘플.txt — 실제 상담 이슈 로그 78줄(약 30건). SOURCE_FILES에 없어 검색 대상이 아니다. 평가 문항의 출처.

## Open Work
- **`VECTOR_WEIGHT`/`BIGRAM_WEIGHT`를 44문항으로 재측정하지 않았다.** 현재 값은 8건 평가셋 시절 결정이다.
- **`retrieve`의 `limit=20`을 줄일 수 있는지 미확인.** hit@10이 93.2%라 20→10은 아직 손실이 있다. 판정 호출의 토큰 비용을 낮추려면 여기가 지점이다.
- 상위권이 약하다(hit@1 45.5%). hit@20이 100%인 지금은 여유분이지만, 후보 상한을 줄이거나 코퍼스가 커지면 병목이 된다.
- LH 전세임대 문의는 clarify에 머물고 담당자 이관까지 가지 않는다. 규정이 재임대 형태를 다루지 않는 사안이다.
- **날짜 계산 도구는 아직 필요 없다.** 미커버로 지적됐던 유형 4개를 실제로 태워본 결과 휴직 8개월 지원기간 산정·15일 경계·신청기한 6개월 역산은 맞게 답했고, 틀린 것은 재부임 하나였는데 원인이 날짜가 아니라 고정 문구 규칙이었다(수정 완료, 회귀에 추가).
- 추출 스키마가 실패 케이스마다 필드 하나씩 늘던 흐름은 `receiving_support` 제거로 한 칸 되돌렸다(relation → finding). 규정에서 사실 스키마를 한 번에 도출하는 대안은 여전히 미착수.
- 실제 이슈 로그는 숙소지원금 영역에만 존재한다. 경조금·여비·동호회 검증은 합성 질문에 의존한다.
- 브랜치는 push되지 않았고 main은 그대로다. 수강생 배포 시작점을 main으로 유지하려던 의도였다.
- `~/.env`가 홈에 남아 있다. 프로젝트로 복사한 `.env`는 gitignore 적용됨.
- 이 Python 설치에 CA 인증서가 없어 모든 HTTPS 호출이 실패한다. 영구 수정은 `/Applications/Python 3.13/Install Certificates.command` 실행.

## Prompt for New Chat
handoff.md를 읽고, 여기 나열된 파일들을 실제로 Read 도구로 열어서 문서 내용을 코드와 대조 검증한 뒤, 검증되면 이어서 작업해줘. 이 문서는 사실이 아니라 가설로 취급해. 검증은 `test_rules.py`(22건)와 `eval_retrieval.py`(44문항) 두 개를 돌려 문서에 적힌 숫자와 맞는지 보면 된다.
