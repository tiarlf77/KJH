# 프로젝트 작업 지침

이 문서는 이 프로젝트에서 작업하는 Agent가 따라야 할 협업, 개발, 보안 규칙을 정의한다.

## 나와 일하는 방식

- 모든 사용자 응답은 한국어로 작성한다.
- 사실, 요구사항, 프로젝트 상태가 확실하지 않으면 추측하지 않고 사용자에게 먼저 질문한다.
- 코드를 작성하거나 수정하기 전에는 작업 계획을 먼저 사용자에게 보여주고 확인을 받는다.
- 계획에는 변경 대상 파일, 핵심 변경 내용, 검증 방법을 포함한다.

## 개발 규칙

- 구현 코드는 원칙적으로 파이썬 파일 하나에 작성한다.
- 코드는 읽기 쉽고 단순하게 유지하며, 파일을 여러 개로 나누어야 하는 경우 먼저 사용자에게 이유를 설명하고 확인을 받는다.
- 주석은 한글로 작성하고, 코드의 의도와 판단 이유를 이해하기 쉽게 설명한다.
- 새 라이브러리나 패키지를 추가해야 하는 경우 사용 목적과 대안을 먼저 설명하고 사용자에게 승인을 받는다.
- 기존에 설치되어 있거나 프로젝트에서 이미 사용하는 라이브러리는 현재 설정과 사용 방식을 먼저 확인한 뒤 사용한다.

## 보안

- API 키와 비밀값은 반드시 `.env` 파일에서 읽는다.
- 소스 코드, 주석, 테스트 코드, 로그, 문서에 API 키나 비밀값을 직접 작성하지 않는다.
- `.env` 파일의 키 값은 비워 둔다. 실제 값은 사용자가 직접 입력한다.
- 비밀값이 출력되거나 커밋되지 않도록 로그와 파일 변경 내용을 확인한다.

## 기획 문서

복리후생 통합 상담 챗봇 또는 적정성 검토 Agent의 기능·데이터·화면·처리 흐름을 설계하거나 구현할 때는 작업 전에 반드시 [PRD.md](PRD.md)를 읽고 요구사항을 반영한다. PRD의 내용과 사용자 요청이 충돌하면 사용자에게 확인한다.

## 계획 실행 규칙

- 모든 구현 작업은 [PLAN.md](PLAN.md)에 작성된 순서를 따른다.
- `PLAN.md`의 첫 번째 미완료 체크박스 항목만 다음 작업으로 선택한다.
- 현재 항목을 완료하고 검증하기 전에는 다음 항목으로 넘어가지 않는다.
- 항목을 완료하면 즉시 `PLAN.md`의 해당 체크박스를 `- [x]`로 표시한다.
- 하나의 항목을 완료할 수 없으면 추측으로 진행하지 말고, 원인과 필요한 사용자 결정을 먼저 보고한다.
- 작업 시작 전과 완료 후 `PLAN.md`의 체크박스 상태를 확인한다.

## 작업 완료 기준

- 계획에 명시한 파일과 범위만 변경한다.
- 구현 후 관련 검증을 수행하고 결과를 한국어로 보고한다.
- 미확인 사항, 남은 작업, 사용자에게 필요한 설정이 있으면 완료 보고에 명시한다.
