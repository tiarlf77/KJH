# 통합 작업 문서

> 프로젝트의 기획·계획·인수인계·구현·테스트·규정 문서를 파일별 원문 형태로 통합한 문서입니다.
> `.env`, 캐시, 백업, 압축파일, 이미지, 작업 지침 및 중복 TXT 원본은 포함하지 않았습니다.

## 포함 파일

- `PRD.md`
- `PLAN.md`
- `handoff.md`
- `app.py`
- `index.html`
- `eval_retrieval.py`
- `test_rules.py`
- `규정/경조금 지급기준.md`
- `규정/동호회 관리 규정.md`
- `규정/사내 추가 기준.md`
- `규정/숙소지원금 운영 기준.md`
- `규정/여비관리 FAQ.md`
- `규정/여비관리기준.md`

## PRD.md

원본 경로: `PRD.md`

````markdown
# PRD: RAG 기반 사내 복리후생 통합 상담 및 적정성 검토 Agent

## 1. 문서 개요

### 1.1 목적

본 문서는 사내 복리후생 규정, 운영 지침, 신청 절차 및 관련 FAQ를 근거로 직원의 질문에 답하고, 복리후생 신청 또는 문의 내용의 적정성을 사전 검토하는 AI 시스템의 요구사항과 설계 방향을 정의한다.

### 1.2 문서 상태

- 상태: 초안
- 대상 독자: 복리후생 운영 담당자, HR/노무 담당자, 보안 담당자, AI/플랫폼 개발자
- 핵심 원칙: 근거 우선, 최신 규정 우선, 개인정보 최소 처리, 최종 의사결정은 담당자 권한 유지

### 1.3 용어

| 용어 | 정의 |
|---|---|
| 복리후생 지식 | 복리후생 규정, 세부 운영 기준, FAQ, 신청 양식, 공지, 증빙 기준 등 |
| RAG | 사용자 질문을 지식 저장소에서 검색한 뒤 검색 결과를 근거로 답변을 생성하는 방식 |
| 상담 Agent | 직원의 복리후생 질문을 분류·검색·답변하고 필요한 경우 담당자에게 이관하는 Agent |
| 적정성 검토 Agent | 신청 내용과 근거 규정을 비교해 충족·불충족·추가 확인 필요 여부를 제시하는 Agent |
| 근거 | 답변 또는 판정에 사용된 문서, 조항, 시행일, 문서 버전 및 검색 구간 |
| 최종 승인 | HR/복리후생 담당자가 수행하는 공식 판단 및 처리 |

## 2. 배경과 문제 정의

현재 복리후생 문의는 규정과 공지의 분산, 시행일 차이, 개인별 자격 조건, 담당자별 해석 차이로 인해 다음 문제가 발생할 수 있다.

- 직원이 어떤 제도에 해당하는지 찾기 어렵다.
- 같은 질문에 대해 답변 속도와 표현이 달라진다.
- 오래된 규정이나 예외 조건이 누락될 수 있다.
- 신청 서류의 누락과 부적정 신청을 사전에 발견하기 어렵다.
- AI 답변이 공식 판단으로 오인될 위험이 있다.

따라서 시스템은 단순한 문서 검색 챗봇이 아니라, 규정의 유효기간과 적용 범위를 고려하고 모든 답변·판정에 추적 가능한 근거를 연결하는 업무 지원 모듈이어야 한다.

## 3. 목표와 비목표

### 3.1 목표

1. 직원이 자연어로 복리후생 제도와 신청 절차를 문의할 수 있게 한다.
2. 답변마다 관련 문서와 조항, 적용 기준일을 제시한다.
3. 사용자 속성(소속, 직급, 고용 형태 등)을 필요한 범위에서만 사용해 개인화한다.
4. 신청 내용의 자격, 기간, 금액, 증빙 요건을 구조화해 적정성을 사전 검토한다.
5. 불확실하거나 상충하는 규정은 추측하지 않고 추가 확인 또는 담당자 이관으로 처리한다.
6. 운영자가 문서를 교체·폐기·재색인하고 답변 품질을 모니터링할 수 있게 한다.

### 3.2 비목표

- AI가 인사·노무·세무상 최종 결정을 내리는 것
- 규정에 없는 혜택이나 예외를 창작하는 것
- 직원의 민감정보를 장기간 축적하는 것
- 기존 HR/전자결재 시스템의 모든 기능을 대체하는 것

## 4. 사용자와 주요 시나리오

### 4.1 직원: 제도 문의

> “올해 건강검진 지원 대상과 신청 방법이 뭐야?”

시스템은 질문을 제도와 절차로 분류하고, 기준일에 유효한 문서를 검색해 대상 조건·지원 범위·신청 절차·기한·필요 서류를 답변한다.

### 4.2 직원: 개인 상황 문의

> “지난달 입사했고 배우자 검진비를 신청할 수 있나요?”

시스템은 입사일, 가족 관계, 신청 기간 등 필요한 정보만 확인한다. 확인되지 않은 사실은 가정하지 않고 추가 질문을 한다.

### 4.3 직원: 신청 전 적정성 검토

사용자가 제도, 사용일, 금액, 대상자, 증빙 보유 여부를 입력하면 시스템은 다음을 구분해 제시한다.

- 충족 가능: 현재 입력과 근거상 요건을 만족
- 불충족 가능: 특정 요건을 만족하지 못함
- 추가 확인 필요: 규정 충돌, 정보 부족, 담당자 판단 필요

판정에는 반드시 적용한 규정과 판단 근거를 함께 표시한다.

### 4.4 담당자: 검토 및 이관 처리

담당자는 대화 요약, 사용자 입력, 적용 규정, 누락 정보, AI 판정 및 신뢰도/경고를 확인한 뒤 승인·반려·추가자료 요청을 수행한다.

## 5. 기능 요구사항

### 5.1 상담 기능

- 자연어 질의 및 대화 맥락 유지
- 의도 분류: 제도 안내, 자격 확인, 신청 절차, 금액/한도, 기한, 증빙, 상태 조회, 담당자 연결
- 복리후생 제도별 답변 템플릿
- 답변 근거 문서와 조항 표시
- 기준일 및 문서 시행일 표시
- 모호한 질문에 대한 최소 확인 질문
- 답변 불가, 규정 부재, 규정 충돌 시 명시적 보류
- 담당 부서/문의 채널로 이관

### 5.2 적정성 검토 기능

- 신청 유형별 입력 스키마 제공
- 규정에서 추출한 자격·기간·금액·증빙 조건과 입력값 비교
- 누락 항목 및 추가 제출 서류 안내
- 조건별 판정 결과와 근거 표시
- 예외·재량·노무 판단이 필요한 건의 자동 승인 금지
- 결과 상태와 검토 시점 저장

### 5.3 운영자 기능

- 문서 등록, 메타데이터 입력, 버전 관리, 폐기
- 시행일/종료일, 적용 조직, 대상 고용 형태, 보안 등급 관리
- 문서 파싱·청킹·임베딩·색인 상태 확인
- 대표 질문과 정답 근거를 이용한 회귀 평가
- 답변 로그, 이관율, 보류율, 근거 누락률 모니터링
- 긴급 규정 변경 시 특정 문서의 우선 검색 또는 서비스 차단

## 6. 비기능 요구사항

- 정확성: 근거 없는 단정적 답변을 최소화한다.
- 최신성: 기준일에 유효한 문서를 우선하며 폐기 문서가 검색되지 않도록 한다.
- 추적성: 모든 답변과 검토 결과에 문서 버전 및 근거 구간을 남긴다.
- 보안: 사내 인증과 권한에 따라 문서 및 사용자 정보 접근을 제한한다.
- 개인정보 보호: 답변에 불필요한 개인정보를 수집·저장하지 않는다.
- 가용성: 일반 상담과 담당자 이관을 분리해 한 기능의 장애가 전체 상담을 막지 않게 한다.
- 성능 목표(초기 제안): 일반 질문의 p95 응답 시간 5초 이내, 적정성 검토 10초 이내.
- 설명 가능성: “왜 그런 결과가 나왔는지”를 조건별로 설명할 수 있어야 한다.

## 7. 제안 아키텍처

```text
[사내 포털/메신저]
          |
 [인증·권한 Adapter]
          |
 [상담/검토 Orchestrator]
      /        |         \
 [질의분류] [RAG 검색] [적정성 규칙 검토]
      |        |         |
 [대화상태] [문서/벡터 저장소] [조건·판정 엔진]
          \    |         /
        [근거 조립·응답 검증]
                  |
        [답변/판정 + 근거 + 이관]
```

### 7.1 핵심 모듈과 인터페이스

#### 상담 모듈

작은 인터페이스로 다음 동작을 제공한다.

```text
answer(request: UserQuestion, context: UserContext) -> AnswerResult
```

`AnswerResult`는 답변 본문, 확인 질문, 근거 목록, 경고, 이관 여부를 포함한다. 모델 호출과 검색 구현은 내부 구현으로 숨기고, 테스트에서는 검색 Adapter와 LLM Adapter를 대체할 수 있어야 한다.

#### 검색 모듈

```text
retrieve(query: SearchQuery) -> EvidenceSet
```

검색 시 문서의 시행일, 종료일, 적용 대상, 조직, 보안 등급을 필터링한 후 키워드 검색과 벡터 검색을 결합한다. 검색 결과는 원문 위치와 문서 버전을 보존해야 한다.

#### 적정성 검토 모듈

```text
review(application: ApplicationInput, asOf: Date) -> ReviewResult
```

판정의 핵심은 생성형 모델이 아니라 구조화된 조건 평가로 둔다. 생성형 모델은 입력 추출, 누락 질문, 사람이 읽을 설명을 보조하며, 규칙·계산·기간 비교는 결정론적 구현으로 검증한다.

#### 문서 관리 모듈

```text
publish(document: PolicyDocument) -> PublicationResult
```

문서의 유효기간과 버전을 검증한 뒤 색인한다. 동일 제도에 겹치는 유효기간이나 상충하는 우선순위가 있으면 게시를 막거나 운영자 확인 상태로 둔다.

## 8. RAG 설계

### 8.1 문서 메타데이터

최소한 다음 필드를 관리한다.

- `document_id`, `version`, `title`, `policy_type`
- `effective_from`, `effective_to`, `published_at`
- `applies_to`: 법인/조직/고용 형태/직급 등
- `confidentiality`: 공개 범위 및 접근 등급
- `source_uri`, `owner`, `last_reviewed_at`
- `supersedes`: 대체하는 이전 문서 식별자

### 8.2 수집·색인

1. 원천 문서 수집 및 바이러스/파일 형식 검사
2. 본문·표·각주·첨부파일 추출
3. 조항/제도 단위의 의미 있는 청킹
4. 문서 버전·시행일·접근권한 메타데이터 부착
5. 키워드 색인과 임베딩 색인 생성
6. 대표 질문으로 검색 품질 검증 후 게시

표와 조건식은 일반 문단과 다르게 보존한다. 금액, 비율, 기간, “단서”, “다만” 같은 예외 표현은 청킹 과정에서 분리되지 않도록 한다.

### 8.3 검색·생성 정책

- 사용자 질문의 기준일을 먼저 결정한다. 기준일이 없으면 현재일을 사용하되 답변에 표시한다.
- 접근 권한과 적용 대상 필터를 검색 단계에서 적용한다.
- 최신 유효 문서를 우선하되, 상위 규정과 하위 운영 지침의 관계를 함께 고려한다.
- 검색 근거가 충분하지 않으면 답변 생성을 중단하고 추가 정보 또는 담당자 확인을 요청한다.
- 답변에는 검색된 근거에 없는 숫자, 기한, 자격 조건을 새로 만들지 않는다.

## 9. 적정성 검토 판정 모델

### 9.1 판정 단계

1. 신청 유형 식별
2. 입력값 정규화: 날짜, 금액, 조직, 대상자 관계 등
3. 필수 정보 누락 확인
4. 적용 가능한 규정 집합 결정
5. 조건별 평가
6. 예외·상충·재량 여부 확인
7. 결과와 근거 생성

### 9.2 결과 구조

```json
{
  "status": "eligible | ineligible | needs_review",
  "as_of": "YYYY-MM-DD",
  "checks": [
    {
      "item": "지원 대상 여부",
      "result": "pass | fail | unknown",
      "reason": "판정 설명",
      "evidence": ["문서ID:버전:조항"]
    }
  ],
  "missing_information": [],
  "warnings": [],
  "human_review_required": true
}
```

`eligible`는 공식 승인과 동일한 의미가 아니다. 사용자 화면에서는 “신청 가능성이 높음/추가 확인 필요”처럼 오해를 줄이는 문구를 사용하고, 최종 처리는 기존 승인 절차를 따른다.

## 10. 안전·보안·거버넌스

- 사내 SSO와 역할 기반 접근 제어를 적용한다.
- 민감한 인사정보는 필요한 필드만 토큰화하거나 마스킹한다.
- 사용자 입력, 검색 근거, 모델 출력, 담당자 수정 이력을 감사 로그로 남긴다.
- 모델 학습 데이터로 대화 로그를 재사용할 경우 별도 동의·비식별화·보존기간 정책을 적용한다.
- 프롬프트 인젝션과 문서 내 악성 지시를 데이터로 취급하고, 시스템 지시보다 우선하지 않게 한다.
- 문서 접근권한은 검색 결과뿐 아니라 인용문과 요약에도 동일하게 적용한다.
- 규정 변경 시 영향받는 제도와 평가 세트를 재검증한다.
- 의료·노무·세무 판단 또는 분쟁 가능성이 있는 질문은 전문 담당자 이관을 기본값으로 한다.

## 11. 평가 계획

### 11.1 오프라인 평가 세트

복리후생 담당자가 작성한 대표 질문·신청 사례를 다음 유형으로 구성한다.

- 단순 사실 질문
- 여러 조건이 필요한 자격 질문
- 시행일이 다른 문서가 있는 질문
- 예외 조항이 포함된 질문
- 규정에 없는 질문
- 개인정보 또는 권한이 필요한 질문
- 상충 규정 및 담당자 이관 사례

### 11.2 주요 지표

- 근거 적합성: 답변 주장과 인용 근거의 일치율
- 근거 누락률: 근거가 필요한 주장 중 인용이 없는 비율
- 검색 재현율: 정답 문서를 검색 결과에 포함한 비율
- 답변 정확도: 담당자 정답과의 일치율
- 보류 정확도: 답변하면 안 되는 사례를 보류한 비율
- 적정성 판정 일치율: 담당자 판정과의 일치율
- 이관 적절성: 이관이 필요한 사례의 누락률
- 최신성 오류율: 폐기·미시행 문서를 근거로 사용한 비율

### 11.3 출시 기준 제안

정량 기준은 실제 데이터 확보 후 확정하되, 다음 항목은 출시 차단 조건으로 둔다.

- 근거 없는 확정 답변이 발생하는 핵심 시나리오가 존재함
- 폐기된 규정을 현재 규정으로 답변함
- 접근 권한이 없는 문서 내용이 노출됨
- 적정성 검토 결과가 공식 승인으로 오인될 수 있음
- 규정 충돌을 감지하지 못하고 단일 답변을 단정함

## 12. 단계별 구현 계획

### Phase 1: 읽기 전용 상담 MVP

- 핵심 복리후생 3~5개 제도 선정
- 문서 메타데이터와 버전 관리
- 하이브리드 검색 및 근거 인용
- SSO, 기본 감사 로그, 담당자 이관

### Phase 2: 적정성 사전 검토

- 제도별 입력 스키마
- 결정론적 조건·기간·금액 검토 엔진
- 누락 서류 안내와 검토 결과 저장
- 담당자 검토 화면 및 수정 이력

### Phase 3: 업무 시스템 연계

- HR 마스터와 사용자 속성 연계
- 전자결재/복지몰 신청 상태 조회
- 문서 게시 승인 워크플로
- 지속 평가 및 품질 대시보드

## 13. 주요 리스크와 대응

| 리스크 | 대응 |
|---|---|
| 오래된 규정 검색 | 유효기간 필터, 폐기 문서 색인 제외, 최신성 회귀 테스트 |
| 예외 조항 누락 | 조항 단위 청킹, 표/각주 보존, 예외 사례 평가 |
| 환각 또는 과도한 확신 | 근거 필수, 임계값 미달 시 보류, 답변 검증 단계 |
| 개인정보 노출 | 최소 수집, 마스킹, 권한 필터, 보존기간 제한 |
| 규정 해석의 법적/노무적 위험 | 최종 승인권자 유지, 고위험 질문 이관, 감사 로그 |
| 사용자 속성 오류 | HR 원천 데이터 기준시각 표시, 불일치 시 추가 확인 |
| 담당자 신뢰 부족 | 판정 근거·규칙·수정 이력을 투명하게 제공 |

## 14. 오픈 이슈

- 우선 도입할 복리후생 제도와 원천 문서 목록은 무엇인가?
- 사용자 인증과 HR 속성 연계의 시스템 오너는 누구인가?
- 최종 승인과 예외 판단을 담당할 조직 및 SLA는 어떻게 정할 것인가?
- 문서별 보안 등급과 대화 로그 보존기간은 어떻게 정의할 것인가?
- 적정성 검토 결과를 기존 신청 시스템에 저장할지, 별도 검토 기록으로 유지할 것인가?
- 생성형 모델의 사내 반출 금지 데이터와 허용 가능한 모델 운영 방식은 무엇인가?

## 15. 성공 기준

초기 운영 기간 동안 직원이 자주 묻는 복리후생 질문의 상당 부분을 근거와 함께 즉시 안내하고, 담당자는 반복 문의와 누락 서류 확인에 쓰는 시간을 줄인다. 동시에 시스템의 모든 답변과 적정성 검토는 공식 규정과 구분되어야 하며, 사람이 결과를 검증·수정·승인할 수 있어야 한다.
````

## PLAN.md

원본 경로: `PLAN.md`

````markdown
# 구현 계획: RAG 기반 사내 복리후생 통합 상담 및 적정성 검토 Agent

> 상태: PRD.md 기반 계획 초안
>
> 기존 요구사항 문서를 `PRD.md`로 정리했다. 아래 계획은 해당 문서를 기준으로 하며, 세부 우선순위와 완료 기준은 사용자 확인 후 확정한다.
>
> 이 문서는 계획만 담으며, 현재 단계에서는 코드를 작성하지 않는다.

## 1. 착수 전 확인

- [x] `PRD.md`를 프로젝트 루트에 추가한다.
- [x] `PRD.md`의 목표, 사용자, 필수 기능, 제외 범위, 성공 지표를 계획과 대조한다.
- [x] `숙소지원금 운영 기준.txt`를 제목 계층 기반의 Markdown 문서로 정립하고 원문과 대조한다.
- [x] `여비관리기준.txt`를 제목 계층 기반의 Markdown 문서로 정립하고 원문과 대조한다.
- [x] 조사·어미가 결합된 결혼 문의에서도 경조금 규정을 검색하도록 토큰 정규화를 보완하고 회귀 검증한다.
- [x] 주말 복귀 출장 문의를 근태 연계 예외사항으로 보류하고 담당 부서 확인을 안내하도록 보완한다.
- [x] 개인 사유로 출장 전에 주말 조기 이동하는 문의를 비용 인정 예외사항으로 분리하고 담당 부서 확인을 안내한다.
- [x] 해외출장 후 개인휴가를 사용하고 귀국하는 항공편 문의를 예외사항으로 분리하고 담당 부서 확인을 안내한다.
- [x] 해외출장 일정 종료 후 개인 연차 체류 시 귀국 항공편 미지원 원칙을 FAQ와 판정 로직에 반영한다.
- [x] `app.py`의 리프 헤딩 청킹을 path/text 메타데이터로 분리하고, 제목 검색·LLM 프롬프트·본문 응답·fallback 호환성을 검증한다. 여비 검색에는 Markdown 규정을 사용한다.
- [x] 숙소지원금 검색 대상과 전용 필터를 `숙소지원금 운영 기준.md`로 전환하고, 전체 청크의 path/text·계층·본문 보존 및 실제 검색 결과를 검증한다.
- [x] 규정별 검색 필터 3곳을 stem 비교로 전환하고 숙소지원금·여비 근거 라벨 4곳을 Markdown 파일명으로 맞춘다. 확장자별 필터·실제 검색·규정 링크 및 TXT 원본 보존을 검증한다.
- [x] 동호회 개설 표현을 신규 신청 의도로 분류하고 절차형 답변으로 개선한다. 신청 메일 버튼은 백엔드 UI 액션으로 제어하고, 가입·지원금·일반 문의를 분리해 회귀 검증한다.
- [x] `CONSULTATION_GRAPH.invoke`를 seam으로 삼아 과거 수정 동작 15건과 미해결 오답 4건을 검증하는 `test_rules.py` 회귀 스크립트를 만든다.
- [x] 해외출장 전일 이동 답변에 여비관리 FAQ·숙소지원금·경조금 링크가 섞이는 검색·대화 재작성 경로를 재현하고 원인을 확인한다.
- [x] 현재 질문의 핵심 제도에 맞는 파일만 근거로 남기도록 검색 결과 파일 필터를 보완한다.
- [x] 해외출장 전일 이동 대화 이력에서 여비관리기준만 노출되는 회귀 검증을 추가한다.
- [x] 전체 회귀·HTTP API·근거 링크 결과를 검증하고 변경 범위와 비밀값을 점검한다.
- [x] 개인 일정 해외출장에서 FAQ가 표시되고 조의금 질문에 무관한 규정이 섞이는 근거 선택 경로를 재현하고 원인을 확인한다.
- [x] 해외출장 답변의 근거 링크에서 여비관리 FAQ를 제외하고 여비관리기준만 남긴다.
- [x] 조의금·경조사 표현을 경조금 지급기준으로 한정해 무관한 근거 파일을 제외한다.
- [x] 두 근거 링크 회귀 사례와 전체 HTTP API 결과를 검증하고 변경 범위·비밀값을 점검한다.
- [x] 동호회 분야별 허용·조건부 허용·제외 범위와 공통 승인 기준을 검토한다.
- [x] 동호회 관리 규정에 스포츠·레저 및 문화·예술·연구·학습 부문의 승인 범위를 명시한다.
- [x] 동아리 표현도 동호회 관리 규정을 우선 검색하도록 보완한다.
- [x] 낚시 동호회·헬스 동아리 질문에서 분야별 기준이 검색되는지 회귀·HTTP API로 검증한다.
- [x] PRD와 현재 계획이 충돌하는 항목을 목록화하고 사용자에게 확인받는다.
- [x] 승인된 디자인 시안을 기준으로 Codex형 상담 UI를 `index.html`에 선행 반영하고 기존 상담·근거·문의 기능과 반응형 화면을 검증한다.
- [x] Codex형 상담 UI에서 미구현 버튼·임시 최근 상담·예시 사용자 정보를 제거하고 실제 동작 기능과 반응형 화면을 재검증한다.
- [x] 혼인신고 전 결혼예정자의 주소 전입과 실제 동거를 구분해 숙소지원금 예외 기준·판정 지침·회귀 검증에 반영한다.
- [x] 검색 후보와 답변에 실제 사용한 근거를 분리해 화면의 `확인한 규정` 링크를 정제한다.
- [x] 브라우저에 최근 상담을 저장·복원·삭제하는 좌측 이력을 추가하고, 중복된 우측 근거 패널을 제거한 뒤 반응형 화면을 검증한다.
- [x] 기본 상담 버튼 4개를 제도 전체 기준 검색으로 정비하고, 카드·좌측 메뉴의 질문 전달을 검증한다.
- [x] 담당자 문의·동호회 신청 모달의 하단 버튼을 취소·임시저장·발송으로 통일하고, 발송 버튼의 글꼴·크기·위치를 반응형으로 정비한다.
- [x] 담당자 문의 메일 초안의 관련 규정을 파일 단위로 중복 제거하고, 네 제도별 초안 API를 검증한다.
- [ ] 초기 출시 대상 복리후생 제도 3~5개를 확정한다.
- [ ] 원천 규정 문서, FAQ, 신청 양식, 공지의 위치와 담당자를 확정한다.
- [ ] 최종 승인권자, 담당 부서 이관 기준, 운영 SLA를 확정한다.

### 확인된 범위 결정

- 초기 화면의 상담 범위는 현재 구현된 출장·여비, 경조금, 숙소지원금, 동호회 4개 제도를 기준으로 한다.
- 문의 작성·저장·발송 요청은 실제 업무 시스템 연동 전까지 프로토타입 기능으로 표시한다.
- 적정성 검토와 파일 첨부 등 미구현 기능은 화면에 노출하지 않는다.
- 실제 데이터와 연결되지 않은 최근 상담 및 예시 사용자 영역은 화면에 노출하지 않는다.
- 기능 로직은 `app.py`, 화면은 기존 구조를 따라 `index.html`에 유지하며 UI 개편을 기능 로드맵보다 먼저 수행한다.

## 2. 범위와 완료 기준 확정

- [ ] 직원 상담 기능의 필수 질문 유형을 정의한다.
- [ ] 적정성 검토 대상 신청 유형과 입력 항목을 정의한다.
- [ ] AI가 답변할 수 있는 범위와 반드시 사람에게 이관할 범위를 정의한다.
- [ ] 답변·판정에 필요한 근거 표시 형식을 확정한다.
- [ ] “AI 검토 결과”와 “공식 승인”을 구분하는 사용자 문구를 확정한다.
- [ ] MVP의 기능 완료 기준과 출시 차단 조건을 확정한다.

## 3. 프로젝트 구조와 기술 선택

- [ ] 구현은 원칙적으로 하나의 Python 파일로 시작한다.
- [ ] 필요한 경우에만 파일 분리 여부와 분리 사유를 사용자에게 먼저 확인받는다.
- [ ] 기존 실행 환경과 설치된 패키지를 확인한다.
- [ ] 새 라이브러리가 필요한 경우 사용 목적, 대안, 보안·운영 영향을 정리해 사용자 승인을 받는다.
- [ ] API 키와 비밀값의 환경변수 이름을 정의한다.
- [ ] `.env`에 키 이름만 기록하고 실제 값은 비워 둔다.
- [ ] `.env`가 버전 관리 대상에서 제외되는지 확인한다.

## 4. 복리후생 지식 데이터 준비

- [ ] 원천 문서를 수집하고 파일 형식·손상 여부·접근권한을 확인한다.
- [ ] 문서별 `document_id`, 버전, 시행일, 종료일, 적용 조직, 고용 형태, 보안 등급을 정리한다.
- [ ] 상위 규정과 하위 운영 지침의 우선순위를 정의한다.
- [ ] 폐기 문서와 현재 유효 문서를 구분한다.
- [ ] 본문·표·각주·첨부파일을 추출하는 방식을 결정한다.
- [ ] 금액·비율·기간·예외 조건이 청킹 과정에서 분리되지 않도록 검증한다.
- [ ] 키워드 검색과 의미 검색을 결합한 검색 방식을 확정한다.
- [ ] 문서 게시 전 대표 질문으로 검색 결과와 근거 위치를 검증한다.

## 5. 상담 Agent 설계

- [ ] 질문 의도 분류 체계를 정의한다: 제도 안내, 자격, 금액·한도, 기한, 증빙, 신청 절차, 상태 조회, 담당자 연결.
- [ ] 사용자 질문의 기준일을 결정하는 규칙을 정의한다.
- [ ] 사용자 권한과 적용 대상 조건을 검색 단계에서 필터링한다.
- [ ] 모호한 질문에 필요한 최소 확인 질문을 정의한다.
- [ ] 답변 결과 인터페이스를 정의한다: 본문, 확인 질문, 근거, 경고, 이관 여부.
- [ ] 근거가 부족하거나 규정이 상충할 때 보류하는 조건을 정의한다.
- [ ] 답변 생성 후 근거와 주장 간 일치 여부를 검증하는 절차를 정의한다.
- [ ] 모델 Adapter와 검색 Adapter를 교체할 수 있는 seam을 정의한다.

## 6. 적정성 검토 Agent 설계

- [ ] 신청 유형별 입력 스키마를 정의한다.
- [ ] 날짜, 금액, 조직, 대상자 관계 등 입력값 정규화 규칙을 정의한다.
- [ ] 필수 입력 누락과 추가 제출 서류를 식별하는 규칙을 정의한다.
- [ ] 자격·기간·금액·증빙 조건을 결정론적으로 평가하는 검토 모듈을 설계한다.
- [ ] 생성형 모델의 역할을 입력 추출·질문 생성·설명 보조로 제한한다.
- [ ] 예외, 재량, 상충 규정, 노무·세무 판단 필요 사례의 이관 조건을 정의한다.
- [ ] `eligible`, `ineligible`, `needs_review` 결과와 조건별 근거 구조를 확정한다.
- [ ] 검토 결과에 기준일, 규정 버전, 누락 정보, 경고, 사람 검토 필요 여부를 포함한다.

## 7. 보안·개인정보·감사

- [ ] 사내 SSO와 역할 기반 접근 제어 방식을 확정한다.
- [ ] 사용자 속성 중 실제 판단에 필요한 최소 필드만 선정한다.
- [ ] 개인정보와 민감정보의 마스킹·토큰화 범위를 정의한다.
- [ ] 문서 권한이 검색 결과·인용문·요약에도 동일하게 적용되는지 검증한다.
- [ ] 대화, 검색 근거, 모델 출력, 담당자 수정 이력의 감사 로그 항목을 정의한다.
- [ ] 로그 보존기간과 비식별화 정책을 확정한다.
- [ ] 프롬프트 인젝션과 문서 내 악성 지시를 검출·무력화하는 방식을 정의한다.
- [ ] API 키가 코드·주석·테스트·로그에 노출되지 않는지 점검한다.

## 8. 구현 순서

- [ ] 1단계: 문서 메타데이터와 검색 가능한 지식 저장 구조를 만든다.
- [ ] 2단계: 읽기 전용 상담의 최소 인터페이스를 만든다.
- [ ] 3단계: 검색 결과의 시행일·권한 필터와 근거 인용을 연결한다.
- [ ] 4단계: 답변 보류와 담당자 이관 흐름을 구현한다.
- [ ] 5단계: 신청 유형별 입력과 적정성 검토 인터페이스를 만든다.
- [ ] 6단계: 날짜·금액·자격·증빙에 대한 결정론적 조건 평가를 구현한다.
- [ ] 7단계: 사용자 화면 또는 사내 채널 Adapter를 연결한다.
- [ ] 8단계: 운영자용 문서 게시·폐기·재색인 흐름을 연결한다.

## 9. 테스트와 평가

- [ ] 대표 질문·신청 사례로 오프라인 평가 세트를 작성한다.
- [ ] 단순 질문, 복합 조건, 시행일 차이, 예외 조항, 규정 부재, 권한 제한, 상충 규정을 포함한다.
- [ ] 검색 재현율과 근거 적합성을 측정한다.
- [ ] 근거 없는 답변, 최신성 오류, 권한 없는 문서 노출을 차단하는 테스트를 작성한다.
- [ ] 적정성 검토 결과와 담당자 판정의 일치율을 측정한다.
- [ ] 추가 확인·보류·담당자 이관의 적절성을 평가한다.
- [ ] 기준일이 바뀌었을 때 올바른 문서 버전을 선택하는지 검증한다.
- [ ] API 장애, 검색 실패, 모델 응답 지연 시 안전한 오류 처리를 검증한다.
- [ ] 출시 차단 조건이 하나라도 발생하면 원인을 수정한 뒤 재평가한다.

## 10. 운영과 출시

- [ ] 문서 변경 시 영향받는 제도와 평가 세트를 재검증하는 절차를 만든다.
- [ ] 답변 근거 누락률, 보류율, 이관율, 최신성 오류율을 모니터링한다.
- [ ] 운영자가 답변과 판정을 수정하고 피드백을 남길 수 있게 한다.
- [ ] 긴급 규정 변경 시 해당 문서의 우선순위를 조정하거나 관련 상담을 일시 보류할 수 있게 한다.
- [ ] 파일럿 사용자를 선정하고 실제 문의로 시범 운영한다.
- [ ] 파일럿 피드백을 반영해 답변 템플릿과 검토 규칙을 개선한다.
- [ ] 담당자 승인 후 단계적으로 운영 범위를 확대한다.
- [ ] 출시 후 정기적인 문서 최신성·권한·평가 세트 점검 일정을 확정한다.

## 11. 최종 완료 기준

- [ ] PRD의 필수 요구사항이 모두 구현 항목 또는 명시적인 제외 항목으로 추적된다.
- [ ] 모든 상담 답변에 접근 가능한 근거와 기준일이 표시된다.
- [ ] 적정성 검토가 공식 승인과 명확히 구분된다.
- [ ] 불확실·상충·고위험 사례가 사람에게 이관된다.
- [ ] API 키와 개인정보 보호 검증을 통과한다.
- [ ] 대표 평가 세트와 출시 차단 조건 검증을 통과한다.
- [ ] 운영 문서, 담당자 절차, 장애 대응 방법이 정리된다.
- [ ] 사용자에게 구현 결과와 검증 결과를 한국어로 보고한다.
````

## handoff.md

원본 경로: `handoff.md`

````markdown
## Summary
검색 가중치를 44문항으로 재측정해 `VECTOR_WEIGHT`를 0.5→0.8로 올렸고(hit@1 45.5→63.6%), 단계마다 제각각이던 대화 이력 처리를 질문 재작성 하나로 통일했다. 프롬프트에 복사돼 있던 국내출장 금액 기준을 걷어내자 가려져 있던 검색 결함(별첨 표 미검색)이 드러났다. 브랜치 커밋 32개, 회귀 25/25.

## Key Decisions
- `VECTOR_WEIGHT` 0.8 — 44문항 격자 스윕 결과. 0.9 이상은 MRR이 더 높지만 키워드가 지키던 문항 2건이 1위→9위 밖으로 밀리고, 평가셋이 패러프레이즈 편향이라 그 이득은 과대평가다. `BIGRAM_WEIGHT`는 1.0이 여전히 최선.
- 후속 질문 판별을 낱말 목록이 아니라 LLM 재작성으로 — `resolve_question`이 이력을 반영한 자립형 질문 하나를 만들고 검색·판정·생성이 그것을 함께 쓴다. 이력이 없거나 실패하면 원문으로 진행하므로 후속 질문에서만 호출이 는다.
- 판정에 최근 4턴을 넘김 — 재작성만으로는 부족했다. 판정이 자기가 무엇을 되물었는지 몰라 표현만 바꿔 같은 것을 다시 물었다.
- 되묻기 선택지를 검색 상위 조항 제목이 아니라 판정의 `missing`으로 — 순위 기반 목록은 질문과 무관한 조항("5.4 출장복명")과 FAQ 리프("질문"·"답변")를 올렸다.
- 평가셋을 두 세트로 분리 — 회귀 44문항(손으로 작성, 경로 라벨, 더 늘리지 않음)과 실사용 18문항(상담 로그에서 옮김, **본문 문자열 라벨**). 경로 라벨은 청킹을 바꾸면 통째로 흔들려 청킹 실험을 못 잰다.

## Traps to Avoid
- **`starts_new_policy_topic` 같은 낱말 판별을 되살리지 말 것.** 양방향으로 틀린다. "부모님 상당했는데"는 낱말이 안 걸려 지난 환갑 주제가 남았고, "해외출장입니다"는 '출장'이 걸려 필요한 이력이 끊겼다. 삭제됨.
- **44문항 평가로 청킹 변경을 판정하지 말 것.** 표를 별도 청크로 끊는 실험에서 MRR 0.744→0.707로 보였으나, 검색은 오히려 정확해졌고 라벨이 옛 경로를 가리켜 생긴 하락이었다. 실사용 세트의 본문 라벨로 재야 한다.
- **`hit@20 100%`를 커버리지로 읽지 말 것.** 44문항은 코퍼스를 보면서 만들었다. 즉석에서 만든 "국내출장 숙박비 한도가 얼마인가요?"는 미검색이다.
- **판정 지시문 추가분("사용자가 답할 수 있는 사실만 missing")을 되돌리지 말 것.** 과적합으로 의심해 A/B 했으나, 뒤집힌 문항은 근거에 답이 실제로 없어 escalate가 정직한 판정이었다. 이력 전달만으로는 되묻기 반복이 안 멎는다(3조건 비교 완료).
- **규정에 없는 값을 환각으로 단정하지 말 것.** `현진시닝 : 1600-0113`은 사내 추가 규칙이며 `app.py:536,610,806`에 의도적으로 있다. 이번 세션에서 두 번 오진했다.
- **`test_rules.py`는 회차마다 1~2건 흔들린다.** 생성 답변이 LLM이라 A-11(15일 경계)·B-03(이사회) 같은 케이스가 간헐 실패한다. 실패를 회귀로 단정하기 전에 그 케이스만 3회 다시 태울 것 — 실제로 두 건 다 6/6으로 통과했다. 정답표를 지우면 전 케이스가 LLM 경로로 내려가므로 흔들리는 범위가 넓어진다.
- **고정 문구 규칙은 인접 사안에서 조용히 틀린다.** 삭제된 `build_housing_move_answer`가 재부임(5.1-6: 1년, 합산 3년 이하일 때만 3년)을 모른 채 신규 부임 표를 하드코딩해 2년 초과 지급을 단정했다. 규칙이 답을 반환하면 `choose_after_rules`가 그래프를 끝내므로 판정 경로로 내려갈 기회조차 없다.

## Relevant Files
- app.py:272 — `VECTOR_WEIGHT`(0.8). 211-213의 `BIGRAM_WEIGHT`(1.0), `TOP_FILE_SCORE_RATIO`(0.6)와 함께 검색 노브 전부.
- app.py:967-1035 — `RESOLVE_INSTRUCTIONS`, `resolve_question`. 그래프 첫 노드(1037).
- app.py:867-963 — `GROUNDEDNESS_INSTRUCTIONS`, `judge_groundedness(question, evidence, history)`.
- app.py:1098 — `build_clarify_answer(missing, finding)`. `evidence_options`·`GENERAL_SECTIONS`는 삭제됨.
- app.py:175-205 — `split_policy_chunks`. 부모 서두를 첫 자식에 합쳐서 별첨 1 금액표가 `별첨 1 > 주석 > 유류비 산식` 청크에 흡수돼 있다.
- app.py:810-828 — 프롬프트에 남은 규정 복사본과 규정에 없는 주장 4건(경조휴가 2일, 승중상 인정 조건, 주차비 처리, 주말·공휴일).
- eval_retrieval.py:37 `CASES`(회귀 44), :100 `REAL_CASES`(실사용 18).
- test_rules.py:177 `TRIP_HISTORY`, :197 `CONTINUITY_CASES`(C 그룹). 케이스 튜플 4번째 항목이 이력.

## Open Work
- 별첨 1 국내여비기준표가 검색으로 도달 불가한 상태다. 프롬프트의 10만원을 걷어낸 뒤로 "국내출장 숙박비 한도"는 이관으로 떨어진다. 청킹 수정안(표가 있을 때만 부모 서두를 끊기)은 측정만 했고 미적용이다.
- 규정에 근거가 없는 주장 4건(`app.py:810,811,812,828`)의 출처가 미확인이다. 사내 추가 규칙이면 규정 파일로 옮기는 것이 남았고, 아니면 삭제 대상이다. 사용자 확인 대기 중.
- 실사용 평가셋은 숙소지원금 영역에만 존재한다. 여비·경조금·동호회는 실제 로그가 없다.
- 원격은 `3bed965`까지 올라가 있고 `a0c9508`, `564060c` 2개가 로컬에만 있다. push는 사용자가 명시할 때만 한다(세션 중 지시받음). main은 `726bf83` 그대로다.
- 수강생 배포용 단계별 프롬프트(0~8단계)는 대화에만 있고 파일로 저장되지 않았다.
- 서버는 8000·8001·8002가 다른 프로젝트에 점유돼 있어 8008로 띄웠다. `app.py`는 8000이 하드코딩돼 있다.

## Prompt for New Chat
handoff.md를 읽고, 여기 나열된 파일들을 실제로 Read 도구로 열어서 문서 내용을 코드와 대조 검증한 뒤, 검증되면 이어서 작업해줘. 이 문서는 사실이 아니라 가설로 취급해.
````

## app.py

원본 경로: `app.py`

````python
import hashlib
import json
import math
import os
import re
import calendar
from collections import Counter
from datetime import datetime
from datetime import date
from functools import lru_cache
from http.server import SimpleHTTPRequestHandler, ThreadingHTTPServer
from pathlib import Path
from typing import TypedDict
from urllib.error import HTTPError, URLError
from urllib.request import Request, urlopen
from urllib.parse import unquote
from langgraph.graph import END, START, StateGraph


BASE_DIR = Path(__file__).resolve().parent
RULES_DIR = BASE_DIR / "규정"
MODEL = "gpt-5.6-luna"
SOURCE_FILES = {
    "경조금 지급기준.md",
    "동호회 관리 규정.md",
    "숙소지원금 운영 기준.md",
    "여비관리 FAQ.md",
    "여비관리기준.md",
    # 사규에는 없지만 사내에서 운영해 온 기준. 프롬프트에 박혀 있던 것을 옮겼습니다.
    "사내 추가 기준.md",
}
# 규정의 '회갑'과 사용자가 자주 쓰는 '환갑'을 같은 의미로 처리합니다.
HOEGAP_TERMS = ("회갑", "환갑")
# 조사나 어미가 붙어도 규정의 핵심어로 검색해야 하는 표현입니다.
CANONICAL_QUERY_TERMS = ("결혼", "해외출장")
QUERY_SYNONYMS = {
    "동생": {"형제", "자매", "형제자매"},
    "형": {"형제", "형제자매"},
    "누나": {"형제", "자매", "형제자매"},
    "언니": {"형제", "자매", "형제자매"},
    "오빠": {"형제", "형제자매"},
    "장인어른": {"배우자", "부모"},
    "장모님": {"배우자", "부모"},
    "시어머니": {"배우자", "부모"},
    "시아버지": {"배우자", "부모"},
    "처제": {"배우자", "형제", "자매"},
    "처형": {"배우자", "형제", "자매"},
    "처남": {"배우자", "형제", "자매"},
    "시누이": {"배우자", "형제", "자매"},
    "시동생": {"배우자", "형제", "자매"},
    "아주버님": {"배우자", "형제", "자매"},
    "도련님": {"배우자", "형제", "자매"},
    "고모": {"부모", "형제", "자매"},
    "이모": {"외조부모", "부모", "형제", "자매"},
    "외삼촌": {"외조부모", "부모", "형제", "자매"},
    "큰아버지": {"조부모", "부모", "형제"},
    "작은아버지": {"조부모", "부모", "형제"},
    "백숙부": {"조부모", "부모", "형제"},
    "백숙부모": {"조부모", "부모", "형제"},
    "매형": {"형제", "자매"},
    "매제": {"형제", "자매"},
    "제부": {"형제", "자매"},
    "형부": {"형제", "자매"},
    "올케": {"형제", "자매"},
    "와이프": {"배우자"},
    "아내": {"배우자"},
    "남편": {"배우자"},
    "부인": {"배우자"},
}



def load_env():
    """.env의 값을 읽어 환경변수에 없는 값만 설정합니다."""
    env_path = BASE_DIR / ".env"
    if not env_path.exists():
        return
    for line in env_path.read_text(encoding="utf-8").splitlines():
        line = line.strip()
        if not line or line.startswith("#") or "=" not in line:
            continue
        key, value = line.split("=", 1)
        os.environ.setdefault(key.strip(), value.strip().strip('"\''))


# 조사가 붙으면 "회사"와 "회사가"가 다른 토큰이 되어 흔한 단어가 희귀어로 잘못 계산됩니다.
PARTICLES = (
    "에서는", "에게는", "으로는", "이라도", "에서", "에게", "으로", "까지", "부터", "보다",
    "처럼", "마다", "조차", "밖에", "이나", "라도", "은", "는", "이", "가", "을", "를",
    "의", "에", "도", "만", "과", "와", "로", "나",
)


def strip_particle(word):
    """세 글자 이상 한글 어절에서 흔한 조사를 떼어 표제어에 가깝게 만듭니다."""
    if not re.fullmatch(r"[가-힣]+", word):
        return word
    for particle in PARTICLES:
        if len(word) - len(particle) >= 2 and word.endswith(particle):
            return word[: -len(particle)]
    return word


def tokens(text):
    """한글·영문·숫자 어절에서 조사를 떼어 검색 토큰을 만듭니다."""
    return {strip_particle(word) for word in re.findall(r"[가-힣A-Za-z0-9]+", text.lower())}


def bigrams(text):
    """한글 어절을 음절 2-gram으로 쪼갭니다."""
    # 조사·어미가 붙으면 어절이 달라지므로("해체하려면" vs "해체") 2-gram으로 겹치게 합니다.
    result = set()
    for word in re.findall(r"[가-힣]{2,}", text.lower()):
        result.update(word[index:index + 2] for index in range(len(word) - 1))
    return result


def split_policy_chunks(path) -> list[dict]:
    """리프 제목의 경로와 본문을 분리하고, 부모 서두는 첫 자식에 합칩니다."""
    text = path.read_text(encoding="utf-8")
    if path.suffix.lower() != ".md":
        return [{"path": "", "text": p.strip()} for p in re.split(r"\n\s*\n", text) if p.strip()]

    chunks = []
    headings = []
    current_lines = []
    for line in text.splitlines():
        heading = re.match(r"^(#{1,6})\s+(.+?)\s*$", line)
        if heading:
            level = len(heading.group(1))
            title = heading.group(2)
            # 제목을 만나면 지금까지 모은 본문을 지금 경로로 확정합니다. 더 깊은 제목이
            # 올 때 부모 서두를 들고 내려가면 그 본문이 맨 아래 리프의 것이 됩니다.
            # 별첨 1의 국내여비기준표가 실제로 '별첨 1 > 주석 > 유류비 산식' 청크에
            # 흡수돼 있었고, 경로를 본문과 함께 임베딩하므로 벡터까지 유류비 쪽으로
            # 끌려가 숙박비 질문에서 20위 밖으로 밀렸습니다.
            body = "\n".join(current_lines).strip()
            if body and headings:
                chunks.append({"path": " > ".join(name for _, name in headings), "text": body})
            current_lines = []
            # 제목 단계가 건너뛰어져도 실제 상위 제목만 경로에 남깁니다.
            while headings and headings[-1][0] >= level:
                headings.pop()
            headings.append((level, title))
            continue
        current_lines.append(line)
    body = "\n".join(current_lines).strip()
    if body:
        chunks.append({"path": " > ".join(title for _, title in headings), "text": body})

    return chunks


BM25_K1 = 1.5
BM25_B = 0.75
# 2-gram 가중치. 평가셋에서 1.0이 상위 3건 정확도가 가장 높아 어절과 같은 무게로 둡니다.
BIGRAM_WEIGHT = 1.0
# 최고 점수 파일 대비 이 비율 이상인 규정만 함께 근거로 사용합니다.
TOP_FILE_SCORE_RATIO = 0.6


@lru_cache(maxsize=1)
def load_policy_index():
    """승인된 규정의 청크와 idf를 한 번만 계산해 재사용합니다."""
    # ponytail: 규정 파일이 런타임에 바뀌지 않는다고 보고 캐시합니다. 문서를 고치면 재시작이 필요합니다.
    chunks = []
    for path in sorted((*RULES_DIR.glob("*.txt"), *RULES_DIR.glob("*.md"))):
        # 상담 근거로 승인된 파일만 사용하고 샘플 문서는 제외합니다.
        if path.name not in SOURCE_FILES:
            continue
        for chunk in split_policy_chunks(path):
            searchable = f"{chunk['path']}\n{chunk['text']}"
            chunks.append({
                "file": path.name,
                "stem": path.stem,
                "path": chunk["path"],
                "text": chunk["text"],
                "searchable": searchable,
                "tokens": tokens(searchable),
                "bigrams": bigrams(searchable),
            })
    total = len(chunks)
    frequency = Counter(token for chunk in chunks for token in chunk["tokens"] | chunk["bigrams"])
    # 흔한 단어(출장·지원)는 낮게, 희귀어(택시·해체)는 높게 가중합니다.
    idf = {
        token: math.log(1 + (total - count + 0.5) / (count + 0.5))
        for token, count in frequency.items()
    }
    average_length = sum(len(chunk["tokens"]) for chunk in chunks) / total if total else 1.0
    # 계층 경로를 본문과 함께 임베딩합니다. "5.2 지원 대상"처럼 본문에 제도명이 없는 청크는
    # 제목에만 의미가 있어, 본문만 임베딩하면 벡터 점수가 구조적으로 낮게 나옵니다.
    embeddings = embed_texts([chunk["searchable"] for chunk in chunks])
    for chunk in chunks:
        chunk["vector"] = embeddings.get(hashlib.sha256(chunk["searchable"].encode("utf-8")).hexdigest())
    return chunks, idf, average_length


def score_chunk(query_tokens, query_bigrams, chunk, idf, average_length):
    """희귀어 가중과 길이 정규화를 적용한 BM25 점수를 계산합니다."""
    matched = query_tokens & chunk["tokens"]
    # 2-gram은 조사·어미를 넘기 위한 보조 신호라 어절 일치보다 낮게 봅니다.
    matched_bigrams = (query_bigrams & chunk["bigrams"]) - matched
    if not matched and not matched_bigrams:
        return 0.0
    weight = sum(idf.get(token, 0.0) for token in matched)
    weight += BIGRAM_WEIGHT * sum(idf.get(token, 0.0) for token in matched_bigrams)
    length_ratio = len(chunk["tokens"]) / average_length if average_length else 1.0
    normalizer = BM25_K1 * (1 - BM25_B + BM25_B * length_ratio) + 1
    return weight * (BM25_K1 + 1) / normalizer


EMBEDDING_MODEL = "text-embedding-3-small"
EMBEDDING_CACHE_PATH = BASE_DIR / ".embedding_cache.json"
# 조사·어미 변형은 2-gram이, 동의어·패러프레이즈는 임베딩이 잡습니다.
# 44문항 스윕에서 0.5→0.8이 hit@1 45.5→63.6%, MRR 0.639→0.744로 개선됐습니다.
# 0.8을 넘기면 키워드가 지키던 문항("경조휴가 중 결근", "전세 계약 이사비")이 1위→9위 밖으로
# 밀리고, 평가셋이 패러프레이즈 위주라 벡터에 유리하게 편향돼 있어 여기서 멈춥니다.
VECTOR_WEIGHT = 0.8


def request_embeddings(texts):
    """임베딩 API를 호출합니다. 키가 없거나 실패하면 빈 리스트를 반환합니다."""
    api_key = os.environ.get("OPENAI_API_KEY", "").strip()
    if not api_key:
        return []
    vectors = []
    for start in range(0, len(texts), 64):
        batch = texts[start:start + 64]
        request = Request(
            "https://api.openai.com/v1/embeddings",
            data=json.dumps({"model": EMBEDDING_MODEL, "input": batch}).encode("utf-8"),
            headers={"Authorization": f"Bearer {api_key}", "Content-Type": "application/json"},
        )
        try:
            with urlopen(request, timeout=45) as response:
                payload = json.loads(response.read().decode("utf-8"))
        except (HTTPError, URLError, TimeoutError, OSError):
            # 임베딩이 실패하면 키워드 검색만으로 동작하도록 비워서 돌려줍니다.
            return []
        vectors.extend(item["embedding"] for item in payload["data"])
    return vectors


def embed_texts(texts):
    """텍스트별 임베딩을 파일 캐시와 함께 반환합니다."""
    cache = {}
    if EMBEDDING_CACHE_PATH.exists():
        cache = json.loads(EMBEDDING_CACHE_PATH.read_text(encoding="utf-8"))
    keys = [hashlib.sha256(text.encode("utf-8")).hexdigest() for text in texts]
    missing = [text for text, key in zip(texts, keys) if key not in cache]
    if missing:
        fresh = request_embeddings(missing)
        if not fresh:
            return {}
        for text, vector in zip(missing, fresh):
            cache[hashlib.sha256(text.encode("utf-8")).hexdigest()] = vector
        EMBEDDING_CACHE_PATH.write_text(json.dumps(cache), encoding="utf-8")
    return {key: cache[key] for key in keys if key in cache}


def cosine(left, right):
    """두 벡터의 코사인 유사도를 계산합니다."""
    dot = sum(a * b for a, b in zip(left, right))
    size = math.sqrt(sum(a * a for a in left)) * math.sqrt(sum(b * b for b in right))
    return dot / size if size else 0.0


# 코퍼스가 작아 후보를 넓게 주고, 어떤 조항이 답인지는 판정 단계에서 가립니다.
# 이 개수 이하의 청크에만 나오는 낱말을 종목어로 봅니다. 낚시·헬스·등산·볼링은 각 1개입니다.
RARE_TERM_MAX_CHUNKS = 2


# 낱말로 근거 파일을 미리 좁히던 `focused_evidence_files`는 지웠습니다. 측정하면 전 지표가
# 나빠집니다(44문항 MRR 0.760 -> 0.685, 미검색 2 -> 6건). "출장 가면 숙소는 회사가
# 잡아주나요?"는 '숙소'가 걸려 숙소지원금 기준에 갇히지만 답은 여비관리기준에 있습니다.
# 제도 섞임은 낱말이 아니라 점수로 가리는 TOP_FILE_SCORE_RATIO가 이미 맡고 있습니다.
# 후보를 지우는 키워드 규칙은 해롭고, 더하는 확장(rare_query_terms)은 값을 합니다.


def retrieve(question, limit=20):
    """Markdown 제목 청크와 기존 텍스트 규정에서 관련 근거를 찾아 반환합니다."""
    query_tokens = tokens(question)
    # 아래에서 넓히기 전의 원문 토큰. 희소 종목어 판정에 씁니다.
    question_tokens = set(query_tokens)
    compact_question = question.replace(" ", "")
    for term in CANONICAL_QUERY_TERMS:
        if term in compact_question:
            query_tokens.add(term)
    if any(word in question for word in ("숙소", "숙소지원금", "기존 숙소", "전 근무지", "반납", "정리", "유지")):
        # 근무지 이동 관련 질문은 5.5 지원특례의 핵심 표현을 함께 검색합니다.
        query_tokens.update({"전근무지", "숙소정리", "3개월", "최장", "6개월", "처분", "발령"})
    for word, synonyms in QUERY_SYNONYMS.items():
        if word in question:
            query_tokens.update(synonyms)
    query_bigrams = bigrams(question)
    chunks, idf, average_length = load_policy_index()
    question_vectors = embed_texts([question])
    query_vector = next(iter(question_vectors.values()), None)
    scored = []
    for chunk in chunks:
        keyword_score = score_chunk(query_tokens, query_bigrams, chunk, idf, average_length)
        vector_score = 0.0
        if query_vector and chunk.get("vector"):
            vector_score = cosine(query_vector, chunk["vector"])
        if keyword_score or vector_score:
            scored.append((chunk, keyword_score, vector_score))
    if not scored:
        return []
    top_keyword = max(item[1] for item in scored) or 1.0
    top_vector = max(item[2] for item in scored) or 1.0
    results = []
    for chunk, keyword_score, vector_score in scored:
        # 임베딩을 못 쓰면 키워드 점수만으로 순위를 정합니다.
        if query_vector:
            fused = (1 - VECTOR_WEIGHT) * (keyword_score / top_keyword) + VECTOR_WEIGHT * (vector_score / top_vector)
        else:
            fused = keyword_score / top_keyword
        results.append({
            "file": chunk["file"],
            "score": round(fused, 3),
            "keyword_score": round(keyword_score, 3),
            "vector_score": round(vector_score, 3),
            "path": chunk["path"],
            "text": chunk["text"][:3000],
        })
    results.sort(key=lambda item: item["score"], reverse=True)
    if not results:
        return []
    # 최고 점수에 근접한 규정 파일만 선택해 다른 제도 설명이 섞이지 않게 합니다.
    threshold = results[0]["score"] * TOP_FILE_SCORE_RATIO
    top_files = {item["file"] for item in results if item["score"] >= threshold}
    results = [item for item in results if item["file"] in top_files]
    selected = []
    taken = set()
    # 질문에만 등장하는 희소 종목어가 조항 본문에 있으면, 의미 유사도 순위와 관계없이
    # 해당 조항을 근거에 남깁니다. 예: "낚시 동호회", "헬스 동아리".
    # 두 가지로 좁힙니다. 첫째, 넓히기 전 원문 토큰만 봅니다. 주입한 검색어("3개월"·"발령")
    # 까지 희소어로 잡으면 그 낱말을 담은 청크가 근거를 채웁니다. 둘째, 코퍼스에서 몇 개
    # 청크에만 나오는 낱말로 제한합니다. idf만 보면 "해서"(8청크)·"신청"(9청크) 같은 구어가
    # 걸려 20자리를 상한 없이 잠식하고, 정작 답이 있는 조항이 밀려납니다. 실제로 숙소지원금
    # 문항 2건이 그렇게 미검색으로 떨어졌습니다. 이 규칙이 노리는 종목어(낚시·헬스·등산·볼링)는
    # 모두 1개 청크에만 나오므로, 청크 수로 막으면 잠식 폭이 구조적으로 묶입니다.
    rare_query_terms = {
        term for term in question_tokens
        if len(term) >= 2
        and sum(1 for chunk in chunks if term in chunk["text"]) <= RARE_TERM_MAX_CHUNKS
    }
    for item in results:
        if not any(term in item["text"] for term in rare_query_terms):
            continue
        selected.append(item)
        taken.add((item["file"], item["path"]))
    # 여러 규정이 함께 적용될 수 있으므로 규정별 상위 근거를 먼저 확보합니다.
    for path in sorted({item["file"] for item in results}):
        for item in [item for item in results if item["file"] == path][:3]:
            if (item["file"], item["path"]) in taken:
                continue
            selected.append(item)
            taken.add((item["file"], item["path"]))
    # 관련 규정이 하나뿐이면 파일별 상한 탓에 근거가 3건으로 잘리므로 남은 자리를 채웁니다.
    for item in results:
        if len(selected) >= limit:
            break
        if (item["file"], item["path"]) not in taken:
            selected.append(item)
            taken.add((item["file"], item["path"]))
    selected.sort(key=lambda item: item["score"], reverse=True)
    return attach_referenced_chunks(selected[:limit], chunks)


# "별첨 1에 의한다", "제5.11에서 규정한" 처럼 규정이 스스로 가리키는 참조 표현입니다.
REFERENCE_PATTERN = re.compile(r"별첨\s*(\d+)|제\s*(\d+\.\d+)")
# 참조를 따라갈 상위 근거 수. 아래쪽 근거까지 따라가면 질문과 먼 별첨이 붙습니다.
REFERENCE_SOURCE_LIMIT = 5
# 한 청크가 이보다 많은 번호를 담고 있으면 가리키는 것이 아니라 나열하는 것으로 봅니다.
REFERENCE_MARKER_LIMIT = 2
# 참조 하나당, 그리고 한 질문당 붙일 수 있는 근거 수.
REFERENCE_CHUNK_LIMIT = 2
REFERENCE_TOTAL_LIMIT = 4


def attach_referenced_chunks(selected, chunks):
    """근거가 가리키는 별첨·조항을 함께 붙입니다.

    5.10.2는 본문이 "소액경비 및 숙박료 지급기준은 별첨 1에 의한다"뿐이라 금액이 없습니다.
    검색이 이 조항을 1위로 올려도 판정은 근거에 답이 없다고 보고 이관으로 떨어뜨립니다.
    규정이 명시한 링크만 따라가므로 순위 계산에는 손대지 않고 근거에만 더합니다.
    참조가 다시 참조를 물고 오지 않도록 1홉만 따라갑니다.
    """
    if not selected:
        return selected
    taken = {(item["file"], item["path"]) for item in selected}
    targets = []
    for item in selected[:REFERENCE_SOURCE_LIMIT]:
        markers = {
            f"별첨 {attachment}." if attachment else f"{clause} "
            for attachment, clause in REFERENCE_PATTERN.findall(item["text"])
        }
        # 별첨 목록표('6. 기록 및 첨부')처럼 번호를 죽 늘어놓기만 하는 청크는 가리키는
        # 것이 아니라 나열하는 것입니다. 따라가면 별첨 전체가 근거로 딸려옵니다.
        if len(markers) > REFERENCE_MARKER_LIMIT:
            continue
        # 같은 규정 안의 참조만 따라갑니다. 파일이 다르면 번호가 겹쳐도 다른 문서입니다.
        targets.extend((item["file"], marker) for marker in markers)
    added = []
    for file_name, marker in targets:
        hits = [
            chunk for chunk in chunks
            if chunk["file"] == file_name
            and marker in chunk["path"]
            and (chunk["file"], chunk["path"]) not in taken
        ]
        # 별첨 아래 하위 절이 많으면 표가 있는 상위부터 가져옵니다.
        hits.sort(key=lambda chunk: len(chunk["path"]))
        for chunk in hits[:REFERENCE_CHUNK_LIMIT]:
            taken.add((chunk["file"], chunk["path"]))
            # 참조로 딸려온 근거는 검색 점수가 아니라 출처가 근거이므로 0점으로 둡니다.
            added.append({
                "file": chunk["file"],
                "score": 0.0,
                "keyword_score": 0.0,
                "vector_score": 0.0,
                "path": chunk["path"],
                "text": chunk["text"][:3000],
                "referenced": True,
            })
    return selected + added[:REFERENCE_TOTAL_LIMIT]


def add_months(value, months):
    """월말 날짜가 존재하지 않는 경우 해당 월의 마지막 날로 보정합니다."""
    month_index = value.month - 1 + months
    year = value.year + month_index // 12
    month = month_index % 12 + 1
    day = min(value.day, calendar.monthrange(year, month)[1])
    return date(year, month, day)


def extract_birth_date(text):
    """숫자형·하이픈형·한글형 생년월일을 날짜로 변환합니다."""
    patterns = (
        r"(?<!\d)(19\d{2})(\d{2})(\d{2})(?!\d)",
        r"(?<!\d)(19\d{2})[-./](\d{1,2})[-./](\d{1,2})(?!\d)",
        r"(?<!\d)(19\d{2})년\s*(\d{1,2})월\s*(\d{1,2})일",
    )
    for pattern in patterns:
        match = re.search(pattern, text)
        if match:
            try:
                return date(int(match.group(1)), int(match.group(2)), int(match.group(3)))
            except ValueError:
                return None
    return None


def date_deadline_context(question):
    """질문에 사유 발생일이 있으면 경조금 3개월 마감일을 계산합니다."""
    match = re.search(r"(20\d{2})[.\-/년](\d{1,2})[.\-/월](\d{1,2})일?", question)
    if not match or not any(word in question for word in ("경조", "회갑", "결혼", "출산", "사망", "신청")):
        return ""
    try:
        occurred = date(int(match.group(1)), int(match.group(2)), int(match.group(3)))
        deadline = add_months(occurred, 3)
    except ValueError:
        return ""
    return (
        f"\n[계산된 신청기한]\n사유 발생일 {occurred.isoformat()} 기준 3개월 이내 신청 마감일은 "
        f"{deadline.isoformat()}이다. 사유 발생일의 일자가 다음 달에 없으면 월말로 보정한다. "
        "마감일 당일까지 신청 가능하다고 안내한다."
    )


def birthday_context(question):
    """회갑 질문의 생년월일을 계산해 모델이 연령을 추측하지 않게 합니다."""
    if not any(term in question for term in HOEGAP_TERMS):
        return ""
    birth = extract_birth_date(question)
    if not birth:
        return ""
    try:
        today = date.today()
        sixtieth = date(birth.year + 60, birth.month, birth.day)
    except ValueError:
        return ""
    deadline = add_months(sixtieth, 3)
    if sixtieth > today:
        status = f"회갑 사유 발생일은 {sixtieth.isoformat()}이며 현재 기준일 이후이다. 아직 신청할 수 없다."
    elif today <= deadline:
        status = (
            f"회갑 사유 발생일은 {sixtieth.isoformat()}이고 신청 마감일은 {deadline.isoformat()}이다. "
            "현재 기준일은 사유 발생일로부터 3개월 이내이므로 경조금 신청이 가능하다."
        )
    else:
        status = (
            f"회갑 사유 발생일은 {sixtieth.isoformat()}이고 신청 마감일은 {deadline.isoformat()}이다. "
            "현재 기준일은 신청 마감일 이후이므로 청구권이 소멸되어 신청할 수 없다."
        )
    return (
        f"\n[회갑 생년월일 계산 결과]\n생년월일: {birth.isoformat()} / 기준일: {today.isoformat()} / {status} "
        "이 계산 결과를 답변에 반영하고, 대상 관계와 회갑 연령을 구분해 설명한다."
    )


def conversation_context(question, history):
    """후속 답변의 관계와 생년월일을 직전 대화에서 보완합니다."""
    prior_text = " ".join(item.get("content", "") for item in (history or [])[-6:])
    relation = ""
    if any(word in question for word in ("우리 엄마", "우리 어머니", "우리 아버지", "우리 부모님", "우리 부모")):
        relation = "우리 엄마·아버지·부모님은 별도 배우자 표현이 없으므로 본인 부모로 해석한다."
    elif any(word in question for word in ("배우자 어머니", "배우자 엄마", "배우자 아버지", "장모님", "장인어른", "시어머니", "시아버지")):
        relation = "질문 대상은 배우자의 부모로 해석한다."
    birth = extract_birth_date(question + " " + prior_text)
    birthday = ""
    if birth and (any(term in question for term in HOEGAP_TERMS) or any(term in prior_text for term in HOEGAP_TERMS)):
        birthday = birthday_context(f"회갑 {birth.strftime('%Y%m%d')}")
    if not relation and not birthday:
        return ""
    return f"\n[대화 맥락 보완]\n{relation}\n{birthday}".strip()



def build_clarification_answer(question):
    """제도 유형을 알 수 없는 질문에 전체 상담 범위와 재질문 형식을 안내합니다."""
    return (
        "질문의 대상이나 제도 유형을 정확히 확인하기 어렵습니다.\n\n"
        "현재 상담 가능한 복리후생 항목은 다음과 같습니다.\n"
        "- 국내·외 출장 및 파견·부임\n"
        "- 경조금: 결혼, 회갑, 출산장려금, 사망\n"
        "- 숙소지원금\n"
        "- 동호회 지원\n\n"
        "정확한 안내를 위해 제도 유형, 대상 또는 상황, 확인하고 싶은 내용을 포함해 다시 질문해 주세요.\n"
        "예: 지원금, 지원 물품, 자격, 신청기한, 필요 서류, 신청 방법\n\n"
        "예시: ‘서울에서 포항으로 출장 갈 때 교통비가 지원되나요?’ 또는 ‘타지역으로 부임하면 숙소지원금을 받을 수 있나요?’"
    )


def build_unknown_policy_answer(question):
    """제도 식별이 불가능한 질의에 공통 확인 안내를 제공합니다."""
    return (
        "문의하신 내용과 직접 연결되는 복리후생 규정을 확인하지 못했습니다.\n\n"
        "어떤 지원 제도에 대한 문의인지, 확인하고 싶은 비용이나 신청 항목을 조금 더 구체적으로 알려주세요.\n"
        "현재 정보만으로는 지원 가능 여부를 판단하기 어렵습니다."
    )




def is_club_application_question(question):
    """동호회 신규 신청 양식과 담당자 안내를 묻는 질문을 식별합니다."""
    application_words = (
        "신청", "신청서", "신규", "결성", "개설", "창설", "만들", "등록", "양식", "회장", "총무", "계좌번호"
    )
    return "동호회" in question and any(word in question for word in application_words)




def call_openai(question, evidence, history=None):
    """검색 근거를 포함해 Responses API를 호출합니다."""
    api_key = os.environ.get("OPENAI_API_KEY", "").strip()
    if not api_key:
        raise RuntimeError(".env에 OPENAI_API_KEY가 입력되지 않았습니다.")
    evidence_text = "\n\n".join(
        f"[근거 {i}] {item['file']} ({item.get('path', '')})\n{item['text']}"
        for i, item in enumerate(evidence, 1)
    ) or "관련 규정 근거를 찾지 못했습니다."
    instructions = (
        "너는 사내 복리후생 규정 상담 Agent다. 네 개의 제공된 원문 규정을 기준 데이터로 사용하며, 반드시 제공된 근거만 사용해 한국어로 답한다. "
        "불확실한 질의는 세 유형으로 구분한다. 제도 자체를 식별할 수 없으면 어떤 제도·비용인지 구체화를 요청한다. 제도는 식별되지만 정보가 부족하면 부족한 정보만 요청한다. 관련 규정이 없거나 충돌하면 지급 여부를 확정하지 말고 주관 부서 문의를 안내한다. "
        "근거에 없는 금액·조건·사실은 추측하지 않는다. 질문 의도를 먼저 파악하고, "
        "가능 여부를 단정하기 어려우면 필요한 추가 정보를 질문한다. 답변은 자연스러운 대화체로 작성하되 부연 설명은 최소화한다. "
        "답변 마지막에는 '확인한 규정'과 파일명을 간단히 표시한다. 최종 승인·지급은 담당 부서 검토임을 안내한다. "
        "규정에 명시된 예외만 적용하고, 규정에 없는 예외나 담당자 재량은 사용자에게 확인 질문으로 남긴다. "
        f"오늘 기준일은 {date.today().isoformat()}이다. 경조금은 회갑 대상 여부와 신청기한을 별도로 판단한다. 경조금 기준의 청구권은 사유 발생일로부터 3개월 이내이며, "
        "계산된 신청 마감일이 제공되면 그 날짜를 사용한다. 규정의 회갑 대상은 본인 및 배우자 부모이고 지급액은 20만원이다."
        " 부모·배우자 부모의 회갑 질문에서 생년월일이 없으면 연령을 추측하거나 지원을 확정하지 않는다. "
        "답변은 다음 흐름으로 작성한다: '해당 부모가 본인 부모인지 배우자 부모인지 관계 요건은 확인되지만, 현재 정보만으로 실제 회갑 대상인지 판단할 수 없다'고 먼저 설명한다. "
        "그 다음 현재 기준일을 알려주고 생년월일을 YYYYMMDD 형식으로 요청한다. 지급액 20만원과 신청기한은 회갑 대상 판정 이후에 안내한다. "
        "단, 사용자가 '우리 엄마', '우리 아버지', '우리 부모님'이라고 표현하면 별도 배우자 표현이 없는 한 본인 부모로 이해하고 관계를 다시 묻지 않는다. "
        "직전 대화에서 생년월일과 관계가 이미 확인되었으면 같은 질문을 반복하지 말고 회갑일 계산 결과를 안내한다. "
        "사용자가 결혼·사망·출산·동호회·숙소·출장 등 다른 제도를 새로 질문하면 이전 회갑 대화와 분리해 현재 질문의 의도와 규정만 사용한다. "
        "현재 질문과 무관한 제도는 비교·설명·언급하지 않는다. 예를 들어 형제자매 결혼 질문에는 회갑, 숙소, 동호회, 여비를 언급하지 않는다. "
        "결혼·회갑·출산장려금·사망 등 경조금 답변은 반드시 공통 양식을 따른다. 첫 줄에는 해당 여부를 한 문장으로만 답한다. "
        "이후 빈 줄 뒤에 '확인 결과'를 쓰고, '- 관계:', '- 생년월일:'(확인된 경우만), '- 사유 발생일:'(확인된 경우만), "
        "'- 신청 마감일:'(계산 가능한 경우만), '- 현재 기준일:', '- 판정:', '- 지원금:', '- 필요 서류:' 순서로 필요한 항목만 한 번씩 작성한다. "
        "그 뒤 빈 줄 뒤에 신청 가능 여부 또는 추가로 필요한 정보만 한두 문장으로 쓰고, 마지막 문장은 '최종 승인·지급은 담당 부서의 서류 검토를 거쳐 결정됩니다.'로 끝낸다. "
        "같은 내용을 반복하거나 현재 질문과 무관한 제도를 언급하지 않는다. "
        "사망 문의에서는 첫 문장을 '경조금 지급 대상입니다.'처럼 간단히 작성하고 '해당됩니다'로 시작하지 않는다. "
        "사망일을 아직 받지 못한 경우 사망일로부터 3개월 이내 신청해야 한다는 문장과 사망일 요청 문장을 쓰지 않는다. "
        "사망 문의에서는 화환·조화 지원 여부를 별도 부연 문장으로 설명하지 않는다. "
        # 담당 업체 연락처와 아래 세 건은 '사내 추가 기준.md'로 옮겼습니다. 규정에 없는 값을
        # 지시문에 적어 두면 근거 없이 나오고, 근거로 표시되지도 않아 환각과 구분되지 않습니다.

        "숙소지원금 질문에서 근무지역 이동, 기존 숙소 유지·반납·정리가 언급되면 숙소지원금 운영 기준 5.5 지원특례를 반드시 확인한다. "
        "해당 규정은 전 근무지 숙소 정리 비용을 3개월간 숙소지원금 한도 내 실비로 지원하고, 부득이한 경우 처분 노력 입증자료 제출을 전제로 1개월 단위 최장 6개월까지 연장할 수 있다고 정한다. "
        "회갑일이 지났더라도 사유 발생일로부터 3개월 이내이면 신청 가능하다. "
        "계산 결과가 신청 마감일 이후이면 '확정하기 어렵다', '추가 확인 필요' 같은 유보 표현을 쓰지 않는다. "
        "이 경우에는 회갑일·신청 마감일·청구권 소멸을 2~3문장으로 간단히 안내한다."
        " 근무지역 이동 시 '숙소 정리비'라는 별도 지원금 표현을 쓰지 말고, '전 근무지 숙소 정리 기간에 발생한 비용'으로 안내한다. "
        "해당 비용은 기본 3개월, 처분 노력 입증자료 제출 시 1개월씩 최대 3회 연장하여 최장 6개월(3개월+1개월+1개월+1개월)까지 지원할 수 있다. 청소비는 제외한다."
        # 국내출장 금액 기준은 규정에 있으므로 여기에 옮겨 적지 않는다. 옮겨 적었던 문장은
        # 5.14.2의 거리 조건(왕복 120km/50km/미만)을 떼고 단정했고, "식비 1일 3만원"은
        # 5.14.3 자가차량 동승자 기준을 일반 기준으로 잘못 옮긴 것이었다.
        " 동호회 정기지원은 분기 1회 이상 활동 시 인당 3만원·최대 30만원이며 영수증·활동사진·참석자 명단이 필요하다."
        " 사용자가 이미 말한 사실(관계, 날짜, 근무지, 이사 여부, 동거 여부, 질문 항목)은 다시 묻지 않는다."
        " 발령·부임·이사·부임비·이전비·가족 잔류·혼자 거주 표현이 있으면 부임 및 숙소지원금 복합 문의로 파악한다."
        " 이 경우 실제 이사하지 않으면 부임비 지급이 없다는 점을 먼저 답하고, 숙소지원금은 새 근무지·실제 단신 거주·주택 보유 여부·임대차계약서를 기준으로 필요한 만큼만 안내한다."
        " 가족 명의(본인·배우자·부모·자녀·형제자매) 건물에 전세 또는 월세로 거주하는 경우와 단신부임으로 신청한 뒤 실제 동거인이 있는 사실이 확인된 경우에만 숙소지원금 지원 불가로 안내한다."
        " 주민등록상 주소 등록과 실제 동거를 동일한 사실로 추정하지 않는다. 주소만 전입했고 실제 동거하지 않는다고 사용자가 밝히면 자동 제외하지 않으며, 실제 동거 여부가 불명확하면 그 사실만 확인한다."
        " 허위 신청이나 신청 내용과 실제 거주 형태의 불일치가 확인된 경우에만 전세금·월세 지원금 계산을 하지 말고, '신청 내용이나 실제 거주 형태가 사실과 다르면 윤리위반으로 감사 대상이 될 수 있습니다.'라고 짧게 경고한다. 주소만 전입했거나 실제 동거 여부가 불명확한 단계에서는 이 경고를 조건부 설명으로도 언급하지 않는다."
        " 기존 숙소지원금 수급 중 월세와 전세의 계약 형태가 바뀌면 변경된 임대차계약서와 계약조건 확인 자료를 새로 제출하도록 안내한다."
        " 월세에서 전세로 바뀌면 전세금 1,000만원당 월 10만원, 전세에서 월세로 바뀌면 월 차임만 지원하고 관리비·공과금은 제외한다고 표시한다."
        " 계약 전환의 세부 제출서류와 적용 시점은 숙소지원금 담당자에게 문의하도록 답변을 끝낸다."
        " 해외출장 또는 교육연수의 국내공항·국내 주차비는 지급하지 않는다고 명확히 안내한다. 회사 차량·개인 차량 여부에 따른 예외를 만들지 않는다."
        # 일반 국내출장 주차비를 "현지교통비 2만원 범위에서 처리"로 단정하던 문장은 지웠고
        # '사내 추가 기준.md'로도 옮기지 않았다. 여비관리기준 165행이 "회사 차량 이용 시 발생한
        # 주유비, 주차비 등은 기타 경비 항목으로 지급할 수 있다"고 정해 정면으로 충돌한다.
        # 규정과 어긋나는 주장을 승인된 근거 파일에 올리면 환각을 근거로 승격시키는 셈이다.

        " 질문의 제도를 식별할 수 있으면 전체 복리후생 목록을 보여주지 않는다. 전체 목록은 제도와 상황을 전혀 알 수 없는 질문에서만 사용한다."
        " 모든 제도 답변은 질문에 대한 결론 한 문장, 확인 결과, 반드시 필요한 추가 확인 한두 항목 순서로 간결하게 작성한다."
    )
    conversation = []
    for item in (history or [])[-8:]:
        if item.get("role") in ("user", "assistant") and item.get("content"):
            conversation.append({"role": item["role"], "content": item["content"]})
    prior_text = " ".join(item.get("content", "") for item in (history or [])[-4:])
    birthday_question = question if any(term in question for term in HOEGAP_TERMS) else (f"회갑 {question}" if any(term in prior_text for term in HOEGAP_TERMS) else question)
    conversation.append({"role": "user", "content": f"{question}{date_deadline_context(question)}{birthday_context(birthday_question)}{conversation_context(question, history)}\n\n검색된 규정 근거:\n{evidence_text}"})
    payload = {
        "model": MODEL,
        # 규정 검색 결과를 요약하는 상담은 낮은 지연을 우선합니다.
        "reasoning": {"effort": "none"},
        "instructions": instructions,
        "input": conversation,
    }
    request = Request(
        "https://api.openai.com/v1/responses",
        data=json.dumps(payload).encode("utf-8"),
        headers={"Authorization": f"Bearer {api_key}", "Content-Type": "application/json"},
        method="POST",
    )
    with urlopen(request, timeout=45) as response:
        data = json.loads(response.read().decode("utf-8"))
    if data.get("output_text"):
        return data["output_text"]
    parts = []
    for output in data.get("output", []):
        for content in output.get("content", []):
            if content.get("type") == "output_text":
                parts.append(content.get("text", ""))
    return "\n".join(parts).strip() or "응답 내용을 확인하지 못했습니다."


POLICY_INTENTS = ("ceremony", "housing", "relocation", "trip", "club", "other")

# 판정기가 근거 번호를 넓게 고르더라도 현재 제도와 직접 관련된 문서만 사용자에게 표시합니다.
# 부임은 여비와 숙소 기준이 함께 적용될 수 있어 두 문서군을 모두 허용합니다.
INTENT_EVIDENCE_FILES = {
    "ceremony": {"경조금 지급기준.md", "사내 추가 기준.md"},
    "housing": {"숙소지원금 운영 기준.md", "사내 추가 기준.md"},
    "relocation": {"여비관리기준.md", "숙소지원금 운영 기준.md", "사내 추가 기준.md"},
    "trip": {"여비관리기준.md", "여비관리 FAQ.md", "사내 추가 기준.md"},
    "club": {"동호회 관리 규정.md"},
}
# 선택된 문서의 직접 어휘 점수가 최고 문서의 80%보다 낮으면 같은 제도 문서라도 표시하지 않습니다.
USED_EVIDENCE_KEYWORD_RATIO = 0.8
# 질문에 업무 항목이 명시된 경우에는 점수보다 해당 표현이 실제 근거에 있는지를 우선합니다.
EVIDENCE_QUERY_ANCHORS = {
    "주차": ("주차",),
    "숙소지원금": ("숙소지원금", "숙소 지원"),
    "부임비": ("부임비",),
    "이전비": ("이전비", "이전료"),
    "경조금": ("경조금",),
    "조의금": ("조의금", "사망"),
    "동호회": ("동호회",),
    "동아리": ("동호회", "동아리"),
}


GROUNDEDNESS_INSTRUCTIONS = (
    "너는 사내 복리후생 규정 상담의 근거 판정기다. 사용자 질문과 검색된 규정 근거를 읽고, "
    "그 근거만으로 질문에 답할 수 있는지 판정한다. 답을 작성하지 말고 판정만 한다.\n"
    "판정은 셋 중 하나다.\n"
    "- answerable: 근거에 질문의 답이 실제로 들어 있다.\n"
    "- clarify: 제도는 맞게 찾았으나 관계·금액·기간처럼 답을 정하는 정보가 질문에 빠져 있다.\n"
    "clarify일 때 missing에는 질문에 아직 없는 정보만 넣는다. 질문이 이미 밝힌 사실은 "
    "다시 묻지 않는다. 예를 들어 질문에 '개인 사정으로'라고 적혀 있으면 "
    "업무상인지 개인 사정인지는 묻지 않는다. 되물을 것이 남지 않으면 clarify가 아니다.\n"
    "missing에는 사용자가 스스로 답할 수 있는 사실만 넣는다. 날짜·금액·관계·실제 일정처럼 "
    "사용자가 아는 것이다. '회사가 인정하는지', '승인이 나는지', '규정상 가능한지'처럼 "
    "담당 부서의 판단이 필요한 항목은 missing이 아니다. 그것은 사용자가 물은 질문 자체이므로 "
    "되물으면 대화가 제자리를 돈다. 그런 항목만 남는다면 clarify가 아니라 escalate다.\n"
    "이전 대화가 함께 주어지면 그 안에서 이미 되물은 항목과 사용자가 답한 사실을 확인한다. "
    "표현을 바꿔서 다시 묻지 않는다. 같은 것을 두 번 물어야 할 상황이면 이미 답을 받은 것이므로 "
    "clarify가 아니라 answerable 또는 escalate로 판정한다.\n"
    "- escalate: 근거가 질문의 주제를 다루지 않는다. 어휘가 겹쳐도 다른 항목을 다루면 escalate다.\n"
    "예를 들어 근거가 '주차비는 기타 경비로 지급'인데 질문이 '주차 위반 과태료'라면, "
    "주차라는 단어가 겹쳐도 과태료를 다루지 않으므로 escalate다.\n"
    "판정과 함께 질문의 제도 영역과 대상 관계도 뽑는다.\n"
    "- intent: ceremony(경조금) | housing(숙소지원금) | relocation(부임비·이전비) | "
    "trip(출장·여비) | club(동호회) | other 중 하나. 질문이 실제로 묻는 제도를 고른다. "
    "단어가 겹쳐도 묻는 제도가 아니면 고르지 않는다. '이사회 참석 출장비'는 relocation이 아니라 trip이다.\n"
    "other는 회사의 복리후생·비용 지원과 무관한 질문에만 쓴다. 점심 메뉴 추천이 그런 예다. "
    "회사가 비용을 부담하는지 묻는 질문은 규정에 해당 항목이 없더라도 가장 가까운 영역을 고른다. "
    "'주차 위반 과태료를 회사가 내주는지'는 other가 아니라 trip이다.\n"
    "- relation: 경조사·경조금 질문에서 사유가 발생한 대상과 사용자의 관계. "
    "본인, 본인 부모, 배우자 부모, 본인 형제·자매, 배우자 형제·자매, 자녀, 조부모처럼 적는다. "
    "본인이 당사자면 '본인'이다. 해당 없으면 null.\n"
    "- finding: clarify일 때, 근거만으로 이미 확정할 수 있는 사실을 한두 문장으로 적는다. "
    "기한이 지났다거나 원칙은 무엇이고 어떤 예외가 남았는지처럼 사용자가 바로 알아야 할 내용이다. "
    "확정할 수 있는 것이 없으면 빈 문자열.\n"
    "- evidence_ids: 판정·finding에 직접 사용한 근거 번호만 정수 배열로 적는다. 질문과 같은 제도라는 "
    "이유만으로 넣지 말고, 실제 판단 문장을 뒷받침하는 최소한의 근거만 고른다. 직접 근거가 없으면 빈 배열이다.\n"
    "JSON만 출력한다. 형식: "
    '{"verdict": "answerable|clarify|escalate", "reason": "한 문장", "missing": ["질문에 빠진 정보"], '
    '"finding": "이미 확정되는 사실", '
    '"intent": "ceremony|housing|relocation|trip|club|other", "relation": "관계 또는 null", '
    '"evidence_ids": [1, 2]}'
)


def judge_groundedness(question, evidence, history=None):
    """검색 근거만으로 답할 수 있는지 LLM에 판정을 맡깁니다."""
    api_key = os.environ.get("OPENAI_API_KEY", "").strip()
    if not api_key or not evidence:
        return {"verdict": "escalate", "reason": "검색된 근거가 없습니다.", "missing": [], "evidence_ids": []}
    evidence_text = "\n\n".join(
        f"[근거 {index}] {item['file']} ({item.get('path', '')})\n{item['text'][:1200]}"
        for index, item in enumerate(evidence, 1)
    )
    # 이전 대화를 함께 넘깁니다. 판정이 자기가 무엇을 되물었는지 모르면 표현만 바꿔 같은 것을
    # 다시 묻고, 사용자가 답해도 대화가 끝나지 않습니다.
    prior = "\n".join(
        f"{'사용자' if item.get('role') == 'user' else '상담'}: {item.get('content', '')[:600]}"
        for item in (history or [])[-4:]
        if item.get("role") in ("user", "assistant") and item.get("content")
    )
    prior_text = f"이전 대화:\n{prior}\n\n" if prior else ""
    payload = {
        "model": MODEL,
        "reasoning": {"effort": "none"},
        # 신청기한처럼 경과일로 갈리는 판정에는 기준일이 있어야 합니다.
        "instructions": f"{GROUNDEDNESS_INSTRUCTIONS}\n오늘 기준일은 {date.today().isoformat()}이다.",
        "input": [{"role": "user", "content": f"{prior_text}질문: {question}\n\n검색된 규정 근거:\n{evidence_text}"}],
    }
    request = Request(
        "https://api.openai.com/v1/responses",
        data=json.dumps(payload).encode("utf-8"),
        headers={"Authorization": f"Bearer {api_key}", "Content-Type": "application/json"},
        method="POST",
    )
    try:
        with urlopen(request, timeout=45) as response:
            data = json.loads(response.read().decode("utf-8"))
    except (HTTPError, URLError, TimeoutError, OSError):
        # 판정에 실패하면 단정하지 않고 담당 부서 확인으로 보냅니다.
        return {"verdict": "escalate", "reason": "근거 판정을 수행하지 못했습니다.", "missing": [], "evidence_ids": []}
    text = data.get("output_text") or ""
    if not text:
        for output in data.get("output", []):
            for content in output.get("content", []):
                if content.get("type") == "output_text":
                    text += content.get("text", "")
    match = re.search(r"\{.*\}", text, re.S)
    if not match:
        return {"verdict": "escalate", "reason": "판정 응답을 해석하지 못했습니다.", "missing": [], "evidence_ids": []}
    try:
        result = json.loads(match.group(0))
    except json.JSONDecodeError:
        return {"verdict": "escalate", "reason": "판정 응답을 해석하지 못했습니다.", "missing": [], "evidence_ids": []}
    if result.get("verdict") not in ("answerable", "clarify", "escalate"):
        result["verdict"] = "escalate"
    if result.get("intent") not in POLICY_INTENTS:
        # 의도를 못 뽑으면 규칙을 걸러내지 않고 기존 키워드 판별에 맡깁니다.
        result["intent"] = None
    result.setdefault("reason", "")
    result.setdefault("missing", [])
    result.setdefault("relation", None)
    result.setdefault("finding", "")
    result.setdefault("evidence_ids", [])
    return result


def select_used_evidence(question, candidate_evidence, judgement):
    """검색 후보 중 판정에 직접 사용한 근거만 사용자 표시용으로 남깁니다.

    판정기의 번호는 신뢰하지 않고 현재 후보 범위의 정수만 허용합니다. 번호가 없거나 모두
    잘못됐으면 검색 1위와 같은 파일의 근거만 남겨, 후보 전체가 화면에 노출되지 않게 합니다.
    """
    intent = judgement.get("intent")
    allowed_files = INTENT_EVIDENCE_FILES.get(intent)
    eligible = [
        item for item in candidate_evidence
        if not allowed_files or item.get("file") in allowed_files
    ]
    if not eligible:
        eligible = list(candidate_evidence)

    # 같은 제도 안에서도 질문의 직접 어휘와 거리가 먼 문서는 표시 근거에서 제외합니다.
    # 의미 검색 후보는 판정용으로 유지하되, 사용자에게 보이는 출처에는 더 엄격한 기준을 씁니다.
    best_keyword_by_file = {}
    for item in eligible:
        file_name = item.get("file")
        best_keyword_by_file[file_name] = max(
            best_keyword_by_file.get(file_name, 0.0),
            float(item.get("keyword_score", 0.0)),
        )
    top_keyword = max(best_keyword_by_file.values(), default=0.0)
    anchor_terms = {
        evidence_term
        for query_term, evidence_terms in EVIDENCE_QUERY_ANCHORS.items()
        if query_term in question
        for evidence_term in evidence_terms
    }
    anchored_files = {
        item.get("file")
        for item in eligible
        if anchor_terms and any(term in f"{item.get('path', '')} {item.get('text', '')}" for term in anchor_terms)
    }
    strong_files = anchored_files or {
        file_name for file_name, score in best_keyword_by_file.items()
        if not top_keyword or score >= top_keyword * USED_EVIDENCE_KEYWORD_RATIO
    }

    selected = []
    seen = set()
    raw_ids = judgement.get("evidence_ids", [])
    if not isinstance(raw_ids, list):
        raw_ids = []
    for raw_id in raw_ids:
        if isinstance(raw_id, bool) or not isinstance(raw_id, int):
            continue
        index = raw_id - 1
        if not 0 <= index < len(candidate_evidence) or index in seen:
            continue
        item = candidate_evidence[index]
        if item not in eligible or item.get("file") not in strong_files:
            continue
        selected.append(item)
        seen.add(index)
    if selected:
        # 화면에는 파일명이 표시되므로 선택 단위도 파일로 맞춥니다. 같은 규정 안에서 판정기가
        # 직접 고르지 않은 표·제출서류 청크도 생성에는 필요할 수 있어 선택된 파일의 후보를
        # 함께 전달합니다. 다른 파일은 계속 제외됩니다.
        selected_files = {item.get("file") for item in selected}
        return [item for item in eligible if item.get("file") in selected_files]
    if not eligible:
        return []
    top_file = max(best_keyword_by_file, key=best_keyword_by_file.get) if top_keyword else eligible[0].get("file")
    return [item for item in eligible if item.get("file") == top_file]


RESOLVE_INSTRUCTIONS = (
    "너는 사내 복리후생 상담의 질문 정리기다. 이전 대화와 사용자의 새 입력을 읽고, "
    "새 입력을 그것만 읽어도 이해되는 하나의 질문으로 다시 쓴다. 답변하지 말고 질문만 만든다.\n"
    "- 이전 대화에 나온 날짜·장소·금액·관계·이동 경로 등 판단에 필요한 사실을 새 질문에 옮겨 담는다.\n"
    "- 새 입력이 직전 답변의 확인 요청에 대한 대답이면, 무엇을 확인해 준 것인지 드러나게 쓴다.\n"
    "- 새 입력이 이전 대화와 무관한 새 질문이면 그대로 둔다. 이전 주제를 끌어오지 않는다.\n"
    "- 사실을 지어내지 않는다. 이전 대화에 없는 내용은 넣지 않는다.\n"
    "질문 문장 하나만 출력한다. 설명이나 따옴표를 붙이지 않는다."
)


def resolve_question(question, history):
    """후속 입력을 이전 대화의 사실을 담은 자립형 질문으로 다시 씁니다.

    낱말 목록으로 새 주제인지 판별하던 방식은 양방향으로 틀렸습니다. "해외출장입니다"는
    '출장'이 걸려 이력이 끊겼고, "부모님 상당했는데"는 아무것도 안 걸려 지난 주제가 남았습니다.
    후속인지 아닌지를 코드가 정하지 않고, 다시 쓴 질문 하나를 검색·판정·생성이 함께 씁니다.
    """
    api_key = os.environ.get("OPENAI_API_KEY", "").strip()
    if not history or not api_key:
        return question
    conversation = [
        {"role": item["role"], "content": item["content"]}
        for item in history[-6:]
        if item.get("role") in ("user", "assistant") and item.get("content")
    ]
    if not conversation:
        return question
    conversation.append({"role": "user", "content": f"새 입력: {question}"})
    payload = {
        "model": MODEL,
        "reasoning": {"effort": "none"},
        "instructions": RESOLVE_INSTRUCTIONS,
        "input": conversation,
    }
    request = Request(
        "https://api.openai.com/v1/responses",
        data=json.dumps(payload).encode("utf-8"),
        headers={"Authorization": f"Bearer {api_key}", "Content-Type": "application/json"},
        method="POST",
    )
    try:
        with urlopen(request, timeout=30) as response:
            data = json.loads(response.read().decode("utf-8"))
    except (HTTPError, URLError, TimeoutError, OSError):
        # 재작성에 실패하면 원문 질문으로 진행합니다. 맥락은 잃어도 답변 경로는 살립니다.
        return question
    text = (data.get("output_text") or "").strip()
    if not text:
        for output in data.get("output", []):
            for content in output.get("content", []):
                if content.get("type") == "output_text":
                    text += content.get("text", "")
    text = text.strip().strip('"')
    return text or question


class ConsultationState(TypedDict, total=False):
    """LangGraph가 상담 단계 사이에서 전달하는 최소 상태입니다."""

    question: str
    resolved: str
    history: list[dict]
    intent: str
    candidate_evidence: list[dict]
    used_evidence: list[dict]
    analysis: dict
    answer: str
    ui_actions: list[str]


def resolve_question_node(state: ConsultationState):
    """이후 모든 단계가 함께 쓸 자립형 질문을 먼저 만듭니다."""
    return {"resolved": resolve_question(state["question"], state.get("history", []))}


def retrieve_policy_node(state: ConsultationState):
    """현재 제도 질문에 맞는 규정 근거를 검색합니다."""
    return {"candidate_evidence": retrieve(state.get("resolved") or state["question"])}


def analyze_question_node(state: ConsultationState):
    """검색 근거로 답할 수 있는지와 함께 제도 영역·대상 관계를 한 번에 뽑습니다."""
    question = state.get("resolved") or state["question"]
    return {
        "analysis": judge_groundedness(
            question,
            state.get("candidate_evidence", []),
            state.get("history", []),
        )
    }



def build_clarify_answer(missing, finding=""):
    """확정된 사실을 먼저 알리고, 판정이 짚은 부족한 정보만 되묻습니다.

    선택지를 검색 상위 조항 제목으로 만들던 블록은 지웠습니다. 판정과 무관한 순위라
    교통비 문의에 "5.4 출장복명"이 올라오고, FAQ 파일의 리프인 "질문"·"답변"도 섞였습니다.
    무엇이 부족한지는 판정이 이미 missing으로 뽑아 두므로 그것만 되묻습니다.
    """
    if finding:
        lines = [f"{finding}\n"]
    else:
        lines = ["문의하신 제도는 확인했지만, 답변을 확정하려면 정보가 조금 더 필요합니다.\n"]
    if missing:
        lines.append("확인이 필요한 내용")
        lines.extend(f"- {item}" for item in missing[:4])
        lines.append("")
        closing = "위 내용을 알려주시면 해당 기준으로 안내해 드리겠습니다. "
    else:
        closing = "어떤 부분을 확인하고 싶은지 알려주시면 해당 기준으로 안내해 드리겠습니다. "
    lines.append(
        closing
        + "바로 담당자 확인이 필요하시면 아래 ‘담당자에게 문의하기’ 버튼으로 문의 메일 초안을 만들 수 있습니다."
    )
    return "\n".join(lines)


def build_escalation_answer(reason, evidence):
    """규정이 다루지 않는 문의는 단정하지 않고 담당 부서로 넘깁니다."""
    checked = sorted({item.get("file", "") for item in evidence if item.get("file")})
    lines = ["문의하신 내용은 현재 제공된 규정만으로 지원 여부를 확정할 수 없습니다.\n"]
    lines.append("확인 결과")
    if reason:
        lines.append(f"- 판정 사유: {reason}")
    if checked:
        lines.append(f"- 확인한 규정: {', '.join(Path(name).stem for name in checked)}")
    lines.append("- 판정: 주관 부서 확인 필요\n")
    lines.append(
        "아래 ‘담당자에게 문의하기’ 버튼을 누르면 지금까지의 문의 내용이 담긴 메일 초안을 만들 수 있습니다. "
        "최종 지원 여부는 노무관리 주관부서의 규정 검토를 거쳐 결정됩니다."
    )
    return "\n".join(lines)


def generate_answer_node(state: ConsultationState):
    """근거로 답할 수 있는지 먼저 판정하고, 답변·되묻기·이관으로 나눕니다."""
    # 재작성된 질문이 이전 대화의 사실을 이미 담고 있으므로 이력을 낱말로 끊지 않습니다.
    question = state.get("resolved") or state["question"]
    history = state.get("history", [])
    candidate_evidence = state.get("candidate_evidence", [])
    if not candidate_evidence:
        answer = build_unknown_policy_answer(question)
        used_evidence = []
    else:
        judgement = state.get("analysis") or judge_groundedness(question, candidate_evidence, history)
        used_evidence = select_used_evidence(question, candidate_evidence, judgement)
        verdict = judgement["verdict"]
        if verdict == "clarify":
            answer = build_clarify_answer(judgement.get("missing", []), judgement.get("finding", ""))
        elif verdict == "escalate":
            # 복리후생과 무관한 질문은 담당 부서로 넘기지 않고 상담 범위를 안내합니다.
            if judgement.get("intent") == "other":
                answer = build_clarification_answer(question)
            else:
                answer = build_escalation_answer(judgement.get("reason", ""), used_evidence)
        else:
            answer = call_openai(question, used_evidence, history)
    result = {"answer": answer, "used_evidence": used_evidence}
    # 동호회 신규 신청 문의에는 답변 경로와 무관하게 메일 초안 버튼을 띄웁니다.
    if is_club_application_question(question):
        result["ui_actions"] = ["club_application_draft"]
    return result




def build_consultation_graph():
    """상담 요청을 재작성·검색·판정·생성으로 연결한 LangGraph를 만듭니다."""
    graph = StateGraph(ConsultationState)
    graph.add_node("resolve", resolve_question_node)
    graph.add_node("retrieve", retrieve_policy_node)
    graph.add_node("analyze", analyze_question_node)
    graph.add_node("generate", generate_answer_node)
    # 규정 본문을 파이썬 문자열로 들고 있던 rules 노드는 지웠습니다. 답이 규정과 어긋나면
    # 고정 문구를 고치는 대신 검색·판정을 고칩니다. 모든 질문이 같은 경로를 지납니다.
    graph.add_edge(START, "resolve")
    graph.add_edge("resolve", "retrieve")
    graph.add_edge("retrieve", "analyze")
    graph.add_edge("analyze", "generate")
    graph.add_edge("generate", END)
    return graph.compile()


CONSULTATION_GRAPH = build_consultation_graph()
INQUIRY_HISTORY = []


def build_inquiry_draft(question, answer, evidence):
    """상담 결과를 담당자 문의 메일 초안으로 변환합니다."""
    # 검색 근거는 같은 규정의 여러 조항 청크를 포함할 수 있으므로, 메일에는 규정 파일별로 한 번만 적습니다.
    evidence_files = []
    seen_files = set()
    for item in evidence:
        file_name = str(item.get("file", "")).strip()
        if not file_name or file_name in seen_files:
            continue
        seen_files.add(file_name)
        evidence_files.append(Path(file_name).stem)
    evidence_lines = "\n".join(f"- {file_name}" for file_name in evidence_files) or "- 확인된 규정 근거 없음"
    body = (
        "안녕하세요.\n"
        "복리후생 지원 가능 여부를 확인 부탁드립니다.\n\n"
        "[문의 내용]\n"
        f"{question}\n\n"
        "[AI 사전 검토 내용]\n"
        f"{answer}\n\n"
        "[관련 규정]\n"
        f"{evidence_lines}\n\n"
        "최종 지원 가능 여부와 필요한 추가 서류를 확인해 주시면 감사하겠습니다.\n\n"
        "감사합니다.\n복리후생 상담 시스템 드림"
    )
    return {
        "recipient": "복리후생 담당자",
        "recipient_email": "welfare-demo@example.com",
        "subject": "복리후생 지원 가능 여부 확인 요청",
        "body": body,
        "evidence": evidence,
    }


def build_club_application_draft():
    """동호회 신규 신청에 필요한 정보를 채울 수 있는 담당자 메일 초안을 만듭니다."""
    body = (
        "안녕하세요. 노사발전그룹 동호회 담당자님,\n\n"
        "아래와 같이 동호회 등록을 신청합니다.\n\n"
        "- 동호회 회장: [입력]\n"
        "- 동호회 총무: [입력]\n"
        "- 동호회 명: [입력]\n"
        "- 종목(스포츠, 문화 등): [입력]\n"
        "- 동호회 회칙 작성 및 게시판 등록: [완료 / 미완료]\n"
        "- 총무 계좌번호 동호회 시스템 입력: [완료 / 미완료]\n"
        "- 비고: [입력]\n\n"
        "신청 내용을 확인해 주시고, 등록 절차 및 추가 제출자료가 있으면 안내 부탁드립니다.\n\n"
        "감사합니다.\n[신청자명] 드림"
    )
    return {
        "recipient": "노사발전그룹 동호회 담당자",
        "recipient_email": "",
        "subject": "[동호회 신청] [동호회명] 등록 신청",
        "body": body,
        "evidence": [{"file": "동호회 관리 규정.md", "score": 1, "text": "동호회 신청 양식 및 담당자 안내"}],
    }


class Handler(SimpleHTTPRequestHandler):
    """정적 화면과 상담 요청을 함께 제공하는 간단한 HTTP 핸들러입니다."""

    def do_POST(self):
        if self.path not in ("/api/chat", "/api/inquiries/draft", "/api/club-applications/draft", "/api/inquiries/send", "/api/inquiries/save", "/api/inquiries/delete"):
            self.send_error(404)
            return
        try:
            length = int(self.headers.get("Content-Length", "0"))
            body = json.loads(self.rfile.read(length).decode("utf-8"))
            if self.path == "/api/club-applications/draft":
                self.respond(200, build_club_application_draft())
                return
            if self.path == "/api/inquiries/draft":
                question = str(body.get("question", "")).strip()
                answer = str(body.get("answer", "")).strip()
                if not question or not answer:
                    raise ValueError("문의 초안을 만들 상담 내용이 없습니다.")
                self.respond(200, build_inquiry_draft(question, answer, body.get("evidence", [])))
                return
            if self.path == "/api/inquiries/send":
                subject = str(body.get("subject", "")).strip()
                inquiry_body = str(body.get("body", "")).strip()
                if not subject or not inquiry_body:
                    raise ValueError("메일 제목과 본문을 입력해 주세요.")
                inquiry = {
                    "id": len(INQUIRY_HISTORY) + 1,
                    "recipient": str(body.get("recipient", "복리후생 담당자")),
                    "recipient_email": str(body.get("recipient_email", "welfare-demo@example.com")),
                    "subject": subject,
                    "body": inquiry_body,
                    "sent_at": datetime.now().strftime("%Y-%m-%d %H:%M"),
                    "status": "메일 요청 완료",
                }
                INQUIRY_HISTORY.append(inquiry)
                self.respond(200, inquiry)
                return
            if self.path == "/api/inquiries/save":
                inquiry = {
                    "id": len(INQUIRY_HISTORY) + 1,
                    "recipient": str(body.get("recipient", "복리후생 담당자")),
                    "recipient_email": str(body.get("recipient_email", "welfare-demo@example.com")),
                    "subject": str(body.get("subject", "")).strip(),
                    "body": str(body.get("body", "")).strip(),
                    "saved_at": datetime.now().strftime("%Y-%m-%d %H:%M"),
                    "status": "작성 중",
                }
                if not inquiry["subject"] or not inquiry["body"]:
                    raise ValueError("메일 제목과 본문을 입력해 주세요.")
                INQUIRY_HISTORY.append(inquiry)
                self.respond(200, inquiry)
                return
            if self.path == "/api/inquiries/delete":
                inquiry_id = int(body.get("id", 0))
                INQUIRY_HISTORY[:] = [item for item in INQUIRY_HISTORY if item["id"] != inquiry_id]
                self.respond(200, {"deleted": inquiry_id})
                return
            question = str(body.get("question", "")).strip()
            if not question:
                raise ValueError("질문을 입력해 주세요.")
            result = CONSULTATION_GRAPH.invoke({"question": question, "history": body.get("history", [])})
            self.respond(200, {
                "answer": result["answer"],
                "evidence": result.get("used_evidence", []),
                "ui_actions": result.get("ui_actions", []),
            })
        except (ValueError, RuntimeError, HTTPError, URLError) as error:
            self.respond(400, {"error": str(error)})
        except Exception:
            self.respond(500, {"error": "상담 처리 중 오류가 발생했습니다."})

    def do_GET(self):
        """규정 원문 링크와 기존 정적 파일을 제공합니다."""
        if self.path == "/api/inquiries":
            self.respond(200, {"items": INQUIRY_HISTORY})
            return
        if self.path.startswith("/rules/"):
            requested = unquote(self.path[len("/rules/"):]).split("?", 1)[0]
            candidate = (RULES_DIR / requested).resolve()
            if candidate.parent != RULES_DIR.resolve() or not candidate.is_file():
                self.send_error(404)
                return
            content = candidate.read_bytes()
            self.send_response(200)
            content_type = "text/markdown" if candidate.suffix.lower() == ".md" else "text/plain"
            self.send_header("Content-Type", f"{content_type}; charset=utf-8")
            self.send_header("Content-Length", str(len(content)))
            self.send_header("Content-Disposition", "inline")
            self.end_headers()
            self.wfile.write(content)
            return
        super().do_GET()

    def respond(self, status, payload):
        """JSON 응답을 반환합니다."""
        raw = json.dumps(payload, ensure_ascii=False).encode("utf-8")
        self.send_response(status)
        self.send_header("Content-Type", "application/json; charset=utf-8")
        self.send_header("Content-Length", str(len(raw)))
        self.end_headers()
        self.wfile.write(raw)


if __name__ == "__main__":
    load_env()
    server = ThreadingHTTPServer(("127.0.0.1", 8000), Handler)
    print("http://127.0.0.1:8000 에서 실행 중입니다.")
    server.serve_forever()
````

## index.html

원본 경로: `index.html`

````html
<!doctype html>
<html lang="ko">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>사내 복리후생 통합 상담</title>
  <style>
    :root{--bg:#fcfcfa;--ink:#232522;--muted:#72766f;--soft:#f5f6f3;--sidebar:#eef6ea;--line:#e1e4de;--accent:#486a4a;--accent-dark:#2f5534;--accent-soft:#e5f0e3;--card:#fff;--warn:#fff8eb}
    *{box-sizing:border-box}
    html{scroll-behavior:smooth}
    body{margin:0;background:var(--bg);color:var(--ink);font-family:Pretendard,"Noto Sans KR","Apple SD Gothic Neo",Arial,sans-serif}
    button,textarea,input{font:inherit}
    button{color:inherit}
    .app{min-height:100vh;display:flex}
    .sidebar{position:fixed;inset:0 auto 0 0;width:264px;background:linear-gradient(145deg,#edf6e9 0%,#f5f9f2 68%,#eef6ea 100%);border-right:1px solid #dce7d8;display:flex;flex-direction:column;padding:18px 14px 14px;z-index:5}
    .brand-row{display:flex;align-items:center;gap:11px;padding:0 8px 18px}.mark{width:38px;height:38px;border-radius:11px;background:var(--accent);color:#fff;display:grid;place-items:center;font-weight:800;font-size:18px;box-shadow:0 5px 14px #36573a24}.brand{font-weight:760;font-size:17px}
    .new-chat{border:0;background:var(--accent);color:#fff;border-radius:11px;padding:13px 15px;display:flex;align-items:center;gap:11px;font-weight:700;cursor:pointer;box-shadow:0 5px 14px #36573a1f}.new-chat span{font-size:22px;font-weight:400;line-height:1}
    .sidebar nav{min-height:0;overflow:auto}.nav-section{margin-top:25px}.nav-title{padding:0 10px 8px;color:#777f75;font-size:12px;font-weight:700}.nav-item{width:100%;border:0;background:transparent;border-radius:10px;padding:10px 11px;display:flex;align-items:center;gap:11px;text-align:left;cursor:pointer;font-size:14px}.nav-item:hover,.nav-item.active{background:#e1ecdd}.nav-icon{width:20px;text-align:center;color:#435244}.sidebar-rule{border:0;border-top:1px solid #dbe6d7;margin:18px 8px 0}
    .recent-section{margin-top:22px}.recent-head{display:flex;align-items:center;justify-content:space-between;padding:0 7px 7px 10px}.recent-head .nav-title{padding:0}.clear-history{border:0;background:transparent;color:var(--muted);font-size:11px;padding:3px;cursor:pointer}.clear-history:hover{color:var(--accent-dark)}.recent-list{display:grid;gap:3px}.recent-empty,.storage-note{padding:7px 10px;color:var(--muted);font-size:11px;line-height:1.5}.recent-item{display:grid;grid-template-columns:minmax(0,1fr) 24px;align-items:center;border-radius:9px}.recent-item:hover,.recent-item.active{background:#e1ecdd}.recent-open{min-width:0;border:0;background:transparent;padding:9px 4px 9px 11px;text-align:left;cursor:pointer}.recent-title{display:block;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;font-size:13px}.recent-date{display:block;color:var(--muted);font-size:10px;margin-top:3px}.recent-delete{border:0;background:transparent;color:#7e847c;width:24px;height:28px;border-radius:6px;cursor:pointer}.recent-delete:hover{background:#d2e0ce;color:#83453e}.storage-note{margin-top:5px}.mobile-menu{display:none;border:0;background:transparent;font-size:22px;padding:4px;cursor:pointer}.sidebar-backdrop{display:none}
    .workspace{min-width:0;flex:1;margin-left:264px}.mobile-top{display:none}.content-grid{min-height:100vh}.main-content{width:min(920px,calc(100% - 48px));margin:0 auto;padding:70px 0 190px}.hero{text-align:center;min-height:525px;display:flex;flex-direction:column;justify-content:center;align-items:center}.assistant-mark{width:56px;height:48px;margin-bottom:24px;position:relative;border:4px solid #5f8662;border-radius:22px;display:flex;align-items:center;justify-content:center;gap:7px}.assistant-mark:after{content:"";position:absolute;bottom:-8px;left:10px;width:14px;height:14px;border-left:4px solid #5f8662;transform:skew(-30deg) rotate(-18deg);background:var(--bg)}.assistant-mark i{width:6px;height:6px;border-radius:50%;background:#5f8662}.hero h1{font-size:34px;letter-spacing:-1.2px;margin:0 0 12px}.hero>p{color:var(--muted);font-size:16px;margin:0 0 34px}.suggestions{width:min(720px,100%);display:grid;grid-template-columns:1fr 1fr;gap:14px}.suggestion{border:1px solid var(--line);background:#fff;border-radius:14px;padding:18px;display:grid;grid-template-columns:42px 1fr 18px;align-items:center;gap:13px;text-align:left;cursor:pointer;transition:.18s ease}.suggestion:hover{border-color:#b8cab5;transform:translateY(-1px);box-shadow:0 8px 22px #354d3710}.suggestion-icon{width:42px;height:42px;border-radius:50%;display:grid;place-items:center;background:var(--accent-soft);font-size:20px}.suggestion:nth-child(2) .suggestion-icon{background:#fff1d9;color:#9b6312}.suggestion strong{display:block;font-size:15px;margin-bottom:5px}.suggestion small{display:block;color:var(--muted);line-height:1.45}.suggestion-arrow{color:#8b9089;font-size:20px}
    .chat{display:flex;flex-direction:column;gap:24px}.chat:not(:has(.empty)){padding-top:8px}.message{display:flex;gap:12px;line-height:1.7}.avatar{width:34px;height:34px;border-radius:10px;display:grid;place-items:center;flex:0 0 34px;font-size:12px;font-weight:700}.user{justify-content:flex-end}.user .avatar{order:2;background:#dce8da;color:#315638}.assistant .avatar{background:var(--accent);color:#fff}.bubble{max-width:calc(100% - 46px);white-space:pre-wrap}.user .bubble{background:#f0f1ee;border:0;border-radius:15px 4px 15px 15px;padding:12px 16px}.assistant .bubble{background:#fff;border:1px solid var(--line);border-radius:4px 15px 15px 15px;padding:18px;flex:1;box-shadow:0 3px 12px #31433108}
    .evidence{display:grid;gap:10px;margin-top:18px}.evidence-card{background:var(--card);border:1px solid var(--line);border-radius:10px;padding:14px 16px}.evidence-title{font-weight:700;margin-bottom:6px;color:var(--accent-dark)}.meta{font-size:12px;color:var(--muted)}.tag{display:inline-block;background:var(--accent-soft);color:var(--accent-dark);border-radius:5px;padding:3px 8px;font-size:12px;margin:8px 6px 0 0}.notice{background:var(--warn);border:1px solid #f0d9a7;border-radius:9px;padding:12px 14px;margin-top:12px;color:#805914}.inquiry-button{margin-top:14px;border:1px solid #aebfad;background:#fff;color:var(--accent-dark);border-radius:8px;padding:8px 12px;cursor:pointer;font-weight:700}.inquiry-button:hover{background:#f3f8f1}.quick-links{margin-top:16px;padding-top:12px;border-top:1px solid var(--line)}.quick-links-title{font-size:12px;color:var(--muted);margin-bottom:8px}.quick-links-list{display:flex;flex-wrap:wrap;gap:8px}.quick-link{display:inline-flex;align-items:center;gap:6px;border:1px solid var(--line);background:#fff;color:#515650;border-radius:8px;padding:6px 10px;font-size:12px;text-decoration:none}.quick-link:hover{border-color:#9fb59c;color:var(--accent-dark)}
    .sources{display:flex;align-items:center;flex-wrap:wrap;gap:8px;margin-top:18px;padding-top:12px;border-top:1px solid var(--line);font-size:12px;color:var(--muted)}.sources a{color:var(--accent-dark);text-decoration:none;border-bottom:1px solid #abc0a8}.sources a:hover{color:#1e4726}
    .hero.hidden{display:none}.empty{display:none}.inquiry-history{display:none;margin-top:32px;background:#fff;border:1px solid var(--line);border-radius:12px;padding:18px}.inquiry-history.visible{display:block}.inquiry-history h3{margin:0 0 10px;font-size:16px}.history-row{border-top:1px solid var(--line);padding:11px 0;font-size:13px}.history-status{color:#34703e;font-weight:700}.secondary{border:1px solid var(--line);background:#fff;border-radius:8px;padding:9px 14px;cursor:pointer}
    .inquiry-panel{position:fixed;inset:0;background:#17201766;display:grid;place-items:center;padding:20px;z-index:20}.inquiry-card{width:min(680px,100%);max-height:90vh;overflow:auto;background:#fff;border-radius:16px;padding:24px;box-shadow:0 18px 60px #13201438}.inquiry-card h2{margin:0 0 18px}.inquiry-card label{display:block;font-weight:700;font-size:13px;margin:12px 0 6px}.inquiry-card input,.inquiry-card textarea{width:100%;border:1px solid var(--line);border-radius:9px;padding:10px;outline-color:#6c8d6b}.inquiry-card textarea{min-height:260px;resize:vertical}.inquiry-actions{position:sticky;bottom:0;z-index:1;display:flex;justify-content:flex-end;align-items:center;gap:10px;margin-top:20px;padding-top:16px;background:#fff;border-top:1px solid #edf0eb}.inquiry-actions .secondary,.inquiry-actions .inquiry-submit{min-width:92px;height:44px;padding:0 17px;border-radius:10px;font-size:14px;font-weight:700}.inquiry-actions .secondary{color:#4f574e}.inquiry-actions .inquiry-submit{border:0;background:var(--accent-dark);color:#fff;cursor:pointer}.inquiry-actions .inquiry-submit:hover{background:#24472a}
    .composer-wrap{position:fixed;left:264px;right:0;bottom:0;z-index:4;background:linear-gradient(transparent,var(--bg) 25%);padding:36px 28px 20px}.context-chips{display:flex;justify-content:center;gap:8px;margin-bottom:10px}.context-chip{background:#eef2ec;color:#556255;border-radius:20px;padding:5px 11px;font-size:11px}.composer{width:min(920px,100%);margin:auto;background:#fff;border:1px solid #d9ddd6;border-radius:18px;box-shadow:0 8px 28px #27392714;display:flex;align-items:flex-end;padding:11px 11px 11px 18px}.composer textarea{border:0;outline:0;resize:none;overflow-y:hidden;flex:1;min-height:34px;max-height:130px;line-height:1.5;padding:6px 0;background:transparent}.send{border:0;background:var(--accent-dark);color:#fff;border-radius:50%;width:38px;height:38px;cursor:pointer;font-size:20px;display:grid;place-items:center}.send:disabled{background:#c9cdc7;cursor:not-allowed}.hint{width:min(920px,100%);margin:9px auto 0;text-align:center;color:#858a83;font-size:11px}
    @media(max-width:760px){.sidebar{display:flex;transform:translateX(-100%);transition:transform .2s ease;box-shadow:10px 0 30px #1f2c2230;z-index:12}.sidebar.open{transform:translateX(0)}.sidebar-backdrop.open{display:block;position:fixed;inset:0;background:#17201766;z-index:11}.workspace{margin-left:0}.mobile-top{height:58px;display:flex;align-items:center;gap:10px;padding:0 16px;border-bottom:1px solid var(--line);background:#f7faf5;position:sticky;top:0;z-index:6}.mobile-menu{display:block}.mobile-top .mark{width:32px;height:32px;font-size:15px}.mobile-top .brand{font-size:15px}.main-content{width:calc(100% - 30px);padding:35px 0 175px}.hero{min-height:auto;padding:45px 0}.hero h1{font-size:27px}.hero>p{font-size:14px;line-height:1.6}.suggestions{grid-template-columns:1fr}.composer-wrap{left:0;padding:30px 14px 14px}.context-chips{display:none}.hint{font-size:10px}.message{gap:8px}.avatar{width:30px;height:30px;flex-basis:30px}.bubble{max-width:calc(100% - 38px)}.inquiry-card{padding:18px}.inquiry-actions{flex-wrap:wrap}.inquiry-actions .secondary,.inquiry-actions .inquiry-submit{flex:1;min-width:0}}
  </style>
</head>
<body>
  <div class="app">
    <aside class="sidebar" aria-label="주 메뉴">
      <div class="brand-row"><div class="mark">복</div><div class="brand">복리후생 AI 상담</div></div>
      <button class="new-chat" type="button"><span>＋</span>새 상담</button>
      <nav>
        <div class="recent-section">
          <div class="recent-head"><div class="nav-title">최근 상담</div><button id="clear-chat-history" class="clear-history" type="button">전체 삭제</button></div>
          <div id="recent-chat-list" class="recent-list"><div class="recent-empty">저장된 상담이 없습니다.</div></div>
          <div class="storage-note">최근 상담은 이 브라우저에만 30일간 저장됩니다.</div>
        </div>
        <div class="nav-section"><div class="nav-title">주요 메뉴</div>
          <button class="nav-item active" type="button" data-action="new"><span class="nav-icon">✎</span>새 상담</button>
          <button class="nav-item" type="button" data-action="history"><span class="nav-icon">◷</span>담당자 문의 내역</button>
        </div>
        <hr class="sidebar-rule">
        <div class="nav-section"><div class="nav-title">복리후생 제도</div>
          <button class="nav-item benefit-link" type="button" data-question="여비관리 기준에서 출장·파견·부임 관련 지원 대상, 지급 항목, 한도 및 신청 절차를 알려줘."><span class="nav-icon">✈</span>출장·여비</button>
          <button class="nav-item benefit-link" type="button" data-question="경조금 지급기준에서 지급 대상, 금액, 신청 기한 및 필요 서류를 알려줘."><span class="nav-icon">♧</span>경조금</button>
          <button class="nav-item benefit-link" type="button" data-question="숙소지원금 운영 기준에서 지원 대상, 지원 금액, 제외 조건, 신청 기한 및 필요 서류를 알려줘."><span class="nav-icon">⌂</span>숙소지원금</button>
          <button class="nav-item benefit-link" type="button" data-question="동호회 관리 규정에서 개설·가입 조건, 지원 기준, 신청 절차 및 필요 서류를 알려줘."><span class="nav-icon">♙</span>동호회</button>
        </div>
      </nav>
    </aside>
    <div id="sidebar-backdrop" class="sidebar-backdrop"></div>
    <div class="workspace">
      <header class="mobile-top"><button id="mobile-menu" class="mobile-menu" type="button" aria-label="최근 상담 메뉴 열기">☰</button><div class="mark">복</div><div class="brand">복리후생 AI 상담</div></header>
      <div class="content-grid">
        <main class="main-content">
          <section class="hero" id="hero">
            <div class="assistant-mark" aria-hidden="true"><i></i><i></i></div>
            <h1>무엇을 도와드릴까요?</h1>
            <p>사내 규정을 근거로 복리후생 상담과 신청 조건 확인을 도와드립니다.</p>
            <div class="suggestions">
              <button class="suggestion" type="button" data-question="여비관리 기준에서 출장·파견·부임 관련 지원 대상, 지급 항목, 한도 및 신청 절차를 알려줘."><span class="suggestion-icon">✈</span><span><strong>여비관리 기준</strong><small>출장·파견·부임의 지급 기준과 신청 절차를 확인해 보세요.</small></span><span class="suggestion-arrow">›</span></button>
              <button class="suggestion" type="button" data-question="경조금 지급기준에서 지급 대상, 금액, 신청 기한 및 필요 서류를 알려줘."><span class="suggestion-icon">♧</span><span><strong>경조금 지급 기준</strong><small>지급 대상, 지원 금액, 신청 기한을 확인해 보세요.</small></span><span class="suggestion-arrow">›</span></button>
              <button class="suggestion" type="button" data-question="숙소지원금 운영 기준에서 지원 대상, 지원 금액, 제외 조건, 신청 기한 및 필요 서류를 알려줘."><span class="suggestion-icon">⌂</span><span><strong>숙소지원금 기준</strong><small>지원 대상, 제외 조건, 신청 기준을 확인해 보세요.</small></span><span class="suggestion-arrow">›</span></button>
              <button class="suggestion" type="button" data-question="동호회 관리 규정에서 개설·가입 조건, 지원 기준, 신청 절차 및 필요 서류를 알려줘."><span class="suggestion-icon">♙</span><span><strong>동호회 기준</strong><small>개설·가입 조건과 지원 절차를 확인해 보세요.</small></span><span class="suggestion-arrow">›</span></button>
            </div>
          </section>
          <section id="chat" class="chat"><div class="empty">질문을 입력하면 관련 규정을 찾아 답변해 드립니다.</div></section>
          <section id="inquiry-history" class="inquiry-history"><h3>담당자 문의 내역</h3><div id="inquiry-list" class="meta">아직 발송 요청 내역이 없습니다.</div></section>
        </main>
      </div>
      <div class="composer-wrap">
        <div class="context-chips"><span class="context-chip">▤ 규정 기반 상담</span><span class="context-chip">♢ 최종 승인은 담당 부서</span></div>
        <form id="form" class="composer"><textarea id="question" rows="1" placeholder="질문 예시를 불러오는 중..." aria-label="질문 입력"></textarea><button id="send" class="send" type="submit" aria-label="전송">↑</button></form>
        <div class="hint">AI 답변은 참고용이며 최종 승인·지급 여부는 담당 부서의 규정 및 서류 검토에 따릅니다.</div>
      </div>
    </div>
  </div>
  <script>
    // 규정의 핵심 검색 단위입니다. 실제 운영에서는 원문 문서 색인 결과로 교체합니다.
    const rules = [
      {name:'경조금 지급기준', code:'PCR-B-211 Rev. 13', date:'2026-03-04', keys:['경조','결혼','출산','사망','조의','축의','가족관계','청첩장','경조금'], facts:['본인 결혼 200만원, 자녀 결혼 30만원, 본인·배우자 형제자매 결혼 20만원입니다.','출산장려금은 첫째 300만원, 둘째 700만원, 셋째 이상 1,000만원이며 사내부부는 1명에게만 지급합니다.','사망 경조금은 대상 관계에 따라 30만원부터 500만원까지 지급하며, 신청 시 관계를 증명하는 서류가 필요합니다.','경조금 청구권은 사유 발생일로부터 3개월 이내 행사해야 합니다.','급여 지급이 정지된 자는 지급 대상에서 제외되며, 해당 부서장이 지급을 승인합니다.'], docs:['경조금 지급기준 5.2 지급기준','경조금 지급기준 5.3 서류제출','경조금 지급기준 5.6 유효기간']},
      {name:'동호회 관리 규정', code:'내부 운영 기준', date:'기준 문서 확인 필요', keys:['동호회','동아리','활동보조금','회원','회장','총무','행사','영수증','활동사진'], facts:['동호회 등록 최소 인원은 5명이며, 신규 등록은 노사협력그룹과 사전 협의가 필요합니다.','정기지원은 분기 1회 이상 활동 시 분기 단위 인당 3만원, 최대 30만원입니다.','특별지원은 회사를 대표하는 사외 행사 등에 대해 연간 2회, 인당 1만원, 최대 30만원입니다.','활동보조금 지급에는 활동보고서, 영수증, 활동사진, 참석자 명단이 필요합니다.','신규 등록 동호회는 등록 3개월 이후부터 지원하며, 동일 분야 이중 가입은 제한될 수 있습니다.'], docs:['동호회 관리 규정 5.1~5.4 결성·가입','동호회 관리 규정 7 활동보조금 지급 기준','동호회 관리 규정 7.2 서류 제출']},
      {name:'숙소지원금 운영 기준', code:'PCR-B-219 Rev. 1', date:'2026-03-04', keys:['숙소','주거','월세','전세','부임','타지역','무주택','단신','임대','통근버스','40만원','60만원'], facts:['지원 대상은 타지역 출신의 무주택 단신부임 직원이며, 신규 채용·부임·그룹사 인력교류자가 포함될 수 있습니다.','신규 채용은 서울 외 지역에서 발령일로부터 5년간 월 40만원을 지원합니다.','신규 부임은 서울 외 지역 월 40만원, 서울 월 60만원을 발령일로부터 3년간 지원합니다.','월세는 월 차임만 지원하고 관리비·공과금은 제외합니다. 전세는 전세금 1,000만원당 월 10만원 기준입니다.','발령 월 다음 달부터 6개월 이내 신청하지 않으면 자격을 상실할 수 있으며, P8 이상 임원·파견비 대상자 등은 제외될 수 있습니다.','타지역이어도 통근버스가 운행되면 지급이 중단될 수 있습니다.'], docs:['숙소지원금 운영 기준 5.1 지원내용','숙소지원금 운영 기준 5.2~5.4 대상·제외·지원기준','숙소지원금 운영 기준 5.6 제출서류']},
      {name:'여비관리기준', code:'여비관리기준', date:'기준 문서 확인 필요', keys:['여비','출장','국내출장','해외출장','국외','부임비','이전비','교통비','교통','숙박비','소액경비','택시','렌터카','유류비','연비','교육','교육연수','연수','포항','서울','토요일','주말','기차','기차비','철도','열차','자가차량'], facts:['국내 출장의 전 직원 소액경비는 1일 5만원, 숙박비는 실비 10만원 한도 기준이 적용될 수 있습니다.','교육·연수 참석도 출장 목적과 이동 경로가 확인되면 국내여비 기준을 검토할 수 있습니다.','자가차량 이용 시 유류비·통행료·감가상각비를 기준에 따라 실비 지급하며, 거리와 차량 유형에 따라 산식이 달라집니다.','왕복 120km 이상 근거리 출장에는 소액경비 전액, 50km 이상은 1/2, 50km 미만은 미지급 기준이 있습니다.','부임비와 이전비는 직급·이동 지역·자택 여부에 따라 달라지며, 부임 후 6개월 이내 1회 신청 기준이 있습니다.','토요일·주말 이동 여부만으로 기차비 지급 여부를 확정할 수 없으며, 출장명령·교육 참석 승인·실제 이동 구간 확인이 필요합니다.'], docs:['여비관리기준 5.10 국내여비','여비관리기준 5.11 부임비 및 이전비','여비관리기준 5.14 국내여비 특례','여비관리기준 별첨 1~4 지급기준']}
    ];

    const form=document.querySelector('#form'), input=document.querySelector('#question'), chat=document.querySelector('#chat'), send=document.querySelector('#send');
    const CHAT_STORAGE_KEY='welfare-consultations-v1', CHAT_RETENTION_DAYS=30, CHAT_MAX_ITEMS=20;
    const conversation=[];
    let currentMessages=[],currentConversationId=null;
    input.placeholder='복리후생 제도나 신청 조건을 물어보세요';
    send.disabled=true;
    // 빠른 질문은 입력창에 내용을 채우고 바로 상담을 시작합니다.
    document.querySelectorAll('[data-question]').forEach(button=>button.addEventListener('click',()=>{input.value=button.dataset.question;input.dispatchEvent(new Event('input'));form.requestSubmit()}));
    document.querySelector('[data-action="history"]').addEventListener('click',()=>{closeMobileSidebar();const history=document.querySelector('#inquiry-history');history.classList.add('visible');history.scrollIntoView({behavior:'smooth',block:'center'})});
    document.querySelectorAll('.new-chat,[data-action="new"]').forEach(button=>button.addEventListener('click',resetChat));
    document.querySelector('#mobile-menu').addEventListener('click',openMobileSidebar);
    document.querySelector('#sidebar-backdrop').addEventListener('click',closeMobileSidebar);
    document.querySelector('#clear-chat-history').addEventListener('click',clearChatHistory);
    document.querySelector('#recent-chat-list').addEventListener('click',event=>{const remove=event.target.closest('[data-delete-chat]');if(remove){deleteConversation(remove.dataset.deleteChat);return}const open=event.target.closest('[data-open-chat]');if(open)restoreConversation(open.dataset.openChat)});
    input.addEventListener('input',()=>{input.style.height='auto';input.style.height=Math.min(input.scrollHeight,130)+'px';send.disabled=!input.value.trim()});
    // 엔터는 전송하고, 여러 줄 질문이 필요할 때는 Shift+Enter를 사용합니다.
    // 한글 조합 중 엔터는 무시합니다. IME는 조합 확정 전에 Enter를 먼저 보내므로,
    // 그대로 전송하면 확정된 마지막 글자가 입력창에 남아 다음 엔터에 또 전송됩니다.
    // keyCode 229는 isComposing이 없는 구형 Safari 대비입니다.
    input.addEventListener('keydown',event=>{if(event.isComposing||event.keyCode===229)return;if(event.key==='Enter'&&!event.shiftKey){event.preventDefault();if(input.value.trim()&&!send.disabled)form.requestSubmit()}});
    form.addEventListener('submit',async e=>{e.preventDefault();const q=input.value.trim();if(!q)return;addUser(q);conversation.push({role:'user',content:q});currentMessages.push({role:'user',content:q});input.value='';input.style.height='auto';send.disabled=true;addTyping();const controller=new AbortController();const timeout=setTimeout(()=>controller.abort(),50000);try{const response=await fetch('/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({question:q,history:conversation.slice(0,-1)}),signal:controller.signal});const data=await response.json();removeTyping();if(!response.ok)throw new Error(data.error||'상담 처리 중 오류가 발생했습니다.');addAnswer(data.answer,data.evidence,data.ui_actions);conversation.push({role:'assistant',content:data.answer});currentMessages.push({role:'assistant',content:data.answer,evidence:data.evidence||[],uiActions:data.ui_actions||[]});saveCurrentConversation()}catch(error){removeTyping();conversation.pop();currentMessages.pop();addError(error.name==='AbortError'?'응답 시간이 초과되었습니다. 잠시 후 다시 시도해 주세요.':error.message)}finally{clearTimeout(timeout);send.disabled=!input.value.trim()}});

    function resetChat(){conversation.splice(0);currentMessages=[];currentConversationId=null;chat.innerHTML='<div class="empty">질문을 입력하면 관련 규정을 찾아 답변해 드립니다.</div>';document.querySelector('#hero').classList.remove('hidden');document.querySelector('#inquiry-history').classList.remove('visible');input.value='';input.style.height='auto';send.disabled=true;renderRecentChats();closeMobileSidebar();window.scrollTo({top:0,behavior:'smooth'});input.focus()}
    function addUser(text,scroll=true){document.querySelector('#hero').classList.add('hidden');const empty=chat.querySelector('.empty');if(empty)empty.remove();chat.insertAdjacentHTML('beforeend',`<div class="message user"><div class="avatar">나</div><div class="bubble">${escapeHtml(text)}</div></div>`);if(scroll)window.scrollTo({top:document.body.scrollHeight,behavior:'smooth'})}
    function addTyping(){chat.insertAdjacentHTML('beforeend',`<div id="typing" class="message assistant"><div class="avatar">AI</div><div class="bubble">답변을 준비하고 있습니다...</div></div>`)}
    function removeTyping(){const typing=document.querySelector('#typing');if(typing)typing.remove()}
    function addError(text){chat.insertAdjacentHTML('beforeend',`<div class="message assistant"><div class="avatar">AI</div><div class="bubble"><div class="notice">⚠ ${escapeHtml(text)}</div></div></div>`)}
    function addAnswer(answer,usedEvidence,uiActions,scroll=true){
      const cleanAnswer=answer.replace(/(?:\*\*)?확인한 규정(?:\*\*)?[:：][\s\S]*$/i,'').trim();
      const sources=[...new Set((usedEvidence||[]).map(x=>x.file))];const sourceLinks=sources.length?`<div class="sources"><span>확인한 규정</span>${sources.map(file=>`<a href="/rules/${encodeURIComponent(file)}" target="_blank" rel="noopener">${escapeHtml(file.replace(/\.(txt|md)$/,''))}</a>`).join('')}</div>`:'';
      const currentQuestion=conversation.at(-1)?.content||'';
      const inquiryData=encodeURIComponent(JSON.stringify({question:currentQuestion,answer:cleanAnswer,evidence:usedEvidence||[]}));
      const travelQuestion=/(출장|여비|기차|철도|열차|교통비|환율|외환|숙박비|부임비|이전비|교육|연수)/.test(currentQuestion);
      const quickLinks=travelQuestion?`<div class="quick-links"><div class="quick-links-title">출장 관련 바로가기</div><div class="quick-links-list"><a class="quick-link" href="https://map.naver.com/" target="_blank" rel="noopener noreferrer">⌖ 네이버지도 ↗</a><a class="quick-link" href="http://www.smbs.biz/ExRate/TodayExRate.jsp" target="_blank" rel="noopener noreferrer">▥ 서울외환중개 ↗</a><a class="quick-link" href="https://www.korail.com/ticket/search/general" target="_blank" rel="noopener noreferrer">▣ 코레일 ↗</a><a class="quick-link" href="https://www.yeogi.com/" target="_blank" rel="noopener noreferrer">▢ 여기어때 ↗</a></div></div>`:'';
      const clubApplicationButton=(uiActions||[]).includes('club_application_draft')?`<button class="inquiry-button club-application-button">동호회 신청 메일 작성</button>`:'';
      chat.insertAdjacentHTML('beforeend',`<div class="message assistant"><div class="avatar">AI</div><div class="bubble">${renderAnswer(cleanAnswer)}${sourceLinks}${quickLinks}<br>${clubApplicationButton}<button class="inquiry-button" data-inquiry="${inquiryData}">담당자에게 문의하기</button></div></div>`);if(scroll)window.scrollTo({top:document.body.scrollHeight,behavior:'smooth'});
      return;
      /* 기존 브라우저 내장 답변 로직은 백엔드 LLM 답변으로 대체되었습니다. */
      /*
      const normalized=q.toLowerCase();const ranked=rules.map(r=>({r,score:r.keys.reduce((n,k)=>n+(normalized.includes(k.toLowerCase())?1:0),0)})).sort((a,b)=>b.score-a.score);const hits=ranked.filter(x=>x.score>0).slice(0,3);
      let body, notice='';
      if(!hits.length){body='말씀하신 상황과 직접 연결되는 규정을 아직 특정하지 못했습니다. 어떤 비용이나 지원을 확인하려는지, 사용일·출장 목적·대상 등을 조금만 더 알려주세요.';notice='현재 정보만으로는 규정 근거가 부족해 담당 부서 확인이 필요합니다.'}
      else {const primary=hits[0].r;let facts=primary.facts.filter(f=>{const terms=(f.match(/[가-힣A-Za-z0-9]+/g)||[]);return terms.some(t=>normalized.includes(t.toLowerCase()))});if(!facts.length)facts=primary.facts.slice(0,2);facts=facts.slice(0,3);body=buildConversationalAnswer(normalized,hits,facts);if(hits.length>1)notice='질문에 여러 규정이 연결되었습니다. 아래 규정의 조건을 함께 확인해 주세요.';if(primary.name==='여비관리기준'&&(normalized.includes('토요일')||normalized.includes('주말')||normalized.includes('교육'))){notice='주말·교육이라는 사실만으로 지급 여부를 확정할 수 없습니다. 출장명령 또는 교육 참석 승인 여부와 실제 이동 구간을 확인해야 합니다.'}}
      const cards=hits.map(x=>`<div class="evidence-card"><div class="evidence-title">${x.r.name}</div><div class="meta">문서번호: ${x.r.code} · 시행일: ${x.r.date}</div>${x.r.docs.map(d=>`<span class="tag">${d}</span>`).join('')}</div>`).join('');
      chat.insertAdjacentHTML('beforeend',`<div class="message assistant"><div class="avatar">AI</div><div class="bubble">${escapeHtml(body)}${notice?`<div class="notice">⚠ ${escapeHtml(notice)}</div>`:''}<div class="evidence"><div class="meta">확인한 규정 근거</div>${cards||'<div class="evidence-card">검색된 근거 없음</div>'}</div></div></div>`);window.scrollTo({top:document.body.scrollHeight,behavior:'smooth'});send.disabled=!input.value.trim();
      */
    }
    function buildConversationalAnswer(q,hits,facts){
      const primary=hits[0].r;
      if(primary.name==='여비관리기준'&&(q.includes('기차')||q.includes('철도')||q.includes('교통')||q.includes('교육'))){
        return `질문하신 상황은 여비관리기준의 국내여비 항목을 먼저 확인해야 합니다.\n\n토요일에 이동했다는 이유만으로 기차비 지급 여부가 정해지지는 않습니다. 교육 참석이 회사의 출장 또는 교육 일정으로 승인되었고, 실제 이동 구간이 확인되면 교통비 지급 기준을 검토할 수 있습니다.\n\n현재 확인되는 관련 기준은 다음과 같습니다.\n${facts.map(x=>'• '+x).join('\n')}\n\n정확히 확인하려면 교육 참석 승인 여부, 출장명령 여부, 출발지·도착지, 이동일과 열차표 영수증이 필요합니다.`;
      }
      return `${primary.name} 기준을 확인해 보았습니다.\n\n${facts.map(x=>'• '+x).join('\n')}\n\n질문하신 상황에 정확히 적용하려면 대상·일자·소속·직급·금액 등 추가 정보가 필요할 수 있습니다. 해당 정보가 있으면 조건을 하나씩 대조해 드리겠습니다.`;
    }
    function escapeHtml(s){return s.replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}
    // 모델이 마크다운을 쓰는 것을 지시로 막는 대신 렌더링합니다. 회차마다 지켜지지 않아서입니다.
    // 반드시 이스케이프한 뒤에 정규식을 겁니다. 순서를 바꾸면 답변 안의 태그가 살아납니다.
    // 버블이 white-space:pre-wrap이라 줄바꿈이 유지되므로 목록은 <ul> 없이 가운뎃점으로 충분합니다.
    function renderAnswer(text){
      return escapeHtml(text)
        .replace(/^#{1,6}\s*(.+)$/gm,'<strong>$1</strong>')
        .replace(/\*\*(.+?)\*\*/g,'<strong>$1</strong>')
        .replace(/^\s*[-*]\s+/gm,'• ');
    }
    // 상담 저장소는 화면 로직과 분리해 두어 향후 로그인 기반 서버 저장소로 교체할 수 있게 합니다.
    function readConversations(){
      try{const stored=JSON.parse(localStorage.getItem(CHAT_STORAGE_KEY)||'[]');const cutoff=Date.now()-CHAT_RETENTION_DAYS*86400000;return Array.isArray(stored)?stored.filter(item=>new Date(item.updatedAt).getTime()>=cutoff&&Array.isArray(item.messages)).slice(0,CHAT_MAX_ITEMS):[]}
      catch(error){return []}
    }
    function writeConversations(items){try{localStorage.setItem(CHAT_STORAGE_KEY,JSON.stringify(items.slice(0,CHAT_MAX_ITEMS)))}catch(error){console.warn('최근 상담을 브라우저에 저장하지 못했습니다.')}}
    function saveCurrentConversation(){
      const firstQuestion=currentMessages.find(item=>item.role==='user')?.content;if(!firstQuestion)return;
      const items=readConversations().filter(item=>item.id!==currentConversationId);
      currentConversationId=currentConversationId||(crypto.randomUUID?.()||`${Date.now()}-${Math.random().toString(16).slice(2)}`);
      items.unshift({id:currentConversationId,title:firstQuestion.length>28?firstQuestion.slice(0,28)+'…':firstQuestion,updatedAt:new Date().toISOString(),messages:currentMessages});
      writeConversations(items);renderRecentChats();
    }
    function restoreConversation(id){
      const item=readConversations().find(entry=>entry.id===id);if(!item)return;
      currentConversationId=item.id;currentMessages=item.messages;conversation.splice(0);
      chat.innerHTML='';document.querySelector('#hero').classList.add('hidden');document.querySelector('#inquiry-history').classList.remove('visible');
      item.messages.forEach(message=>{if(message.role==='user')addUser(message.content,false);else addAnswer(message.content,message.evidence||[],message.uiActions||[],false);conversation.push({role:message.role,content:message.content})});
      renderRecentChats();closeMobileSidebar();window.scrollTo({top:document.body.scrollHeight,behavior:'smooth'});input.focus();
    }
    function deleteConversation(id){
      if(!confirm('이 상담 이력을 삭제할까요?'))return;writeConversations(readConversations().filter(item=>item.id!==id));if(currentConversationId===id)resetChat();else renderRecentChats();
    }
    function clearChatHistory(){
      if(!readConversations().length||!confirm('최근 상담 이력을 모두 삭제할까요?'))return;writeConversations([]);resetChat();
    }
    function renderRecentChats(){
      const list=document.querySelector('#recent-chat-list'),items=readConversations();
      list.innerHTML=items.length?items.map(item=>`<div class="recent-item${item.id===currentConversationId?' active':''}"><button class="recent-open" type="button" data-open-chat="${escapeHtml(item.id)}"><span class="recent-title">${escapeHtml(item.title)}</span><span class="recent-date">${formatRecentDate(item.updatedAt)}</span></button><button class="recent-delete" type="button" data-delete-chat="${escapeHtml(item.id)}" aria-label="${escapeHtml(item.title)} 삭제">×</button></div>`).join(''):'<div class="recent-empty">저장된 상담이 없습니다.</div>';
    }
    function formatRecentDate(value){const date=new Date(value),today=new Date();return date.toDateString()===today.toDateString()?new Intl.DateTimeFormat('ko-KR',{hour:'2-digit',minute:'2-digit'}).format(date):new Intl.DateTimeFormat('ko-KR',{month:'numeric',day:'numeric'}).format(date)}
    function openMobileSidebar(){document.querySelector('.sidebar').classList.add('open');document.querySelector('#sidebar-backdrop').classList.add('open')}
    function closeMobileSidebar(){document.querySelector('.sidebar').classList.remove('open');document.querySelector('#sidebar-backdrop').classList.remove('open')}
    chat.addEventListener('click',event=>{const clubButton=event.target.closest('.club-application-button');if(clubButton){openClubApplicationDraft();return}const button=event.target.closest('.inquiry-button');if(!button)return;openInquiry(JSON.parse(decodeURIComponent(button.dataset.inquiry)))});
    async function openClubApplicationDraft(){const response=await fetch('/api/club-applications/draft',{method:'POST',headers:{'Content-Type':'application/json'},body:'{}'});const draft=await response.json();if(!response.ok){alert(draft.error||'동호회 신청 메일 초안을 만들지 못했습니다.');return}openInquiry(draft)}
    async function openInquiry(data){
      const draft=data.body?data:await (await fetch('/api/inquiries/draft',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)})).json();
      document.body.insertAdjacentHTML('beforeend',`<div class="inquiry-panel" id="inquiry-panel"><div class="inquiry-card"><h2>담당자 문의</h2><div class="notice">현재 환경에서는 메일 발송 요청 내역만 저장됩니다.</div><label>수신 부서</label><input id="inquiry-recipient" value="${escapeHtml(draft.recipient)}"><label>메일 제목</label><input id="inquiry-subject" value="${escapeHtml(draft.subject)}"><label>메일 본문</label><textarea id="inquiry-body">${escapeHtml(draft.body)}</textarea><div class="inquiry-actions"><button class="secondary" onclick="closeInquiry()">취소</button><button class="secondary" onclick="saveInquiry()">임시저장</button><button class="inquiry-submit" onclick="sendInquiry()">발송</button></div></div></div>`);
    }
    function closeInquiry(){document.querySelector('#inquiry-panel')?.remove()}
    function inquiryPayload(){return {recipient:document.querySelector('#inquiry-recipient').value,subject:document.querySelector('#inquiry-subject').value,body:document.querySelector('#inquiry-body').value}}
    async function saveInquiry(){const response=await fetch('/api/inquiries/save',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(inquiryPayload())});const item=await response.json();if(!response.ok){alert(item.error||'임시 저장에 실패했습니다.');return}closeInquiry();alert('문의 내용이 임시 저장되었습니다.');loadInquiryHistory()}
    async function sendInquiry(){const response=await fetch('/api/inquiries/send',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(inquiryPayload())});const item=await response.json();if(!response.ok){alert(item.error||'메일 요청에 실패했습니다.');return}closeInquiry();alert(`메일 요청이 완료되었습니다.\n상태: ${item.status}`);loadInquiryHistory()}
    async function resumeInquiry(id){const data=await (await fetch('/api/inquiries')).json();const item=data.items.find(x=>x.id===id);if(item)openInquiry(item)}
    async function viewInquiry(id){const data=await (await fetch('/api/inquiries')).json();const item=data.items.find(x=>x.id===id);if(!item)return;document.body.insertAdjacentHTML('beforeend',`<div class="inquiry-panel" id="inquiry-panel"><div class="inquiry-card"><h2>문의 내용</h2><label>수신 부서</label><input value="${escapeHtml(item.recipient)}" readonly><label>메일 제목</label><input value="${escapeHtml(item.subject)}" readonly><label>문의 본문</label><textarea readonly>${escapeHtml(item.body)}</textarea><div class="inquiry-actions"><button class="secondary" onclick="closeInquiry()">닫기</button></div></div></div>`)}
    async function deleteInquiry(id){if(!confirm('문의 내역을 삭제할까요?'))return;await fetch('/api/inquiries/delete',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({id})});loadInquiryHistory()}
    async function loadInquiryHistory(){const response=await fetch('/api/inquiries');const data=await response.json();const list=document.querySelector('#inquiry-list');if(!data.items.length){list.textContent='아직 문의 내역이 없습니다.';return}list.innerHTML=data.items.slice().reverse().map(item=>`<div class="history-row"><strong>${escapeHtml(item.subject)}</strong><br><span class="meta">${escapeHtml(item.recipient)} · ${escapeHtml(item.sent_at||item.saved_at)} · </span><span class="history-status">${escapeHtml(item.status)}</span><br>${item.status==='작성 중'?`<button class="secondary" onclick="resumeInquiry(${item.id})">이어서 작성</button> `:''}<button class="secondary" onclick="viewInquiry(${item.id})">내용 보기</button> <button class="secondary" onclick="deleteInquiry(${item.id})">삭제</button></div>`).join('')}
    writeConversations(readConversations());
    renderRecentChats();
    loadInquiryHistory();
  </script>
</body>
</html>
````

## eval_retrieval.py

원본 경로: `eval_retrieval.py`

````python
"""검색 단계만 따로 재는 평가입니다.

답변 문자열이 아니라 "정답 조항이 후보 안에 들어왔는가"를 봅니다.
test_rules.py가 최종 답변을 고정한다면, 이 파일은 그 앞 단계인 retrieve()의 품질을 재서
BIGRAM_WEIGHT·VECTOR_WEIGHT·경로 임베딩 같은 노브를 의견이 아니라 숫자로 판단하게 합니다.

실행: SSL_CERT_FILE=$(python3 -m certifi) python3 eval_retrieval.py
      뒤에 -v를 붙이면 전 문항의 순위를 봅니다.

세트가 둘이다. 목적이 다르니 섞어 보지 말 것.
- 회귀 44문항: 손으로 만든 문항. 고친 것이 다시 깨지는지 본다. 라벨은 계층 경로.
  코퍼스를 보면서 만들었으므로 커버리지 지표로는 신뢰할 수 없다. 더 늘리지 말 것.
- 실사용 18문항: 실제 상담 로그(`규정/숙소지원금 이슈 샘플.txt`)에서 옮긴 문항. 라벨은 본문
  문자열이라 청킹을 바꿔도 유효하다. 다만 로그가 숙소지원금 영역에만 있어 여비·경조금·동호회
  커버리지는 여전히 없다.

기준선 (2026-09-10, 경로+본문 임베딩 / BIGRAM_WEIGHT 1.0 / VECTOR_WEIGHT 0.8):
    회귀   hit@1 63.6%  hit@3 81.8%  hit@5 90.9%  hit@10 93.2%  hit@20 100%  MRR 0.744
    실사용 hit@1 50.0%  hit@3 88.9%  hit@5 94.4%  hit@10  100%  hit@20 100%  MRR 0.681
직전 기준선(VECTOR_WEIGHT 0.5)은 hit@1 45.5% / MRR 0.639였습니다. 두 상수는 이 44문항으로
격자 스윕해 정했고, BIGRAM_WEIGHT는 0.5·1.5·2.0 모두 1.0보다 나빴습니다.
노브를 바꾼 뒤 이 숫자와 비교하세요. 질문 임베딩도 파일 캐시를 타므로 재실행은 무료입니다.

hit@20이 100%라는 건 판정기가 언제나 정답 조항을 보고 있다는 뜻입니다. 그래서 상위권
지표는 후보 상한(retrieve의 limit)을 줄이거나 코퍼스가 커질 때 비로소 병목이 됩니다.
"""

import sys

from app import load_env, load_policy_index, retrieve

load_env()


# (질문, 정답 조항 경로의 일부) — 하나라도 후보에 들어오면 적중으로 봅니다.
# 라벨은 청크 계층 경로의 부분문자열이며, 실행 시 실제 청크와 대조해 검증합니다.
CASES = [
    # --- 경조금 지급기준 ---
    # 결혼·회갑 답은 5.2의 지급표에 있다. 이 세 문항이 '5.2.1 출산장려금 적용 조건'을
    # 가리키고 있던 것은, 평가셋을 만들 당시 청킹이 5.2의 지급표를 5.2.1 청크에 흡수해
    # 경로가 그렇게 찍혔기 때문이다. 그 흡수를 고친 뒤 라벨만 옛 경로에 남아 있었다.
    ("동생이 결혼하는데 경조금 얼마 나와?", ["5.2 경조금 지급기준"]),
    ("형제 결혼 경조금 신청할 때 무슨 서류 내야 해?", ["5.3.1 경조사별 제출서류"]),
    ("조부모상인데 가족관계증명서는 누구 기준으로 떼야 하나요?", ["5.3.2 사망 관련 가족관계증명서 제출 기준"]),
    ("경조사가 넉 달 전인데 지금 경조금 신청해도 되나요?", ["5.6 유효기간"]),
    ("둘째 출산했는데 출산장려금 얼마예요?", ["5.2.1 출산장려금 적용 조건"]),
    ("장인어른 회갑인데 경조금 나오나요?", ["5.2 경조금 지급기준"]),
    ("경조휴가 중에 결근하면 경조금은 어떻게 되나요?", ["5.5 결근 및 환수"]),
    ("경조금은 어떤 절차로 지급되나요?", ["5.1 경조금 지급절차"]),
    # --- 숙소지원금 운영 기준 ---
    ("서울사무소로 부임하면 숙소지원금 월 얼마인가요?", ["5.1 지원내용"]),
    ("월세가 아니라 전세인데 숙소지원금은 어떻게 계산해요?", ["5.1 지원내용"]),
    ("9월 20일에 숙소지원금 신청하면 9월분부터 나오나요?", ["5.1 지원내용"]),
    ("3월 10일자로 발령났는데 숙소지원금 언제까지 신청해야 하나요?", ["5.1 지원내용"]),
    ("예전에 근무했던 지역으로 다시 부임하면 숙소지원금 얼마나 더 받나요?", ["5.1 지원내용"]),
    ("숙소지원금은 누가 받을 수 있나요?", ["5.2 지원 대상"]),
    ("숙소지원금 받다가 휴직을 8개월 하면 계속 지원되나요?", ["5.3 지원 제외 대상"]),
    ("파견이 6개월 넘어가면 숙소지원금은 어떻게 되나요?", ["5.3 지원 제외 대상"]),
    ("국내에서 숙소지원금 받다가 해외주재원으로 나가면요?", ["5.3 지원 제외 대상"]),
    ("본가에서 회사까지 직선거리 22km인데 숙소지원금 대상인가요?", ["5.4 지원기준"]),
    ("발령나서 옮기는데 전 근무지 숙소 정리 비용도 지원되나요?", ["5.5 지원특례"]),
    ("전세 대출 이자도 숙소지원금으로 지원받을 수 있나요?", ["5.5 지원특례"]),
    ("숙소지원금 처음 신청할 때 제출서류 알려주세요", ["5.6.1 최초 숙소지원금 신청 시"]),
    ("숙소지원금 1년 지나면 갱신 서류를 내야 하나요?", ["5.6.2 지급 갱신·지원 중지·지원 자격 확인 시", "5.8 기타사항"]),
    ("무주택자 기준이 어떻게 되나요?", ["3.3 무주택자"]),
    ("단신 근무자가 정확히 무슨 뜻인가요?", ["3.4 단신 근무자"]),
    ("부임비를 받았는데 숙소지원금은 언제부터 지급되나요?", ["5.8 기타사항"]),
    ("숙소 계약이 바뀌면 회사에 알려야 하나요?", ["5.7 보고의무"]),
    # --- 여비관리기준 / FAQ ---
    ("국내출장 갔을 때 택시 타도 정산되나요?", ["5.14.4 국내출장 시 택시 이용"]),
    ("자가차량으로 출장 갈 때 동승자가 있으면 어떻게 정산해요?", ["5.14.3 자가차량 동승자"]),
    ("부임비랑 이전비는 얼마씩 나오나요?", ["5.11 부임비 및 이전비", "별첨 2. 국내이전료 정액표"]),
    ("해외출장 항공권은 어느 등급까지 지원되나요?", ["5.17.1 지급기준", "별첨 4. 국외여비기준표"]),
    ("출장 중에 아파서 며칠 더 머물면 그 일수도 출장에 포함되나요?", ["5.1 여행일수 계산"]),
    ("파견 근무자 숙박료는 하루 얼마인가요?", ["5.15.1 일반 파견 근무자"]),
    ("해외출장 끝나고 개인 연차 붙여서 늦게 귀국하면 항공료 지원되나요?", ["해외출장 종료 후 개인 일정 체류"]),
    ("출장 다녀오면 복명은 언제까지 해야 하나요?", ["5.4 출장복명"]),
    # --- 제도 경계 문항 ---
    # retrieve()의 키워드 파일 필터가 오작동하는 조합입니다. 질문에 "결혼"·"숙소"·"전세"가
    # 있지만 답은 다른 규정에 있어, 필터가 정답 파일을 통째로 걷어냅니다.
    # FAQ 게이트도 "휴가"·"연차" 낱말이 없으면 FAQ 청크를 아예 후보에서 제외합니다.
    ("결혼하면서 이사하는데 이전비 나와요?", ["5.11 부임비 및 이전비", "별첨 2. 국내이전료 정액표"]),
    ("출장 가면 숙소는 회사가 잡아주나요?", ["5.10.2 소액경비 및 숙박비", "별첨 1. 국내여비기준표"]),
    ("숙소에서 출장지까지 가는 교통비도 정산되나요?", ["5.10.1 교통비", "별첨 1. 국내여비기준표"]),
    ("전세 계약한 집으로 옮기는데 이사비 지원되나요?", ["5.11 부임비 및 이전비", "별첨 2. 국내이전료 정액표"]),
    ("해외출장 끝나고 며칠 더 머물다 오면 항공료는 어떻게 되나요?", ["해외출장 종료 후 개인 일정 체류"]),
    ("동호회 회원끼리 결혼하면 축의금은 어디서 나오나요?", ["5.2 경조금 지급기준", "3.1 축의금"]),
    # --- 동호회 관리 규정 ---
    ("동호회를 새로 만들려면 어떻게 해야 하나요?", ["5.1 동호회 결성"]),
    ("동호회 활동보조금은 분기에 얼마까지 나오나요?", ["7.1 동호회 활동보조금 지급 시기"]),
    ("동호회를 해체하려면 무슨 절차가 필요한가요?", ["5.6 동호회의 해체"]),
    ("향우회도 동호회로 등록해서 지원받을 수 있나요?", ["5.3 동호인 등록 제한"]),
]

# --- 실사용 세트 ---
# 위 CASES는 코퍼스를 보면서 만든 문항이라 커버리지 지표로는 신뢰할 수 없습니다. hit@20이 100%로
# 나오지만 "국내출장 숙박비 한도" 같은 평범한 질문 하나가 미검색으로 떨어집니다.
# 아래는 `규정/숙소지원금 이슈 샘플.txt`의 실제 상담 30여 건 중 규정으로 판정 가능한 것을
# 직원 말투로 옮긴 것입니다. 상황을 제가 고르지 않았다는 점이 이 세트의 값어치입니다.
#
# 라벨이 경로가 아니라 **본문 문자열**입니다. 경로 라벨은 청킹을 바꾸면 통째로 흔들려서
# 청킹 실험을 아예 못 잽니다. 본문 문자열은 조항이 어느 청크에 들어가든 따라갑니다.
REAL_CASES = [
    ("파견이 잦아 숙소 계약을 연장하지 못했는데, 나중에 다시 계약하면 재신청할 수 있나요? 못 받은 기간도 지원기간에 들어가나요?",
     ["전체 지원기간에 포함한다"]),
    ("4월 3일에 숙소 계약을 했는데 발령은 4월 20일입니다. 4월 월세도 지원되나요?",
     ["매월 15일을 기준으로", "완료된 달로부터 기산"]),
    ("예전에 근무했던 지역으로 다시 발령났습니다. 숙소지원금을 얼마나 더 받을 수 있나요?",
     ["재부임할 경우 발령일로부터 1년간"]),
    ("포항에서 2개월, 구미에서 3년 조금 넘게 받았고 이번에 포항으로 재부임합니다. 3년까지 받을 수 있나요?",
     ["합이 3년 이하일 경우에는 3년까지"]),
    ("숙소지원금을 받다가 베트남 주재원으로 나가게 됐습니다. 언제까지 지급되나요?",
     ["해외주재원 부임 대상자는 부임일이 속한 월의 다음 달부터"]),
    ("자기개발 휴직을 반년 넘게 쓸 예정인데 숙소지원금은 언제부터 끊기나요?",
     ["휴직 기간이 연속해서 6개월을 초과"]),
    ("육아휴직 6개월 쓰고 복직했다가 다시 6개월 휴직하려는데 계속 지원받을 수 있나요?",
     ["휴직 기간이 연속해서 6개월을 초과"]),
    ("작년에 연장 신청을 못 해서 올해 미지급 중입니다. 다시 신청하려면 어떤 서류가 필요한가요?",
     ["월세 이체내역"]),
    ("결혼했고 배우자가 인천에 살고 있습니다. 저는 세종에서 근무하는데 지원 대상인가요?",
     ["기혼자는 배우자 주소지", "반경 25km를 초과하는 지역에 거주"]),
    ("6월 15일에 발령이 나는데 부임비는 6월에 적용되나요, 7월인가요?",
     ["매월 15일을 기준으로"]),
    ("세금 문제로 가족을 두 달만 숙소 주소지로 전입시키려 합니다. 지원에 문제가 되나요?",
     ["무주택 단신부임", "실제 거주 여부 확인"]),
    ("본가가 경주 안강이고 근무지는 포항입니다. 직선거리로 22km인데 숙소지원금 받을 수 있나요?",
     ["반경 25km를 초과하는 지역에 거주"]),
    ("이번에 경력으로 입사했고 주재지는 세종, 본가는 경남입니다. 신규 입사자도 대상인가요?",
     ["신규 채용자(신입, 경력, 계약직, 임시직 포함)"]),
    ("그룹사에서 인력교류로 넘어와 파견비를 2년 받았습니다. 파견비가 끝나면 숙소지원금을 받을 수 있나요?",
     ["그룹사 인력교류자", "파견비 지급 대상자"]),
    ("광양으로 발령났는데 포항 숙소를 아직 정리하지 못했습니다. 그동안 월세는 어떻게 되나요?",
     ["전(前) 근무지 숙소를 정리하는 기간"]),
    ("구미 숙소가 경매로 넘어가서 3개월 안에 정리를 못 했습니다. 연장이 되나요?",
     ["처분 노력을 입증할 자료"]),
    ("작년 7월에 서울사무소로 발령났는데 이제야 신청하려 합니다. 지원 대상이 되나요?",
     ["6개월 이내에 신청하지 않으면 지원자격을 상실"]),
    ("해외파견으로 2년간 지원을 못 받았습니다. 복귀하면 그 2년만큼 뒤로 밀리나요?",
     ["전체 지원기간에 포함한다"]),
]

CUTOFFS = (1, 3, 5, 10, 20)


def validate_labels(cases, field):
    """라벨이 실제 청크와 맞는지 확인해 오타로 인한 가짜 실패를 막습니다."""
    haystack = [chunk[field] for chunk in load_policy_index()[0]]
    unknown = []
    for question, golds in cases:
        for gold in golds:
            if not any(gold in item for item in haystack):
                unknown.append((question, gold))
    if unknown:
        print(f"청크 {field}와 맞지 않는 라벨이 있습니다. 규정이 바뀌었는지 확인하세요.")
        for question, gold in unknown:
            print(f"  - {gold!r} ← {question}")
        raise SystemExit(2)


def first_gold_rank(question, golds, field):
    """정답 조항이 검색 결과 몇 번째에 나오는지 반환합니다. 없으면 None."""
    for rank, item in enumerate(retrieve(question), 1):
        if any(gold in item.get(field, "") for gold in golds):
            return rank
    return None


def run_set(label, cases, field, verbose):
    """한 세트를 측정해 hit@k와 MRR을 출력합니다."""
    validate_labels(cases, field)
    print(f"[{label}] {len(cases)}문항 · 라벨 기준 {field}")
    ranks = []
    for question, golds in cases:
        rank = first_gold_rank(question, golds, field)
        ranks.append(rank)
        if verbose or rank is None or rank > 5:
            position = f"{rank}위" if rank else "미검색"
            print(f"{position:>6}  {question}")
            if rank is None:
                print(f"        기대 조항: {' | '.join(golds)}")

    total = len(ranks)
    found = [rank for rank in ranks if rank]
    print()
    for cutoff in CUTOFFS:
        hits = sum(1 for rank in found if rank <= cutoff)
        print(f"hit@{cutoff:<2} {hits / total:6.1%}  ({hits}/{total})")
    print(f"MRR    {sum(1 / rank for rank in found) / total:6.3f}")
    print(f"미검색 {total - len(found)}건")
    print()


def main():
    verbose = "-v" in sys.argv
    # 회귀 세트는 경로 라벨, 실사용 세트는 본문 라벨을 쓴다. 뒤쪽은 청킹을 바꿔도 유효하다.
    run_set("회귀", CASES, "path", verbose)
    run_set("실사용", REAL_CASES, "text", verbose)


if __name__ == "__main__":
    main()
````

## test_rules.py

원본 경로: `test_rules.py`

````python
"""복리후생 상담 규칙의 사용자 관점 동작을 고정하는 회귀 테스트입니다."""

from app import (
    CONSULTATION_GRAPH,
    attach_referenced_chunks,
    load_env,
    resolve_question,
    retrieve_policy_node,
    select_used_evidence,
)

# 키를 읽지 않으면 LLM 경로 사례가 전부 담당 부서 이관으로 떨어져 검증이 되지 않습니다.
load_env()


# (질문, 답변에 있어야 할 문자열, 답변에 없어야 할 문자열[, 이전 대화 이력])
# 이력은 이전 주제가 새 질문을 덮어쓰는지 보는 사례에만 붙입니다.
PROTECTED_CASES = [
    # 경조금 지급기준 5.2·5.3: 본인 형제·자매 결혼은 20만원과 부모 기준 증빙을 안내한다.
    (
        "제 동생이 결혼하는데 경조금과 필요한 서류 알려줘",
        "지원금: 200,000원",
        "배우자 부모 기준 가족관계증명서",
    ),
    # 경조금 지급기준 5.2·5.3: 배우자 형제·자매 결혼에는 배우자 부모 기준 증빙이 필요하다.
    (
        "배우자의 여동생이 결혼하는데 필요한 서류 알려줘",
        "배우자 부모 기준 가족관계증명서",
        "관계: 본인 형제·자매",
    ),
    # 경조금 지급기준 5.2: 승중상 인정 조건이 없으면 금액을 확정하지 않고 확인사항을 안내한다.
    # 되묻기 항목은 판정이 그때그때 문장으로 만든다("장손자 여부" / "본인이 장손자인지
    # 여부"). 회차마다 흔들리지 않는 '장손자'로만 조건을 물었는지 확인하고, 금액을
    # 확정하지 않는지는 금지 문자열이 맡는다.
    (
        "승중상 경조금 문의",
        "장손자",
        "- 지원금: 500,000원",
    ),
    # 경조금 지급기준 5.2: 본인 부모 사망 경조금은 100만원이다.
    (
        "우리 아버지가 돌아가셨는데 경조금 얼마야?",
        "- 지원금: 1,000,000원",
        "- 지원금: 300,000원",
    ),
    # 경조금 지급기준 5.2: 본인 및 배우자 조부모 사망 경조금은 30만원이다.
    (
        "본인 조부모님이 돌아가셨는데 경조금 얼마야?",
        "- 지원금: 300,000원",
        "- 지원금: 1,000,000원",
    ),
    # 경조금 지급기준 5.2: 부친 사망·장손자·상주 조건이 확인된 승중상은 50만원이다.
    (
        "아버지가 이미 사망했고 제가 장손으로 상주를 맡아 조부모상을 치르는 승중상입니다. 경조금은?",
        "- 지원금: 500,000원",
        "승중상 인정 조건 확인 필요",
    ),
    # 경조금 지급기준 5.2·5.3: 지급표에 없는 고모 관계는 금액을 단정하지 않는다.
    # '노사발전그룹'은 삭제된 build_death_answer의 문구였다. 규정이 요구하는 것은
    # 금액 단정 금지(금지 문자열)이므로, 필수 쪽은 이관 판정이면 표현을 가리지 않는다.
    (
        "고모가 돌아가셨는데 경조금 받을 수 있어?",
        ("노사발전그룹", "주관 부서 확인 필요"),
        "- 지원금:",
    ),
    # 경조금 지급기준 5.2·5.6: 부모 환갑은 생년월일 확인 후 3개월 기한을 판단한다.
    (
        "부모님 환갑 경조금 얼마야?",
        "부모님의 생년월일",
        "- 판정: 신청 가능",
    ),
    # 숙소지원금 운영 기준 5.5: 기존 숙소 정리는 기본 3개월, 최장 6개월까지 지원한다.
    # 고정 문구 규칙을 지운 뒤로는 "최장"과 "최대"가 번갈아 나오므로 기간만 확인한다.
    (
        "숙소지원금 2년 받다가 포항으로 발령났는데 기존 숙소 정리 비용은?",
        "6개월",
        "전 근무지 정리 비용은 지원 대상이 아닙니다",
    ),
    # 숙소지원금 운영 기준 5.1-6: 재부임은 1년이 원칙이며 기존 수급 합이 3년 이하일 때만 3년까지다.
    # 삭제한 build_housing_move_answer가 재부임을 모르고 신규 부임 기준을 그대로 단정하던 사례다.
    (
        "예전에 포항에서 2년 숙소지원금 받았는데 이번에 다시 포항으로 재부임했어요. 얼마나 더 받을 수 있나요?",
        "1년",
        "발령일로부터 3년간",
    ),
    # 숙소지원금 운영 기준 5.1-4: 매월 15일 이후 신청자는 다음 달부터 지급한다.
    # 생성 답변은 "다음 달"과 "10월"을 번갈아 쓴다. 낱말이 아니라 어느 달부터인지가 판정이다.
    (
        "9월 20일에 숙소지원금 신청하면 9월분부터 나오나요?",
        ("10월", "다음 달"),
        "9월분부터 지급",
    ),
    # 숙소지원금 운영 기준 5.5: 월세에서 전세 전환 시 전세금 1천만원당 월 10만원 기준을 쓴다.
    # 규정 51행의 표기는 "전세금 1,000만원당 10만원/월"이다. 옛 기대값
    # '전세금 10,000,000원당 월 100,000원'은 삭제된 고정 문구 쪽 표기였다.
    (
        "숙소지원금 받는데 월세에서 전세로 바꾸면 어떻게 돼?",
        ("전세금 1,000만원당", "전세금 10,000,000원당"),
        "관리비·공과금은 제외",
    ),
    # 여비관리기준 5.11과 숙소 기준: 실제 이사가 없으면 부임비·이전비를 지급하지 않는다.
    # 규정 97행이 "주거 이전을 하는 부임자에게" 지급한다고 정한다. 옛 기대값
    # '이사하지 않으면 지급 없음'은 삭제된 build_relocation_answer의 문구였다.
    (
        "광양에서 포항으로 발령났는데 이사는 하지 않고 혼자 살 예정이야. 부임비 나와?",
        ("지급되지 않습니다", "지급 없음", "지급 불가"),
        "부임비·이전비: 지급 대상",
    ),
    # 여비관리 FAQ: 해외출장 후 개인 연차로 늦춘 귀국 항공편은 회사가 지원하지 않는다.
    (
        "해외출장 후 개인 연차를 쓰고 늦게 귀국하면 변경한 항공편도 지원돼?",
        "회사에서 지원하지 않습니다",
        "판정: 지원 대상",
    ),
    # 주말 복귀 출장: 승인 일정인지 개인 사유인지에 따라 갈리므로 지급을 단정하지 않는다.
    # A-14와 같이 답변 형태(이관·되묻기·조건부 안내)는 고정하지 않고 오지급 단정만 막는다.
    (
        "국내출장을 마치고 개인 사정으로 일요일에 복귀하면 교통비가 지급돼?",
        "",
        "지급 대상입니다",
    ),
    # 개인 사유 주말 선이동: 개인 일정으로 추가된 교통비에 지원 근거를 만들지 않는다.
    # 고정 문구 규칙을 지운 뒤로는 담당 부서 이관과 되묻기 사이에서 답변 형태가 갈린다.
    # 둘 다 규정에 맞는 처리이므로 형태를 고정하지 않고, 잘못 지급을 단정하지 않는지만 확인한다.
    (
        "개인 사유로 출장 전에 주말에 미리 이동하면 추가 교통비가 지급돼?",
        "",
        "지급 대상입니다",
    ),
    # 동호회 관리 규정 5.1: 개설 문의에는 최소 등록 인원 5명과 사전 협의 절차를 안내한다.
    # 고정 문구를 반환하던 규칙을 지웠으므로, 규칙의 표현이 아니라 규정 원문의 내용을 확인한다.
    (
        "동호회 개설을 하고 싶은데 방법은?",
        "5명",
        "동호회 지원은 노사발전그룹의 등록·활동 실적 확인 후 지급됩니다",
    ),
    # 숙소지원금 운영 기준 5.4: 반경 25km 이내 거주는 원칙적 대상이 아니며 예외 검토 대상이다.
    # "숙소지원금 받을 수 있나요"를 기존 수급으로 오인해 전 근무지 정리 규칙이 켜지던 사례다.
    (
        "본가가 경주 안강인데 포항양극재로 발령났어요. 직선거리 22km인데 숙소지원금 받을 수 있나요?",
        "25km",
        "기존 숙소지원금 수급 중",
    ),
    # 사용자 제공 운영 예외: 혼인신고 전 결혼예정자가 주소만 전입했더라도 실제 동거하지
    # 않으면 단신 근무자 지위를 상실한 것으로 보지 않는다. 주소 등록을 동거로 추정하거나
    # 자격 상실 전제의 통보·윤리위반 경고를 붙이지 않는지도 함께 확인한다.
    (
        "혼인신고는 안 했고, 아직 동거는 안 해요.",
        ("실제 동거", "함께 거주", "함께 살"),
        ("지원 대상이 아닙니다", "지원 불가", "윤리위반", "전입신고 사실을 14일 이내"),
        [
            {
                "role": "user",
                "content": "숙소지원금을 받고 있는데 결혼예정자가 현재 제가 사는 원룸으로 전입신고를 했어요. 숙소지원금에서 제외되나요?",
            },
            {"role": "assistant", "content": "혼인신고 여부와 실제 동거 여부를 알려주세요."},
        ],
    ),
    # 실제 동거를 시작했다면 단신 근무자 지위 상실 가능성과 14일 이내 통보를 안내한다.
    (
        "숙소지원금을 받고 있는데 혼인신고 전 결혼예정자와 실제 동거를 시작했어요.",
        "14일 이내",
        "자동 제외하지 않음",
    ),
    # 주소 전입만 말하고 동거 여부를 밝히지 않았다면 지원 불가로 단정하지 않고 확인한다.
    (
        "숙소지원금을 받고 있는데 결혼예정자가 제 원룸으로 전입신고했어요. 제외되나요?",
        ("실제 동거", "함께 거주", "함께 살"),
        ("지원 대상이 아닙니다", "지원 불가", "윤리위반"),
    ),
    # 경조금 지급기준 5.2: 앞선 환갑 대화가 남아 있어도 새 부모상 문의는 사망 기준으로 답한다.
    # 삭제한 build_hoegap_answer가 "부모님"이라는 낱말만 보고 이전 회갑 주제를 이어받아,
    # 100만원 대상자에게 환갑 20만원 안내를 반환하며 그래프를 끝내던 사례다.
    (
        "부모님 상당했는데 지원 가능한가요",
        "1,000,000원",
        "환갑",
        [
            {"role": "user", "content": "부모님 환갑 경조금 얼마야?"},
            {"role": "assistant", "content": "환갑(회갑) 경조금 기준은 만 60세가 되는 경우이며, 지원금은 200,000원입니다."},
        ],
    ),
]


# 아직 미해결 — 리팩터링 후 통과해야 함
KNOWN_FAILURE_CASES = [
    # 사망 오탐: '이상·증상'의 '상'과 질문의 '본인'이 본인 사망으로 잘못 결합된다.
    (
        "출장 갔다가 이상 증상이 생기면 본인이 부담하나요?",
        "",
        "5,000,000원",
    ),
    # 결혼 오탐: '유형' 안의 '형'이 본인 형제·자매 관계로 잘못 인식된다.
    # 유형을 나열하며 형제·자매를 언급하는 것은 정상이므로, 본인 사안을 형제·자매로
    # 단정하는 판정 줄만 금지한다.
    (
        "결혼 준비 중인데 유형별로 어떤 지원이 있는지 알려줘",
        "",
        "관계: 본인 형제·자매",
    ),
    # 부임 오탐: '이사회' 안의 '이사'가 실제 이사를 동반한 부임 질문으로 잘못 인식된다.
    (
        "이사회 참석하러 서울 가는데 출장비 되나요?",
        "",
        "부임비",
    ),
    # 숙소 오탐: 자녀 동거 표현만으로 숙소지원금 부정 수급 상황을 단정한다.
    (
        "자녀와 함께 살 예정인데 부임비 나오나요?",
        "",
        "윤리위반",
    ),
]


# 후속 질문이 이전 대화의 사실을 이어받는지는 최종 답변 문구로 재면 회차마다 흔들린다.
# 재작성된 질문 자체를 확인한다. 낱말 목록으로 주제 전환을 판별하던 때는 "해외출장입니다"의
# '출장'이 걸려 이력이 통째로 끊겼고, 앞선 일정·경로가 답변에 하나도 반영되지 않았다.
TRIP_HISTORY = [
    {
        "role": "user",
        "content": (
            "제 본가가 수도권인 관계로, 9/11(금)에 미리 수도권으로 이동하여 주말을 보낸 뒤 "
            "9/14(월)에 공항으로 이동하고자 합니다. 상세 일정: 9/11 포항 자택-포항역 택시, "
            "9/11 포항역-광명역 KTX, 9/14 서울-인천공항 공항버스. "
            "개인 신용카드로 결제한 후 출장비로 정산하면 되는지 확인 부탁드립니다."
        ),
    },
    {
        "role": "assistant",
        "content": (
            "9/11 개인 사유로 미리 이동해 주말을 보낸 비용은 출장비로 정산하기 어렵고, "
            "9/14 공항 이동 비용도 해외출장 관련 국내 이동인지 확인이 필요합니다."
        ),
    },
]

# (질문, 이력, 재작성 결과에 있어야 할 문자열들, 없어야 할 문자열들)
CONTINUITY_CASES = [
    ("해외출장입니다", TRIP_HISTORY, ["인천공항", "9/14"], []),
    # 반대 방향도 막는다. 무관한 새 질문에 지난 주제를 끌어오면 안 된다.
    ("동호회 개설하려면?", TRIP_HISTORY, ["동호회"], ["인천공항", "포항역"]),
]


# 검색과 규칙 답변 단계를 지난 뒤에도 현재 제도와 무관한 규정 링크가 남지 않아야 합니다.
EVIDENCE_CASES = [
    (
        "해외출장 전일 이동으로 보면 됩니다",
        (
            "9월 11일부터 9월 14일까지 수도권에 체류한 뒤 9월 14일 인천공항으로 이동하는 "
            "일정을 해외출장 전일 이동으로 볼 때, 숙소지원금 운영 기준과 해외출장 비용을 어떻게 적용하나요?"
        ),
        "trip",
        {"여비관리기준.md"},
        {"여비관리 FAQ.md", "숙소지원금 운영 기준.md", "경조금 지급기준.md"},
    ),
    (
        "해외출장 종료 후 개인 휴가로 이틀 더 머물렀는데 귀국 항공권 변경 비용도 회사가 부담하나요?",
        "해외출장 종료 후 개인 휴가로 이틀 더 머문 뒤 귀국 항공권 변경 비용의 회사 부담 여부",
        "trip",
        {"여비관리기준.md"},
        {"여비관리 FAQ.md", "숙소지원금 운영 기준.md", "경조금 지급기준.md"},
    ),
    (
        "조의금 신청 시 가족관계증명서를 즉시 제출하기 어려워도, 사실관계를 증명할 수 있는 다른 서류가 있으면 대체 인정될 수 있나요?",
        "조의금 신청 시 가족관계증명서를 대체할 수 있는 증빙서류",
        "ceremony",
        {"경조금 지급기준.md"},
        {"여비관리 FAQ.md", "숙소지원금 운영 기준.md", "동호회 관리 규정.md"},
    ),
]


CLUB_SCOPE_CASES = [
    ("낚시 동호회 개설 가능한가요?", "낚시"),
    ("헬스 동아리 만들어도 되나요?", "헬스"),
]


# 사용자 화면의 "확인한 규정"은 검색 후보 전체가 아니라 답변에 실제 사용한 근거만 보여야 합니다.
DISPLAY_EVIDENCE_CASES = [
    (
        "출장지에서 유료 주차장을 이용했는데 지원됩니까?",
        {"여비관리기준.md"},
        {"숙소지원금 운영 기준.md", "동호회 관리 규정.md", "사내 추가 기준.md"},
    ),
    (
        "광양에서 포항으로 부임하면서 실제 이사합니다. 부임비와 기존 숙소 정리 기간의 숙소지원금은 각각 어떻게 지원되나요?",
        {"여비관리기준.md", "숙소지원금 운영 기준.md"},
        {"경조금 지급기준.md", "동호회 관리 규정.md", "여비관리 FAQ.md"},
    ),
]


def check_continuity(question, history, required_texts, forbidden_texts):
    """후속 입력이 자립형 질문으로 다시 쓰이는지 확인합니다."""
    resolved = resolve_question(question, history)
    assert resolved, "재작성 결과가 비어 있습니다."
    for text in required_texts:
        assert text in resolved, f"이전 대화의 사실이 빠졌습니다: {text!r} / 결과: {resolved!r}"
    for text in forbidden_texts:
        assert text not in resolved, f"무관한 주제가 딸려왔습니다: {text!r} / 결과: {resolved!r}"


def check_evidence(question, resolved, intent, expected_files, forbidden_files):
    """검색 뒤에 현재 제도와 무관한 규정 링크가 남지 않는지 확인합니다.

    원래는 `apply_policy_rules_node`를 함께 태워 규칙이 덮어쓴 근거까지 봤습니다. 그 노드는
    규정 본문을 파이썬 문자열로 들고 있어 삭제했고, 규칙이 근거를 덮어쓰는 경로 자체가
    없어졌습니다. 검증하려던 것(무관한 파일이 근거에 남지 않는다)은 검색 결과로 그대로 봅니다.
    intent는 규칙을 켜는 데만 쓰였으므로 더 이상 필요하지 않습니다.
    """
    evidence = retrieve_policy_node({"question": question, "resolved": resolved})["candidate_evidence"]
    files = {item["file"] for item in evidence}
    assert files == expected_files, f"근거 파일이 다릅니다: {sorted(files)!r}"
    assert not files & forbidden_files, f"무관한 근거 파일이 포함됐습니다: {sorted(files & forbidden_files)!r}"


def check_club_scope(question, required_term):
    """동호회·동아리 질문이 분야별 운영 기준이 담긴 근거를 찾는지 확인합니다."""
    result = retrieve_policy_node({"question": question, "resolved": question})
    evidence = result["candidate_evidence"]
    files = {item["file"] for item in evidence}
    assert files == {"동호회 관리 규정.md"}, f"근거 파일이 다릅니다: {sorted(files)!r}"
    assert any(required_term in item["text"] for item in evidence), (
        f"분야별 운영 기준에 {required_term!r}가 포함되지 않았습니다."
    )


def check_display_evidence(question, expected_files, forbidden_files):
    """전체 상담 뒤 사용자 화면으로 반환되는 근거 파일만 확인합니다."""
    result = CONSULTATION_GRAPH.invoke({"question": question, "history": []})
    files = {item["file"] for item in result.get("used_evidence", [])}
    assert files == expected_files, f"표시 근거 파일이 다릅니다: {sorted(files)!r}"
    assert not files & forbidden_files, f"무관한 표시 근거가 포함됐습니다: {sorted(files & forbidden_files)!r}"


def check_case(question, required_text, forbidden_text, history=()):
    """전체 상담 그래프에서 필수·금지 문구를 확인합니다."""
    result = CONSULTATION_GRAPH.invoke({"question": question, "history": list(history)})
    answer = result.get("answer", "")
    assert answer, "답변이 비어 있습니다."
    if required_text:
        # 같은 판정을 다른 낱말로 쓰는 경우가 있어(다음 달/10월, 최장/최대) 대안을 튜플로 받습니다.
        options = required_text if isinstance(required_text, tuple) else (required_text,)
        assert any(option in answer for option in options), f"필수 문자열이 없습니다: {options!r}"
    if forbidden_text:
        forbidden_items = forbidden_text if isinstance(forbidden_text, (tuple, list)) else (forbidden_text,)
        for item in forbidden_items:
            assert item not in answer, f"금지 문자열이 포함됐습니다: {item!r}"


def run_cases(group, cases, checker=check_case):
    """한 그룹의 모든 사례를 실행하고 개별 결과와 합계를 반환합니다."""
    passed = failed = skipped = 0
    for index, case in enumerate(cases, 1):
        question = case[0]
        case_id = f"{group}-{index:02d}"
        try:
            checker(*case)
        except RuntimeError as error:
            if "OPENAI_API_KEY" in str(error):
                skipped += 1
                print(f"건너뜀 [{case_id}] {question}")
                print("  이유: API 키가 없어 LLM 답변이 필요한 사례를 실행할 수 없습니다.")
                continue
            failed += 1
            print(f"실패 [{case_id}] {question}")
            print(f"  이유: {error}")
        except AssertionError as error:
            failed += 1
            print(f"실패 [{case_id}] {question}")
            print(f"  이유: {error}")
        except Exception as error:
            failed += 1
            print(f"실패 [{case_id}] {question}")
            print(f"  이유: {type(error).__name__}: {error}")
        else:
            passed += 1
            print(f"통과 [{case_id}] {question}")
    return passed, failed, skipped


def check_reference_expansion():
    """참조 확장은 순수 함수라 API 없이 확인합니다. LLM 회차 흔들림과 무관하게 깨집니다."""
    chunks = [
        {"file": "여비관리기준.md", "path": "여비관리기준 > 7. 관련문서 > 별첨 1. 국내여비기준표", "text": "숙박비 100,000원"},
        {"file": "여비관리기준.md", "path": "여비관리기준 > 7. 관련문서 > 별첨 2. 국내이전료 정액표", "text": "이전료"},
        {"file": "경조금 지급기준.md", "path": "경조금 지급기준 > 별첨 1. 다른 문서의 같은 번호", "text": "끌려오면 안 됨"},
    ]
    pointing = [{"file": "여비관리기준.md", "path": "… > 5.10.2 소액경비 및 숙박비",
                 "text": "소액경비 및 숙박료 지급기준은 별첨 1에 의한다.", "score": 0.9}]
    added = attach_referenced_chunks(list(pointing), chunks)[1:]
    assert [item["path"] for item in added] == [chunks[0]["path"]], f"별첨 1만 붙어야 합니다: {added}"

    # 별첨 목록표처럼 번호를 늘어놓기만 하는 청크는 따라가지 않습니다.
    listing = [{"file": "여비관리기준.md", "path": "… > 6. 기록 및 첨부",
                "text": "별첨 1, 별첨 2, 별첨 3 보존연한", "score": 0.9}]
    assert attach_referenced_chunks(list(listing), chunks) == listing, "나열 청크는 따라가면 안 됩니다."

    # 이미 근거에 있는 청크는 중복해서 붙이지 않습니다.
    already = list(pointing) + [dict(chunks[0], score=0.5)]
    assert attach_referenced_chunks(already, chunks) == already, "중복 첨부가 발생했습니다."
    print("통과 [D-01] 참조 확장(별첨 지목·나열 제외·중복 방지)")


def check_used_evidence_selection():
    """판정기 번호 검증과 최고 관련 파일 fallback을 API 없이 확인합니다."""
    candidates = [
        {"file": "여비관리기준.md", "path": "5.14.6", "score": 0.9, "keyword_score": 10.0, "text": "회사 차량 주차비"},
        {"file": "여비관리기준.md", "path": "5.14.5", "score": 0.8, "keyword_score": 9.0, "text": "렌터카 주차료"},
        {"file": "숙소지원금 운영 기준.md", "path": "5.4", "score": 0.7, "keyword_score": 9.5, "text": "지원 기준"},
        {"file": "여비관리 FAQ.md", "path": "해외출장", "score": 0.6, "keyword_score": 7.0, "text": "개인 일정 체류"},
    ]
    selected = select_used_evidence(
        "출장지에서 유료 주차장을 이용했는데 지원됩니까?",
        candidates,
        {"intent": "trip", "evidence_ids": [2, 2, 3, 4, 99, "1", True]},
    )
    assert selected == candidates[:2], f"선택된 규정 파일의 후보 청크만 남아야 합니다: {selected!r}"
    fallback = select_used_evidence(
        "출장비 지급 여부",
        candidates,
        {"intent": "trip", "evidence_ids": "1,2"},
    )
    assert fallback == candidates[:2], f"번호가 잘못되면 최고 관련 파일만 남아야 합니다: {fallback!r}"
    print("통과 [F-00] 실제 사용 근거 번호 검증·fallback")


def main():
    """보호 동작과 알려진 오답을 실행하고 전체 결과를 요약합니다."""
    check_reference_expansion()
    check_used_evidence_selection()
    protected = run_cases("A", PROTECTED_CASES)
    unresolved = run_cases("B", KNOWN_FAILURE_CASES)
    continuity = run_cases("C", CONTINUITY_CASES, check_continuity)
    evidence = run_cases("D", EVIDENCE_CASES, check_evidence)
    club_scope = run_cases("E", CLUB_SCOPE_CASES, check_club_scope)
    display_evidence = run_cases("F", DISPLAY_EVIDENCE_CASES, check_display_evidence)
    total = tuple(sum(values) for values in zip(protected, unresolved, continuity, evidence, club_scope, display_evidence))

    print()
    print(f"(A) 지켜야 할 동작: 통과 {protected[0]} / 실패 {protected[1]} / 건너뜀 {protected[2]}")
    print(f"(B) 아직 미해결: 통과 {unresolved[0]} / 실패 {unresolved[1]} / 건너뜀 {unresolved[2]}")
    print(f"(C) 대화 연속성: 통과 {continuity[0]} / 실패 {continuity[1]} / 건너뜀 {continuity[2]}")
    print(f"(D) 근거 링크 적합성: 통과 {evidence[0]} / 실패 {evidence[1]} / 건너뜀 {evidence[2]}")
    print(f"(E) 동호회 분야 기준 검색: 통과 {club_scope[0]} / 실패 {club_scope[1]} / 건너뜀 {club_scope[2]}")
    print(f"(F) 화면 표시 근거: 통과 {display_evidence[0]} / 실패 {display_evidence[1]} / 건너뜀 {display_evidence[2]}")
    print(f"전체: 통과 {total[0]} / 실패 {total[1]} / 건너뜀 {total[2]}")
    raise SystemExit(1 if total[1] else 0)


if __name__ == "__main__":
    main()
````

## 규정/경조금 지급기준.md

원본 경로: `규정/경조금 지급기준.md`

````markdown
# 경조금 지급기준

## 1. 목적

이 기준은 임·직원의 경·조사 시 회사에서 지급하는 경조금에 관한 사항을 정함을 목적으로 한다.

## 2. 적용범위

이 기준은 임·직원 모두에게 적용한다. 다만, 일용직은 제외한다.

## 3. 용어의 정의

### 3.1 축의금

다음의 경우를 축의금 지급 대상으로 정의한다.

- 본인 결혼
- 자녀 결혼
- 본인·배우자의 형제자매 결혼
- 본인 부모 회갑
- 배우자 부모 회갑
- 출산장려금

### 3.2 조의금

다음의 경우를 조의금 지급 대상으로 정의한다.

- 본인 사망
- 배우자 사망
- 본인·배우자의 부모 사망
- 승중상
- 본인·배우자의 조부모 사망
- 본인 외조부모 사망
- 자녀 사망
- 본인 형제·자매 사망
- 배우자 형제·자매 사망

## 4. 책임과 권한

해당 부서장은 경조금 지급을 승인한다.

## 5. 업무절차

### 5.1 경조금 지급절차

경조금에 관한 사항은 해당자로부터 사유 발생 시 경조금 신청에 의하여 경조금을 지급한다.

### 5.2 경조금 지급기준

임·직원 및 그 가족에 대한 경조금 지급기준은 다음과 같다.

| 구분 | 대상 | 경조금 | 화환·조화 | 장례용품 | 비고 |
|---|---|---:|:---:|:---:|---|
| 결혼 | 본인 | 2,000,000원 | ○ |  |  |
| 결혼 | 자녀 | 300,000원 | ○ |  |  |
| 결혼 | 본인 및 배우자 형제·자매 | 200,000원 |  |  |  |
| 회갑 | 본인 및 배우자 부모 | 200,000원 |  |  |  |
| 출산장려금 | 첫째 | 3,000,000원 |  |  | 사내부부는 1명에 대해서만 지급 |
| 출산장려금 | 둘째 | 7,000,000원 |  |  | 사내부부는 1명에 대해서만 지급 |
| 출산장려금 | 셋째 이상 | 10,000,000원 |  |  | 사내부부는 1명에 대해서만 지급 |
| 사망 | 본인 | 5,000,000원 | ○ | ○ |  |
| 사망 | 배우자 | 2,000,000원 | ○ | ○ |  |
| 사망 | 본인 및 배우자 부모 | 1,000,000원 | ○ | ○ |  |
| 사망 | 자녀 | 1,000,000원 | ○ | ○ |  |
| 사망 | 승중상 | 500,000원 | ○ | ○ |  |
| 사망 | 본인 및 배우자 조부모 | 300,000원 | ○ |  |  |
| 사망 | 본인 외조부모 | 300,000원 | ○ |  |  |
| 사망 | 본인 및 배우자 형제·자매 | 300,000원 |  |  |  |

#### 5.2.1 출산장려금 적용 조건

출산장려금은 직원 또는 직원의 배우자가 자녀를 출산한 경우 지급하며, 만 6세 미만의 아이를 입양한 경우도 포함한다.

사내부부의 경우 출산장려금은 1명에 대해서만 지급한다.

### 5.3 서류제출

경조사가 발생한 사원은 인사노무 관련 시스템의 경조사 신청 화면에서 증빙서류를 첨부하여 소속 부서장의 결재를 득한다.

#### 5.3.1 경조사별 제출서류

- 본인 결혼: 청첩장 및 혼인신고서
- 형제·자매 결혼: 부모 기준 가족관계증명서, 청첩장
- 배우자 형제·자매 결혼: 본인 가족관계증명서, 배우자 부모 기준 가족관계증명서, 청첩장
- 자녀 결혼: 본인 가족관계증명서, 청첩장
- 부모 회갑: 본인 가족관계증명서
- 배우자 부모 회갑: 배우자(본인 기준) 가족관계증명서
- 출산장려금: 출생증명서, 주민등록등본
- 사망: 기본증명서(상세, 사망일 표기 확인), 가족관계증명서, 부고장

#### 5.3.2 사망 관련 가족관계증명서 제출 기준

1. 배우자상·부모상·자녀상·형제자매상·형제수상·승중상: 본인 가족관계증명서
2. 조부모상: 아버지 기준 가족관계증명서
3. 외조부모상: 어머니 기준 가족관계증명서
4. 배우자 부모상·배우자 형제자매상: 배우자 기준 가족관계증명서
5. 배우자 조부모상: 배우자 부모 기준 가족관계증명서
6. 백숙부모상: 아버지 기준 가족관계증명서
7. 매형·매제상, 형부·제부상, 올케상: 가족관계증명서, 형제 가족관계증명서

#### 5.3.3 기타 증빙서류 인정

상기 제출 서류 외에도 사실관계를 증명할 수 있는 기타 서류가 있는 경우, 노무관리 주관부서장 검토 후 인정할 수 있다.

### 5.4 지급제한

급여 지급이 정지된 자에게는 경조금을 지급하지 아니한다.

### 5.5 결근 및 환수

사후 허위 사실이라는 것이 확인되었을 경우 결근 처리 및 환수 조치하며 인사위원회에 회부한다.

### 5.6 유효기간

본 기준에 의한 경조금 청구권 발생일로부터 3개월 이내 행사하지 아니할 경우 그 권리를 포기한 것으로 간주하고 지급하지 아니한다.

## 부칙

### 시행일

이 기준은 2025. 9. 29.부터 개정·시행한다.
````

## 규정/동호회 관리 규정.md

원본 경로: `규정/동호회 관리 규정.md`

````markdown
# 동호회 관리 규정

## 1. 목적

이 기준은 포스코퓨처엠 임직원의 동호회 활동을 지원하는 것을 목적으로 한다.

## 2. 적용범위

이 기준은 임직원 상호 간의 친목 도모 및 여가 활동 기회를 보장하는 동호회 활동 판단 근거를 제공함으로써 원활한 활동을 보장함을 목적으로 한다.

## 3. 용어의 정의

### 3.1 동호회

동호회 활동 지침의 정해진 요건을 갖추고 임직원 상호 간의 친목을 도모하기 위해 결성된 모임

### 3.2 동호회 활동보조금

회사에서 동호회 활동을 지원하기 위해 현금의 형태로 지급하며, 개인적인 목적으로 활용할 수 없는 동호회 공용의 자금

### 3.3 회장과 총무

각 동호회 활동을 주관하며 회원 및 활동을 관리하고 동호회의 대표자로서 권한을 가지고 활동에 책임을 지는 자

단, 동호회 활동의 의사결정은 동호회 회원의 동의를 얻어야 한다.

## 4. 책임과 권한

### 4.1 노사발전그룹장

노사발전그룹장은 전사 동호회의 정책을 결정한다.

### 4.2 노사발전그룹 담당자

노사발전그룹 담당자는 전사 동호회 정책을 시행하고, 접수한 동호회 신청을 최종 심사하여 동호회 총무에게 활동지원비를 지급한다.

## 5. 업무절차

### 5.1 동호회 결성

1. 최소 등록 인원은 5명 이상으로 한다.
2. 신규로 동호회를 결성하는 사람은 구성, 결성 취지 및 활동 사항 등을 사전에 노사발전그룹과 협의한 후 사내 홍보 활동을 통하여 동호회를 결성할 수 있다.

### 5.2 동호회 결성 구비서류

1. 동호인 그룹 등록 신청서
2. 연간 행사 계획서
3. 동호인 그룹 회원 명단
4. 동호인 그룹 원칙

#### 5.2.1 등록 접수 부서

노사발전그룹

#### 5.2.2 등록신청 및 승인 절차

#### 5.2.3 동호회 신청 양식 및 담당자 안내

> 운영 안내: 아래 내용은 사용자 제공 운영 절차이며, 본 규정 원문과 구분하여 관리한다.

동호회 신청 시 다음 항목을 작성한다.

1. 동호회 회장 / 총무
2. 동호회 명
3. 종목(스포츠, 문화 등)
4. 동호회 회칙 작성 및 게시판 등록 여부
5. 비고: 동호회 시스템에 총무 계좌번호 입력(필수)

신청 후 동호회 담당자인 노사발전그룹 담당자에게 신청 사실을 메일로 발신해야 한다.

### 5.3 동호인 등록 제한

1. 스포츠·레저 부문, 문화·예술·연구·학습 부문 중 1인 2개 가입을 허용한다.
2. 최소 등록 인원 5명 이상(활동 최소 인원 5명 이상)을 지원한다.
3. 향우회, 동문회, 부서 친목회 등 취지에 맞지 않는 활동은 지원하지 않는다.
4. 신규 등록 그룹은 등록 3개월 이후부터 지원한다.
5. 3분기 이상 활동하지 않는 동호회는 해체한 것으로 간주한다.
6. 사업장별 동일 분야 동호회는 3개로 제한한다.

#### 5.3.1 분야별 활동 범위

동호회 분야는 아래 허용 범위를 기준으로 심사한다. 분야별 예시는 대표 종목이며, 명시되지 않은 활동은 활동 목적·안전성·공동성 및 공용 보조금 사용 계획을 확인한 뒤 노사발전그룹이 판단한다.

##### 스포츠·레저 부문

- **허용**: 축구·풋살, 야구, 농구, 배드민턴, 테니스, 탁구, 볼링, 러닝, 등산, 자전거, 수영, 요가, 낚시, 헬스·웨이트트레이닝 등 임직원이 함께 정기적으로 참여하는 스포츠·레저 활동
- **조건부 허용**: 골프, 스키·스노보드, 클라이밍, 스쿠버다이빙, 모터스포츠, e스포츠 등은 안전관리 계획, 법규·보험 준수 여부 및 활동비 사용 계획을 사전 확인한 경우에 한해 허용
- **제외**: 도박성·사행성 활동, 불법 또는 무면허 활동, 개인 레슨·개인 장비 구매·개인 헬스장 회원권 중심의 활동

##### 문화·예술·연구·학습 부문

- **허용**: 독서, 글쓰기, 영화, 사진, 미술, 음악, 공연 관람, 보드게임, 외국어, 코딩, 역사·과학·경제 등 정기적인 문화·예술·지식 공유 활동
- **조건부 허용**: 자격증 학습, 직무 관련 연구, 외부 강의·행사는 회사의 공식 교육·부서 과제와 중복되지 않고, 공동 활동 계획과 공용 비용 집행 근거가 있는 경우에 한해 허용
- **제외**: 종교·정치·사회운동 목적 활동, 투자·부업·영업·상품 판매 활동, 개인 수강료·교재비·자격시험 응시료 지원을 주된 목적으로 하는 활동

##### 공통 승인·제외 기준

1. 정기적인 공동 활동과 임직원 상호 간 친목·여가 또는 지식 공유 목적이 확인되어야 한다.
2. 활동보조금은 공동 활동비에만 사용하며, 개인 식비·음주비·장비·회원권·수강료 등 개인적 지출에는 사용할 수 없다.
3. 향우회, 동문회, 부서 친목회 및 단순 식사·음주·친목만을 목적으로 하는 모임은 지원하지 않는다.
4. 대외행사·고위험 활동은 안전관리 및 회사 이미지 훼손 가능성을 포함해 사전 심사를 거쳐야 한다.

### 5.4 회원가입 및 탈퇴

1. 동호회 가입은 소속 사업장과 실근무지를 합하여 최대 1인 2개까지 가입할 수 있다.
2. 활동보조금 중복 지급을 방지하기 위해 동일 분야 이중 가입은 인정하지 않는다.
   - 예: 본사 내 축구 동호인 두 곳에 가입하는 경우 불인정
   - 단, 소속 사업장과 실근무지가 다른 경우에는 허용한다.
3. 동호인 가입 및 탈퇴를 희망하는 인원은 동호인 가입·탈퇴 신청서(별첨 3)를 작성하여 동호인 대표자에게 제출하여야 하며, 가입·탈퇴 신청서 사본은 노사발전그룹에 제출하여야 한다.

### 5.5 등록사항 변경 및 활동 결과 보고서 제출

1. 동호인 등록사항 중 회칙 및 소속 회원의 변경이 있을 경우 노사발전그룹의 승인을 받아야 한다.
2. 동호인 회장은 행사 관련 증빙서류를 첨부하여 동호인 활동 결과 보고서를 노사발전그룹에 제출하고 승인을 받아야 한다.

#### 증빙서류

- 활동보조금 지출 현황 및 영수증
- 활동사진

### 5.6 동호회의 해체

1. 동호회를 해체할 경우 회장은 회원들의 동의를 얻고, 해체 사유서(별첨 1) 및 활동보조금 현황을 주관팀에 제출해야 한다.
2. 다음과 같은 경우 노사발전그룹장은 동호회의 해체를 명할 수 있다.
   1. 동호회를 개인적인 목적으로 이용하는 경우
   2. 동호회 활동이 부진하거나 전무한 경우
   3. 동호회 목적 또는 활동상 불미스러운 문제가 발생하는 경우
   4. 기타 회사의 명예를 손상시킬 만한 행위가 발생할 우려가 있다고 인정되는 경우
3. 기존 동호회가 등록 기간 내에 실적을 등록하지 않은 경우 활동보조금은 지급하지 않는다. 다만, 다음 분기 내에 실적을 등록할 경우 실적 등록 분기에 활동보조금을 지급할 수 있다.
4. 동호회 해체 후 지원금 잔액이 있다면 노사발전그룹으로 반납하여야 한다.

## 6. 동호인 활동의 범위

### 6.1 자체 내 활동

동호회의 모든 행사는 전 회원이 참여하는 자체 내 활동을 원칙으로 한다.

### 6.2 대외행사 참가

대외행사 참가 시 회사의 이미지 제고 및 회사가 행사 참가의 필요성을 인정하는 경우에는 각 사업장장의 승인을 얻어 참가할 수 있다.

### 6.3 활성화 협의

각 동호회는 동호회 활성화에 필요한 사항을 주관팀과 협의하여 실행할 수 있다.

## 7. 동호회 활동보조금 지급 기준

| 구분 | 활동 요건 | 지원 내용 | 최소 인원 | 제출서류 | 비고 |
|---|---|---|---:|---|---|
| 정기지원 | 기제출된 연간계획서에 의거하여 분기별 1회 이상 활동 | 분기 단위 인당 3만원, 최대 30만원 | 5인 이상 | 정기활동보고서, 활동비 영수증, 활동사진, 참석자 명단 | 신규 등록 시 3개월 이후 지원 |
| 특별지원 | 회사를 대표하여 사외 행사에 참가하거나 행사를 개최할 경우. 단, 사전에 계획서 제출 | 연간 2회, 인당 1만원, 최대 30만원 | 5인 이상 | 특별활동보고서, 활동비 영수증, 활동사진, 참석자 명단 | 회사를 대표하여 참가하므로 회사 이미지 제고 및 품위 유지 |

### 7.1 동호회 활동보조금 지급 시기

매 분기 활동 이후 활동 실적을 등록한 후 지급한다.

### 7.2 서류 제출 방법

1. 동호회 활동실적 보고서 1부
2. 행사 진행 증빙자료(사진) 1부
3. 경비 집행 내역(영수증) 1부
4. 전체 회원 명단 및 참석 명단 1부

참석 명단은 개별 확인한다.

### 7.3 동호회 활동보조금 지급 절차

동호회 활동실적 등록 확인 → 노사발전그룹 확인 및 검토 → 지급 품의 → 지급 완료

## 별첨 1. 동호회 등록·해체 사유서

### 기본정보

- 동호회명:
- 구분: 회장 / 총무
- 소속:
- 직위:
- 성명:
- 연락처:
- 회원 현황:
- 해체 사유:

### 보조금 현황

| 일시 | 내용 | 입금 | 출금 | 잔액 |
|---|---|---:|---:|---:|
|  |  |  |  |  |

- 최종 마감액:
- 보조금 잔액 사용계획:

위 사유로 인하여 동호회 등록·해체를 보고합니다.

20년월일

회장(인)
````

## 규정/사내 추가 기준.md

원본 경로: `규정/사내 추가 기준.md`

````markdown
# 사내 추가 기준

## 이 문서에 대하여

원본 사규(경조금 지급기준, 여비관리기준, 여비관리 FAQ, 숙소지원금 운영 기준, 동호회 관리 규정)에는
없지만 사내에서 별도로 운영해 온 기준을 모은 문서다. 사규가 개정되어 원본 파일이 교체되어도
이 문서는 함께 사라지지 않는다.

**규정에 없는 기준을 추가할 때는 이 파일에 적는다. `app.py`의 프롬프트에 적지 않는다.**
프롬프트에 적으면 세 가지가 깨진다. 답변의 "확인한 규정"에 출처가 표시되지 않아 사용자가
사규인지 사내 운영 기준인지 구분할 수 없고, 규정 파일을 grep해도 나오지 않아 나중에 환각으로
오진되며, 검색 근거가 아니라 지시문이라 규정과 충돌해도 아무도 눈치채지 못한다.
이 파일에 적으면 검색·판정·생성이 다른 규정과 똑같이 다룬다. 코드는 고치지 않아도 된다.

항목마다 **출처**를 적는다. 누가·언제·어떤 근거로 정했는지가 비어 있으면 답변 근거로 쓰기 전에
담당 부서 확인이 필요하다. 출처를 적지 않으면 시간이 지난 뒤 이 값이 규정에서 온 것인지
누군가 지어낸 것인지 구분할 수 없게 된다.

한시적으로만 적용되는 항목(공지, 교육연수 특례 등)은 본문에 `적용기간: YYYY-MM-DD ~ YYYY-MM-DD`
줄을 넣는다. 상담 답변은 오늘 날짜를 알고 있으므로 기간이 지난 항목은 적용하지 않는다.

## 승중상 인정 조건

승중상은 부친이 이미 사망하여 장손자가 조부모상의 상주가 된 경우에만 인정한다.

경조금 지급기준 5.2의 지급표에는 승중상 500,000원(화환·조화 ○, 장례용품 ○)이 있으나
어떤 경우를 승중상으로 볼지는 정의되어 있지 않다. 위 세 조건(부친 사망, 장손자, 상주)이
확인되기 전에는 지급 대상 여부와 금액을 확정하지 않고, 세 조건을 확인 항목으로 안내한다.

- 확인 사항: 부친 사망 여부, 장손자 여부, 상주 여부
- 확인에 필요한 서류: 부친 사망 증빙, 본인 가족관계증명서, 상주 확인 자료, 부고장

출처: 확인 필요

## 경조금 지급대상이 아닌 관계의 경조휴가

백숙부모상, 매형·매제상, 형부·제부상, 올케상은 경조금·화환·장례용품 지원이 없고
경조휴가 2일만 적용한다.

경조금 지급기준 5.3.1에는 위 관계의 제출서류 기준이 있으나(백숙부모상은 아버지 기준
가족관계증명서, 매형·매제·형부·제부·올케상은 가족관계증명서와 형제 가족관계증명서),
5.2의 지급표에는 위 관계가 없다. 따라서 경조금은 지급되지 않는다.

경조휴가 일수는 복리후생 규정집 다섯 개 문서 어디에도 없다. 별도 문서(취업규칙 또는
휴가 관련 규정)에 근거가 있을 것으로 보이며, 그 문서를 확인해 출처를 채워야 한다.

이 항목은 위에 열거한 관계에만 적용한다. 백숙부모는 아버지의 형제와 그 배우자를 말하며,
고모·이모·외삼촌은 여기에 포함되지 않는다. 열거되지 않은 관계는 이 항목을 적용하지 말고,
지급표에도 없는 관계이므로 지원 여부를 확정하지 않고 주관 부서 확인으로 안내한다.

출처: 확인 필요 — 경조휴가 규정 원문 미확보

## 경조금 청구기한의 주말·공휴일 처리

경조금 청구기한 3개월의 마지막 날이 주말이나 공휴일이어도 기한을 연장하지 않는다.

경조금 지급기준 5.6은 "청구권 발생일로부터 3개월 이내 행사하지 아니할 경우 그 권리를
포기한 것으로 간주하고 지급하지 아니한다"고만 정하고, 마감일이 휴일인 경우를 다루지 않는다.
연장하지 않는다는 것이 사내 운영 방침이다.

출처: 확인 필요

## 국내출장 중 식사가 제공된 경우

출장 중 식사가 제공되면 제공된 1회당 식비 10,000원을 차감한다.

여비관리기준에는 이 차감 규칙이 없다. 별첨 1은 소액경비를 1일 50,000원 정액으로 정하고,
5.14.2는 그 소액경비를 이동거리에 따라 전액(왕복 120km 이상)·1/2(50km 이상)·미지급(50km 미만)으로
차등한다고만 정한다. 식사 제공 여부는 다루지 않는다.

같이 확인해야 할 것: 식비 단독 금액(1일 30,000원)은 5.14.3 **자가차량 동승자** 기준에만 나온다.
일반 출장자에게 지급되는 것은 소액경비 50,000원이며 거리 조건이 붙는다. "국내출장 식비는
1일 3만원"으로 단정하면 왕복 50km 미만 출장자에게 지급을 단정하게 되므로 그렇게 안내하지 않는다.

출처: 확인 필요

## 경조 담당 업체

사망 경조 문의에서 필요 서류를 안내한 뒤 아래 연락처를 함께 표시한다.

○ 회사 경조 담당 업체(경조물품, 화환 등)
- 현진시닝 : 1600-0113 (24시간)

규정집에는 없는 사내 정보다. 규정 파일을 검색해도 나오지 않으므로 지어낸 값으로
오인하기 쉽다. 실제로 두 차례 환각으로 오진된 적이 있다.

출처: 확인 필요 — 사내 계약 업체

## 결혼예정자의 주소 전입과 단신 근무자 판정

숙소지원금을 받는 직원의 거주지로 혼인신고 전 결혼예정자가 주민등록상 전입했더라도,
실제로 함께 거주하지 않는다면 전입신고만으로 단신 근무자 지위를 상실하거나 숙소지원금
지원 대상에서 제외된 것으로 판단하지 않는다.

숙소지원금 운영 기준 3.4의 단신 근무자 여부는 주민등록상 주소의 일치 여부가 아니라 실제로
본인 외의 자와 함께 거주하는지를 기준으로 판단한다. 혼인신고 여부만으로 실제 동거를
추정하지 않는다. 주소지가 동일하여 사실관계 확인이 필요한 경우에는 5.6.3에 따라 공과금
납부 내역 등 실제 거주 여부를 확인할 수 있는 자료를 추가로 요구할 수 있다.

실제 동거를 시작한 경우에는 단신 근무자 지위를 상실할 수 있으므로, 그 사실이 발생한 날부터
14일 이내에 주관부서 또는 담당자에게 통보하고 지원 자격을 다시 확인한다. 주소만 전입하고
실제 동거하지 않는 상태는 그 자체로 5.7의 자격 상실 통보 사유에 해당하지 않는다.

- 주소만 전입하고 실제 동거하지 않음: 자동 제외하지 않음
- 실제 동거를 시작함: 단신 근무자 지위 상실 가능, 14일 이내 통보 및 자격 재검토
- 실제 동거 여부를 확인할 수 없음: 지원 불가로 단정하지 않고 관련 자료 확인 또는 담당자 이관
- 윤리위반·부정 수급 경고: 허위 신청이나 실제 거주 형태의 불일치가 확인된 경우에만 안내하며,
  주소만 전입했거나 실제 동거 여부가 불명확한 단계에서는 조건부 경고로도 언급하지 않음

출처: 사용자 제공 운영 예외 기준(2026-09-10)
````

## 규정/숙소지원금 운영 기준.md

원본 경로: `규정/숙소지원금 운영 기준.md`

````markdown
# 숙소지원금 운영 기준

## 1. 목적

이 규정은 숙소지원금 관리에 대한 세부기준과 절차를 정한다.

## 2. 적용범위

재직 중인 모든 직원에 대하여 적용하며, 다른 사규에 특별히 정하거나 근로계약 등으로 별도 기준을 정한 경우를 제외하고는 이 규정을 따른다.

## 3. 용어의 정의

### 3.1 숙소지원금

타(他) 지역 출신 직원이 신규 채용이나 부임을 통해 당사 사업장에 배치되는 경우, 안정적인 지역 정착과 생활 적응에 필요한 부담을 경감시키기 위해 회사에서 제공하는 최소한의 주거 지원비를 뜻한다.

### 3.2 타지역 출신

채용이나 부임으로 인해 타(他)지역에 생활(주거) 근거지가 발생하여 주거비용의 추가 부담이 발생하는 자를 의미하며, 기존 생활 근거지의 판단 기준은 미혼자는 부모 주소지, 기혼자는 배우자 주소지를 포함한다.

### 3.3 무주택자

근무 지역에 본인 소유의 주택(전·월세 등의 임차형태는 제외)을 보유하지 않은 경우에 무주택자로 본다. 단, 아래에 해당하는 경우에는 무주택자에서 제외한다.

1. 미혼자는 부모, 기혼자는 배우자가 소유한 주택(전세 및 반전세 포함)이 있을 경우
2. 본인 소유의 주택을 타인에게 임대하거나 기타 수익 목적으로 사용하는 경우
3. 전용면적 85㎡를 초과하는 주택을 임차하는 경우

### 3.4 단신 근무자

근무 지역 내 거주지에 혼자 거주하는 경우를 뜻한다. 단, 가족·직원 등 본인 이외의 자와 함께 거주하는 경우에는 단신 근무자에 해당하지 않는다.

## 4. 책임과 권한

### 4.1 숙소지원금 지급 권한

숙소지원금 지급에 대한 권한은 주관부서 부서장에게 있다.

## 5. 업무절차

### 5.1 지원내용

| 구분 | 지역 | 지원기간 | 지원금액 | 비고 |
|---|---|---:|---:|---|
| 신규 채용 | 서울 외 | 발령일로부터 5년간 | 40만원/월 |  |
| 신규 채용 | 서울 | - | - |  |
| 신규 부임 | 서울 외 | 발령일로부터 3년간 | 40만원/월 | 그룹사 전환채용 포함 |
| 신규 부임 | 서울 | 발령일로부터 3년간 | 60만원/월 | 그룹사 전환채용 포함 |

1. 지원 금액은 월 차임(월세)에 대해서만 지원하며 관리비, 공과금 및 기타 비용은 제외된다.
2. 전세의 경우에는 전세금 1,000만원당 10만원/월을 지원한다.
3. 숙소지원금 신청 및 서류 확인 절차가 완료된 달로부터 기산하여 지급한다.
4. 매월 15일을 기준으로 이전에 신청한 자는 해당 월에 지급하며 이후에 신청한 자는 다음 월에 지급한다.
5. 발령일이 속한 월의 다음 달로부터 6개월 이내에 신청하지 않으면 지원자격을 상실한다. 단, 특별한 사정이 있는 경우에는 노무관리 주관부서장 승인하에 신청 기간을 연장할 수 있다.
6. 이미 근무한 지역으로 재부임할 경우 발령일로부터 1년간 지급한다. 단, 기존에 부임으로 받은 숙소지원금 기간의 합이 3년 이하일 경우에는 3년까지 숙소지원금을 지급할 수 있다.
7. 신규 채용과 부임에 따른 숙소지원금 지급사유가 동시에 발생할 경우에는 발령 시점부터 부임에 따른 숙소지원금만 지급한다. 단, 신규 채용과 부임에 따른 숙소지원 지급기간의 합이 5년보다 짧을 경우에는 차이 기간에 대해서 보전 지급할 수 있다.
8. 숙소지원금 지급 중 정규직 전환 등의 신분 변경 사항이 발생하더라도 해당 기간을 포함하여 지원 기간을 산정한다.

### 5.2 지원 대상

1. 신규 채용자(신입, 경력, 계약직, 임시직 포함)
2. 타 지역의 사업장으로 부임을 명 받은 자
3. 그룹사 인력교류자(전환 채용, 파견 포함)
4. 주관부서 부서장이 지급을 승인한 자

### 5.3 지원 제외 대상

1. P8 이상 임원
2. 파견비 지급 대상자
3. 국내에서 숙소지원금을 받고 있는 해외주재원 부임 대상자는 부임일이 속한 월의 다음 달부터 지원 대상에서 제외한다.
4. 숙소지원금을 지급받고 있는 직원의 휴직 기간이 연속해서 6개월을 초과하는 경우, 6개월을 초과하는 날이 속한 월의 다음 달부터 지원 대상에서 제외한다.
5. 숙소지원금을 지급받고 있는 직원의 파견 기간이 연속해서 6개월을 초과하는 경우, 6개월을 초과하는 날이 속한 월의 다음 달부터 지원 대상에서 제외한다. 파견 지역이 다른 경우라도 파견이 연속하는 경우에는 파견 기간에 합산한다.
6. 휴직 및 파견으로 인해 지원 대상에서 제외되는 기간이 발생하더라도 전체 지원기간에 포함한다.
7. 지원 제외 대상에 속하더라도 주관부서의 부서장이 승인한 경우에는 지원할 수 있다.

### 5.4 지원기준

1. 신규 채용이나 부임을 통해 사업장에 배치된 타(他)지역 출신의 무주택 단신부임 직원
2. 타 지역의 기준은 아래와 같다.

#### 서울 근무지

아래 지역에 거주하는 자(미혼자는 부모, 기혼자는 배우자 주소지 포함)

- 경기: 동두천, 연천, 포천, 가평, 여주, 이천, 안성, 평택, 화성, 오산
- 인천: 옹진군, 강화군

#### 서울 외 근무지

근무 중인 사업장 기준으로 반경 25km를 초과하는 지역에 거주하는 자

- 동일 행정구역이라도 반경 25km를 초과하는 경우에는 타 지역으로 본다.
- 단, 반경 25km 이내라도 차량으로 이동 불가능한 도서 지역은 예외로 둔다.

3. 반경 25km 이내인 경우라도 교통여건 등이 열악할 경우에는 주관부서 판단하에 예외 지원 대상 지역으로 검토할 수 있다.
4. 타 지역에 속하더라도 해당 지역으로 통근버스가 운행될 경우에는 숙소지원금 지급을 중단한다.

### 5.5 지원특례

1. 숙소지원금을 지원받는 직원이 발령에 따라 근무지역을 이동할 경우, 전(前) 근무지 숙소를 정리하는 기간 동안 발생하는 비용에 대해서는 3개월간 숙소지원금 한도 내에서 실비로 지원한다.
2. 부득이한 사정으로 3개월간 전 근무지 숙소를 정리하지 못한 경우에는 1개월 단위로 최장 6개월까지 정리 지원기간을 연장한다.
   - 3+1+1+1개월
   - 처분 노력을 입증할 자료 제출 필수
3. 전세의 경우에는 숙소지원금 지원 대상 주택을 담보로 한 전세 관련 대출에 한해서 한도 내 이자를 지원한다. 단, 증빙으로 확인이 가능한 경우에 한하며 일반 신용대출은 제외한다.
4. 숙소정리 비용이 3개월분을 합산한 총 한도 금액보다 낮은 경우에는 실비로 일시 지급할 수 있다.
5. 계약기간과 관계없는 청소비 등의 기타 비용은 지원 대상에서 제외된다.
6. 전 근무지 숙소정리 비용과 현(現) 근무지 숙소지원금은 중복 지급이 가능하다.

### 5.6 제출서류

#### 5.6.1 최초 숙소지원금 신청 시

아래 서류를 모두 제출하여야 한다.

1. 직원 본인 주민등록등본, 주민등록초본, 가족관계증명서
2. 기혼자인 경우 배우자 주민등록초본, 미혼자인 경우 부친 및 모친 주민등록초본
3. 임대차계약서
4. 숙소지원금 수급 서약서

#### 5.6.2 지급 갱신·지원 중지·지원 자격 확인 시

아래 서류를 모두 제출하여야 한다.

1. 직원 본인 주민등록등본, 주민등록초본, 가족관계증명서
2. 기혼자인 경우 배우자 주민등록초본, 미혼자인 경우 부친 및 모친 주민등록초본
3. 임대차계약서
4. 월세 이체내역

#### 5.6.3 기타

- 실제 거주 여부 확인을 위해 공과금 납부 내역 등 추가 서류 제출을 요구할 수 있다.
- 전입신고가 불가능한 경우 관련 증빙자료를 제출하면 주관부서 판단하에 지원 여부를 결정한다.
- 관련 서류 제출이 불가능한 경우에는 주관부서에서 판단하여 제출서류를 조정할 수 있다.

### 5.7 보고의무

숙소지원금을 지급받는 대상자는 다음 사항이 발생한 경우 14일 이내에 주관부서 또는 담당자에게 통보하여야 한다.

1. 타(他)지역 출신의 무주택 단신부임 지위를 상실하는 경우
2. 회사에서 제공하는 숙소에서 거주하는 경우
3. 숙소지원금과 부임비를 동시에 수령하는 경우
4. 그 외 숙소지원금 지원 자격을 상실하는 경우

### 5.8 기타사항

1. 숙소지원금을 지원받는 직원은 1년 단위로 갱신 서류를 제출하여야 한다. 갱신 서류 미제출 및 미신청자는 지원자격을 상실한다. 단, 특별한 사정이 있는 경우에는 노무관리 주관부서장 승인하에 신청 기간을 연장할 수 있다.
2. 그 외 회사에서 필요하다고 판단되는 경우에는 수시로 관련 서류 제출을 요구할 수 있다.
3. 숙소지원금과 부임비는 지급 성격이 중복되므로 부임비를 수령한 월의 다음 달부터 숙소지원금을 지급한다. 숙소지원비를 이미 지원받고 있는 직원이 타 지역으로 부임할 경우에는 해당 인사명령일이 속한 월의 전 근무지 숙소지원금을 지원받을 수 있다.
4. 예외 적용이 필요한 경우 또는 본 운영 규정에 명시되지 않은 사항은 주관부서 부서장 판단하에 결정한다.
5. 본 규정을 위반하는 행위(제출서류 위·변조 및 미제출, 보고의무 불성실 등)를 하여 숙소지원금을 부정 수급할 경우, 부정 수급액 환수 및 관련 비위행위에 대해 징계 등 적절한 조치를 할 수 있다.
````

## 규정/여비관리 FAQ.md

원본 경로: `규정/여비관리 FAQ.md`

````markdown
# 여비관리 FAQ

## 해외출장 종료 후 개인 일정 체류

### 질문

해외출장 일정을 마친 후 개인 연차나 휴가(개인휴가)를 사용하여 현지에 더 체류한 뒤 귀국해도 항공편을 지원받을 수 있나요?

### 답변

개인 연차나 휴가를 사용하여 승인된 해외출장 일정을 초과해 체류한 뒤 귀국하는 항공편은 회사에서 지원하지 않는다.

### 운영 원칙

1. 해외출장자는 회사가 승인한 해외출장 일정 내에 복귀하는 것을 원칙으로 한다.
2. 개인 연차·휴가 또는 개인 여행을 위해 귀국일을 늦추는 경우, 변경된 귀국 항공편은 개인 일정에 따른 것으로 본다.
3. 개인 일정으로 변경된 귀국 항공편의 항공료, 운임 차액 및 변경 수수료는 본인이 부담한다.
4. 출장 일정 종료 후의 개인 연차·휴가 사용과 체류 기간은 회사의 근태 절차에 따라 별도로 승인받아야 한다.
5. 승인된 해외출장 일정을 임의로 변경하거나 근태 승인을 받지 않은 경우에는 근태 이슈가 발생할 수 있다.

### 예외 및 문의

업무상 사유로 귀국 일정 변경이 필요한 경우에는 개인 일정으로 보지 않으며, 일정 변경 전에 출장 승인권자와 노무관리 주관부서의 승인을 받아야 한다. 개인 일정인지 업무상 일정 변경인지 불명확한 경우에는 항공편을 변경하기 전에 담당 부서에 문의한다.
````

## 규정/여비관리기준.md

원본 경로: `규정/여비관리기준.md`

````markdown
# 여비관리기준

## 1. 목적

이 기준은 회사의 임원 및 직원이 국내외로 출장, 연수, 파견 또는 부임할 때의 여비 지급기준을 정함을 목적으로 한다.

## 2. 적용범위

이 절차서는 임원 및 직원의 교통비, 소액경비, 숙박비, 부임비, 준비금 및 기타 경비에 대하여 적용한다.

## 3. 용어의 정의

### 3.1 가족

“가족”이라 함은 동일 세대 내에서 임원 또는 직원 본인과 생계를 같이하는 다음의 자를 말한다.

1. 배우자(사실상 혼인 관계에 있는 자 포함)
2. 미성년의 직계비속
3. 직계존속

### 3.2 유족

“유족”이라 함은 근로기준법 시행령 제48조(유족의 범위 등)에서 정하는 자를 말한다.

## 4. 책임과 권한

### 4.1 여비기준 변경 권한

출장, 연수, 파견 또는 부임할 때의 여비기준 변경 권한은 소속 본부장(본부장 부재 시 실장)에게 있다.

### 4.2 기준 제정·개정 및 배포 책임

이 기준의 제정·개정 및 배포 책임은 노무관리 주관부서에 있다.

## 5. 업무절차

### 5.1 여행일수 계산

1. 여행일수는 용무로 소요되는 일수에 의한다. 다만, 여행 중 질병이나 부득이한 사유로 체재하는 일수는 의사의 진단서 또는 기타 확실한 증거에 의거하여 여행일수에 가산한다.
2. 전항 단서의 경우 국내여행에 대해서는 용무로 소요되는 일수를 포함하여 30일을 초과할 수 없다.
3. 국내여행의 경우 동일 지역 내 출장일수는 연속 14일을 초과할 수 없다.
4. 해외여행일수가 14일을 초과하는 경우 노무관리 주관부서 및 인사관리 주관부서에 문의한 후 업무성을 판단하여 해외출장 또는 해외단기파견으로 진행해야 한다.

### 5.2 여비의 지급 및 정산

여비는 사후 정산하여 지급한다. 여정이 불분명하거나 장기 출장인 경우에는 귀임 후 30일 이내에 증빙과 함께 정산하여야 한다.

### 5.3 여비 환불 및 추가 지급

다음 각 호의 경우에는 귀사 시 실비 정산하여 잔여분은 환불하고 부족분은 추가 지급받도록 한다.

1. 출장일수 단축·연장 및 출장지 변경에 따른 차액분
2. 항공등급 조정에 따른 차액분
3. 참가비, 렌터카비, 임대료, 통역비, 통신비, 입장료 등 기타 경비 차액분
4. 업무상의 사유로 출장 일정이 변경되어 발생하는 수수료 또는 일정이 취소되어 발생하는 수수료는 소속 부서장의 승인을 받아 지급받을 수 있다.

### 5.4 출장복명

1. 출장자는 출장 용무를 끝내고 귀임한 때에는 국내출장의 경우 지체 없이, 해외출장의 경우 5일 이내에 서면으로 보고하여야 한다. 다만, 간이하거나 비밀에 속하는 사항은 구두로 할 수 있다.
2. 출장 통제부서는 해외출장 통제 시 특별 정보 수집을 지시할 수 있다.

### 5.5 직책 또는 직능자격 변경 시의 여비

출장, 연수 또는 유학 중 직책이나 직능자격의 변경이 있는 때에는 새로운 직책 또는 직능자격 변경일부터 변경된 여비를 지급한다. 다만, 해외출장의 경우에는 직책 또는 직능자격 변경에 따른 차액을 지급하지 않는다.

### 5.6 직무대행 시의 여비

직무대행인 경우에는 해당 직책에 해당하는 여비를 지급한다.

### 5.7 임원 수행자의 여비

직원이 임원 또는 임원에 준하는 자를 수행하여 여행할 때에는 숙식비와 국내 교통비를 임원과 동일한 액수로 지급할 수 있다.

### 5.8 여비 지급의 제한

1. 여행자의 여비(개인연수 여비 포함) 전부 또는 일부를 회사 이외의 기관이 부담하는 경우에는 대행 여비를 지급하지 않는다.
2. 회사의 교통수단 또는 숙박시설 등과 같이 요금 지불을 요하지 않는 경우에는 해당 교통비 또는 숙박료를 지급하지 않는다.

### 5.9 임직원 외의 여비

1. 고문 또는 사외인이 회사의 업무를 위촉받아 여행할 때에는 이 지침을 준용하여 여비를 지급할 수 있다. 다만, 여비 지급에 적용할 직능자격은 발의부서에서 정한다.
2. 출장 목적상 특히 필요하다고 인정하는 경우에는 배우자를 동반할 수 있다. 다만, 배우자의 여비는 출장자에 준하되 숙박비는 반액을 지급한다.

### 5.10 국내여비

#### 5.10.1 교통비

교통비 지급기준은 별첨 1에 의한다.

#### 5.10.2 소액경비 및 숙박비

1. 소액경비 및 숙박료 지급기준은 별첨 1에 의한다.
2. 소액경비는 여행일수, 숙박비는 숙박 수에 따라 지급한다. 다만, 회사 주택시설을 무료로 이용하거나 자택에서 숙박하는 경우에는 숙박비를 지급하지 않는다.

### 5.11 부임비 및 이전비

주거 이전을 하는 부임자에게는 별첨 2의 부임비와 이전비를 지급한다. 다만, 미혼 부임자의 경우에는 별첨 2의 부임비 정액의 3분의 2와 이전비를 지급한다.

### 5.12 퇴직자·휴직자에 대한 여비

여행 중 퇴직 또는 휴직된 자에게는 퇴직 또는 휴직 당시의 직책 또는 직능자격을 기준으로 여비를 지급한다.

### 5.13 사망자의 여비

여행 중 사망할 때에는 사망 당시 직책 또는 직능자격에 해당하는 여비의 2배액을 유족에게 지급한다.

### 5.14 국내여비 특례

#### 5.14.1 신규 채용 또는 복직 후 부임

신규 채용되어 부임한 자 또는 복직되어 부임하는 자에게는 여비를 지급하지 않는다. 다만, 다음 각 호에 해당할 때에는 아래와 같이 여비를 지급할 수 있다.

1. 본사에서 오리엔테이션을 받고 부임하는 자에게는 교통비 및 소액경비를 지급할 수 있다. 박사학위 소지자로서 기술연구원으로 신규 채용되어 부임하는 자에게는 교통비, 소액경비, 이전료 및 가족여비를 지급할 수 있다.
2. 신규 부임하는 임원에게는 별첨 2의 국내이전료 정액표에 따른 이전비(이사비＋부동산중개료)를 지급한다.

#### 5.14.2 자가차량 또는 대중교통 이용 시

자가차량 또는 대중교통을 이용한 출장 시 교통비는 실비로 지급하며, 소액경비는 이동거리에 따라 차등 지급한다.

- 왕복 120km 이상: 소액경비 전액 지급(50,000원)
- 왕복 50km 이상: 소액경비 1/2 지급(25,000원)
- 왕복 50km 미만: 소액경비 미지급

#### 5.14.3 자가차량 동승자

자가차량을 이용한 출장 시 동승자의 소액경비는 이동거리에 따라 차등 지급한다.

- 왕복 120km 이상: 식비만 전액 지급(30,000원)
- 왕복 50km 이상: 식비 1/2만 지급(15,000원)
- 왕복 50km 미만: 소액경비 미지급, 유류비 및 감가상각비 지급

※ 동일 지역 내 사업장 간 업무상 이동으로 50km를 초과할 경우 근거리 출장 기준을 적용한다.

※ 근거리 출장에서 경유지가 발생하여 50km를 초과하는 경우 소액경비는 지급하지 않는다.

#### 5.14.4 국내출장 시 택시 이용

다음 사유로 택시를 예외적으로 이용할 수 있다.

- 사업장 출장으로 철도, 시외버스, 항공 등을 탑승하기 위해 이동하는 경우
- 출장 목적지 내에서 업무를 위해 이동해야 하는 경우
- 현지교통비는 1일 2만원까지 지급하며, 택시비가 이를 초과하는 경우 초과분을 실비로 정산한다.

초과분 정산 시 인정되는 택시비 총액 한도는 다음과 같다.

- 일반 출장: 1일 최대 5만원
- 당일 출장: 1일 최대 10만원

지급액은 다음과 같이 산정한다.

```text
지급액 = 인정금액(택시비 총액 한도 적용) - 현지교통비(2만원)
```

초과금액을 실비로 정산할 때에는 교통비를 포함하며 영수증을 반드시 첨부해야 한다.

#### 5.14.5 회사 차량 또는 렌터카 이용

회사 차량이나 렌터카를 이용할 경우 탑승한 출장자 모두에게 현지교통비를 지급하지 않는다.

렌터카 비용은 렌트비, 유류비, 통행료, 주차료 등을 포함하며 1일 162,000원 한도 내에서 실비로 지급한다. 렌터카 사용일수만큼 탑승한 출장자 모두에게 소액경비 중 현지교통비를 지급하지 않는다.

#### 5.14.6 회사 차량 이용 시 기타 경비

회사 차량 이용 시 발생한 주유비, 주차비 등은 기타 경비 항목으로 지급할 수 있다.

### 5.15 파견 근무자의 여비

#### 5.15.1 일반 파견 근무자

- 그룹사 인력교류 파견자는 사내 파견자와 동일한 기준을 적용할 수 있다.
- 자택 소재지로 파견 명령을 받은 경우 파견비 지급 대상이 아니다.
  - 미혼자는 부모 주소지, 기혼자는 배우자 주소지로 자택 소재지를 판단한다.
- 이전 근무지와 파견지 근무지가 동일한 경우 파견비 지급 대상이 아니다.

세부 지급기준은 다음과 같다.

1. 왕복 교통비는 별첨 1에 따르며, 파견경비는 1일 기준 33,000원 정액으로 지급한다. 파견경비는 파견에 필요한 기타 제반 비용을 포함하는 것으로 본다.
2. 숙박료는 1박 기준 50,000원 정액으로 지급한다. 다만, 15일 이상의 파견기간에 대해서는 파견경비와 숙박비를 정액의 60%로 지급한다. 서울지역 파견 시 숙소를 제공하여 숙박비를 지원하지 않을 경우, 15일 이상의 파견기간에 대해 1년 범위 내에서 파견경비 정액의 80%를 지급한다.
3. 파견지에서 타 지역으로 출장할 때에는 별첨 1에 따라 지급한다. 다만, 자택 소재지로 출장할 때에는 숙박비를 지급하지 않는다. 파견경비는 파견일로부터 14일 이내 출장 시 지급하지 않으며, 파견일로부터 15일 이상 경과 후 출장 시에는 파견경비 정액의 40%를 지급한다.
4. 3개월 이상 파견자로서 가족이 이전하는 경우에는 제5.11에서 규정한 부임비 및 이전비를 지급한다.
5. 파견 시작일 또는 종료일에 해당 근무지로 이동한 경우에는 별첨 1의 교통비만 지급한다. 파견 시작일 전날 파견 근무지로 이동한 경우에는 별첨 1에 따라 교통비, 숙박비 및 소액경비를 모두 지급한다.
6. 단신으로 1년 이상 파견 후 복귀할 때에는 제5.11에서 규정한 이전비를 지급한다.

#### 5.15.2 주재근무를 위한 파견자

1. 제5.11에서 규정한 부임비 및 이전비 지급
2. 파견지역에서 타 지역으로 출장할 경우 별첨 1의 정액 지급

### 5.16 외국인 및 외빈 수행자의 여비

외국인 및 외빈을 동반 수행하여 여행할 때에는 교통비와 숙박료는 실비로 지급하고, 소액경비는 정액의 30%를 가산하여 지급할 수 있다.

### 5.17 국외여비

#### 5.17.1 지급기준

1. 출장자에게 교통비, 소액경비 및 숙박비를 지급한다. 초청자가 여비를 부담하거나 여비가 참가비 등에 포함된 경우에는 해당 부분을 지급하지 않는다. 다만, 필요한 경우 식비를 제외한 소액경비를 지급할 수 있다.
2. 교통비는 항공임 지급을 원칙으로 한다. 다만, 항공노선이 없는 지역 또는 기타 사정으로 항공여행을 할 수 없는 경우에는 철도임 또는 자동차임을 지급한다.
3. 교통비 지급기준은 별첨 4에 의한다.
4. 출장자의 숙박비 및 소액경비 지급기준은 별첨 3에 의한다. 소액경비는 여행일수, 숙박비는 숙박 수에 따라 지급한다.
5. 해외연수 또는 해외유학생의 체재비(소액경비·숙박비)는 별첨 7에 따라 연수 또는 유학기간(출국일부터 귀국일까지)에 맞춰 지급하고, 해외연수자에게는 별도의 준비금을 지급한다. 다만, 준비금은 출국일 이전 1년 이내에 지급받은 자에게는 새로 지급하지 않으며, 연수기간이 30일 미만이면 기준액의 1/2을 지급한다.
6. 준비금 지급기준은 별첨 6에 의한다.
7. 해외유학자가 가족을 동반하는 경우 가족여비는 별도로 지급하지 않는다.
8. 해외출장을 위해 부득이하게 전일 이동해야 하는 경우 국내여비관리 기준에 따라 숙박비, 시외교통비 및 1일분의 소액경비를 지급한다.
9. 해외출장 첫날은 현지 도착일을 기준으로 하며 출국일에는 해외 소액경비를 지급하지 않는다. 단, 현지 도착일과 출국일이 같은 경우에는 해외 소액경비를 지급한다.
10. 해외출장 후 국내 입국일에는 해외 소액경비를 지급하지 않는다. 단, 현지 출발일과 국내 입국일이 같은 경우에는 해외 소액경비를 지급한다.
11. 위의 경우 중 기내에서 숙박하는 경우에는 현지 도착·출발일과 출국·입국일이 같더라도 해외 소액경비를 지급하지 않는다.
12. 국내 입국일에 발생하는 국내 교통비는 기타 경비로 정산하며 소액경비는 지급하지 않는다.

#### 5.17.2 장기출장 및 출장 연기

1. 동일지에 장기 출장하는 경우 30일을 초과하는 일수에 대한 체재비는 해외출장 체재비의 80%를 지급한다.
2. 부득이한 사유로 출장기간을 연장하는 경우 해당 출장자의 승인권자가 승인한 경우에 한하여 연장일수에 해당하는 여비(소액경비·숙박료)를 지급할 수 있다.
3. 14일을 초과하는 출장의 경우 주관부서와 협의해야 한다.

#### 5.17.3 사망자의 여비

여행 중 사망한 때에는 본인 및 동반 가족에게 부임에 준한 귀국여비를 지급한다.

#### 5.17.4 기타 경비의 지급

업무상 필요하다고 인정할 때에는 다음 기준에 따라 기타 경비를 지급할 수 있다.

1. 특별활동비(만찬비·업무추진비 포함)는 사장의 승인이 있을 때 지급한다.
2. 선물비는 해외출장에 한하여 별첨 5의 선물비 지급기준에 따라 지급한다.
3. 도서구입비, 회의실 임대료, 통신비, 통역비, 렌터카비, 참가비, 입장료 등 실비성 기타 경비는 실비로 지급한다.
4. 여권수수료, 출입국세 등의 수속 부대비는 실비로 지급한다.

#### 5.17.5 지급 통화

지급 통화는 미화(USD)를 원칙으로 하되, 필요한 경우 출장국의 통화로 지급할 수 있다.

#### 5.17.6 지침의 효력 제한

이 지침에 의한 국외여비 기준은 외국환 관리규정에 따라 정부기관(기획재정부 또는 한국은행)의 외화 지급이 인정된 경우에 한하여 적용한다. 인정액이 이 규정에 미달하는 경우에는 인정액만 지급한다.

다만, 해외연수 및 유학자에 대해서는 필요한 경우 별도의 사내 외화 지급 상환액을 설정·운영할 수 있다.

#### 5.17.7 숙박료 특례 지급

1. 국외 회의 참가 시 주최 측에서 숙박처를 지정한 경우에는 근거서류에 따라 실비를 인정하여 지급할 수 있다.
2. 전쟁지역 및 테러위험지역 등 신변안전상 특별 숙박처 이용이 필요하다고 인정되는 경우에는 숙박비 기준의 1.5배를 지급할 수 있다.

#### 5.17.8 해외파견 근무자의 여비

1. 왕복 교통비는 별첨 4의 기준에 따라 지급한다.
2. 식비(1일당), 일비(1일당) 및 숙박비(1박당)는 파견국가별 물가 수준, 합작법인 등에서의 숙식 제공 여부 등을 고려하여 별도로 정한다.

## 6. 기록 및 첨부

| 기록명 | 보존연한 | 보관부서 |
|---|---:|---|
| 별첨 1. 국내여비기준표 | 5년 | 노사협력그룹 |
| 별첨 2. 국내이전료 정액표 | 5년 | 노사협력그룹 |
| 별첨 3. 국외여비기준표 | 5년 | 노사협력그룹 |
| 별첨 4. 출·귀국 시 국외교통비 기준표 | 5년 | 노사협력그룹 |
| 별첨 5. 선물비 지급기준 | 5년 | 노사협력그룹 |
| 별첨 6. 국외준비금 정액표 | 5년 | 노사협력그룹 |
| 별첨 7. 해외유학 및 연수자 체재비 정액표 | 5년 | 노사협력그룹 |

## 7. 관련문서

### 별첨 1. 국내여비기준표

단위: 원

| 여비 구분 | 철도임 | 선임 | 항공임 | 자동차임 | 숙박비(1박당) | 소액경비(1일당) |
|---|---|---|---|---|---|---:|
| 임원 | 새마을호 특실 또는 KTX 특실 | 1등 정액 | P10 이상: Business / P7 실장·P8·P9: 2시간 이상 시 Business | 실비 | 실비(Standard Room 이용) | 실비 |
| P6·P7 직책보임자 | 새마을호 보통 또는 KTX 보통 | 2등 정액 | 실비 | 실비 | 실비(100,000원 한도) | 50,000원 |
| 전 직원 | 새마을호 보통 또는 KTX 보통 | 2등 정액 | 실비 | 실비 | 실비(100,000원 한도) | 50,000원 |

#### 주석

1. 자가차량 이용 시 유류비, 통행료 및 감가상각비(50원/km)를 실비로 지급한다.
   - 대중교통 및 자가차량 이용 시 통행료 영수증 첨부·보관 필요
   - 교육연수 및 해외출장 시 국내 주차비는 지급하지 않는다.

##### 유류비 산식

유류비 산식은 다음과 같다.

```text
휘발유·경유·LPG 유류비 = (운행거리 ÷ 연비) × 유종별 단가/ℓ

전기차 충전비 = (운행거리 ÷ 연비) × 전기 단가/kWh

수소차 충전비 = (운행거리 ÷ 연비) × 수소 단가/kg
```

휘발유, 경유, LPG, 전기 및 수소 단가는 수시로 조정한다.

##### 연비 기준

- 휘발유
  - 중형: 8km/ℓ
  - 소형: 10km/ℓ
- 경유
  - 중형: 10km/ℓ
  - 소형: 14km/ℓ
- LPG
  - 중형: 5km/ℓ
  - 소형: 8km/ℓ
- 전기차: 5.1km/kWh
- 수소차: 93km/kg

※ 소형승용차: 배기량 1,500cc 이하 차량

※ 중형승용차: 배기량 1,500cc 초과 차량

##### 장거리 택시비 사후 정산

2. 비행기 결항 등으로 신속하고 긴급한 업무 수행이 불가피하게 발생한 경우 지출된 장거리 택시비는 사후 정산할 수 있다.

### 별첨 2. 국내이전료 정액표

#### 임원(상무보 포함)

단위: 원

| 구분 | 서울 외 지역 | 서울 외 지역 ↔ 서울 |
|---|---:|---:|
| 부임비 | 2,000,000 | 2,300,000 |
| 이전비(이사비＋부동산중개료) | 3,000,000원 한도 내 실비 정산 | 3,000,000원 한도 내 실비 정산 |

#### 직원

단위: 원

| 구분 | 대상 | 서울 외 지역 | 서울 외 지역 ↔ 서울 |
|---|---|---:|---:|
| 부임비 | P5 이상 직책보임자 | 2,000,000 | 2,300,000 |
| 부임비 | 일반직원 | 1,700,000 | 2,000,000 |
| 이전비(이사비＋부동산중개료) | 전 직원 | 1,600,000원 한도 내 실비 정산 | 1,600,000원 한도 내 실비 정산 |

#### 주석

1. 이사하지 않으면 부임비를 지급하지 않는다. 6개월 이내 종전 근무지역으로 재부임할 때에는 부임비를 지급하지 않으며, 1일분의 국내여비(숙박비 제외)와 이전비만 지급한다.
2. 자택 소재지로 부임할 때에는 부임비를 지급하지 않으며, 미혼인 경우 부임비 정액의 3분의 2를 지급한다. 자택 소재지는 미혼자는 부모 주소지, 기혼자는 배우자 주소지로 판단한다.
3. 숙소지원금을 지급받고 있는 직원 또는 숙소지원금 대상 직원이 타 지역으로 부임할 때에는 해당 월 숙소지원금을 지급하지 않는다.
4. 부임비 및 이전비는 부임 후 6개월 이내에 1회만 신청할 수 있다.

### 별첨 3. 국외여비기준표

#### 임원(상무보 포함)

단위: USD

| 구분 | 식비 | 소액경비 | 숙박비 |
|---|---|---|---|
| 임원(상무보 포함) | 실비 | 실비 | 실비(Standard Room 이용) |

#### 직원

단위: USD, 단 일본지역은 JPY 기준

| 구분 | 소액경비 일본 | 소액경비 기타 | 현지교통비 등 일본 | 현지교통비 등 기타 | 숙박비 일본 | 숙박비 아시아·중동 | 숙박비 북미·유럽 | 숙박비 기타 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| P5 이상 직책보임자 | ¥14,000 | $120 | ¥6,000 | $50 | ¥23,000 | $150 | $230 | $150 |
| 일반 | ¥11,000 | $90 | ¥6,000 | $50 | ¥19,000 | $140 | $200 | $130 |

#### 주석

1. 소액경비 중 식비, 현지교통비 및 숙박비는 상기 한도액 범위 내에서 실비로 정산한다. 오세아니아 지역은 북미·유럽 기준을 적용한다.
2. IR 활동, 세미나 참석 등 주최 측에서 숙박처를 지정하는 경우나 회사 대표 자격으로 대외활동 수행 및 보조를 위한 경우에는 해외출장 결재권자의 승인 시 상기 숙박비 한도액을 초과할 수 있다.
3. 달러 매매 기준율을 적용한다.

### 별첨 4. 국외여비기준표

| 구분 | 항공임 | 철도임 | 자동차임 | 선임 |
|---|---|---|---|---|
| 임원 | Business Class | 실비 | 실비 | 실비 |
| P7 실장 | Business Class | 실비 | 실비 | 실비 |
| P7 그룹장 | Economy Class(3시간 이상 Business) | 실비 | 실비 | 실비 |
| P6 그룹장 이하 | Economy Class | 실비 | 실비 | 실비 |

#### 주석

1. IR 활동 등 대외활동 목적의 출장 시 담당 임원 확인으로 상기 기준의 항공임 등급보다 상향 적용할 수 있다.
2. 상기 기준은 동일 국가 내에서 100km 이상 장거리 이동에 따른 교통비 지급이 필요한 경우 적용한다.

### 별첨 5. 선물비 지급기준

단위: USD

| 구분 | 출장자 1~2인 | 출장자 3인 이상 |
|---|---:|---:|
| 지급한도액 | 50 | 100 |

※ 상기 기준은 방문회사 1개소를 기준으로 한다.

### 별첨 6. 국외준비금 정액표

단위: USD

| 구분 | 갑지 | 을지 | 병지 |
|---|---:|---:|---:|
| 임원 | 400 | 300 | 200 |
| 과장 이상 | 300 | 250 | 150 |
| 대리 이하 | 200 | 150 | 100 |

#### 지역 구분

- 갑지역: 남·북미주, 유럽, 호주, 뉴질랜드, 중동, 아프리카
- 을지역: 갑지역과 병지역 이외의 지역
- 병지역: 일본, 중국, 홍콩

### 별첨 7. 해외유학 및 연수자 체재비 정액표

단위: USD

| 지역 | 임원 | 부장 | 리더 이하 | 대리 이하 |
|---|---:|---:|---:|---:|
| 아시아 | 100 | 90 | 85 | 75 |
| 중동 | 110 | 100 | 95 | 85 |
| 북미·유럽 | 105 | 95 | 90 | 80 |
| 기타 | 103 | 93 | 88 | 78 |

#### 1. 적용기준

##### 가. 기간별 지급률

- 유학 또는 연수기간이 1개월 이내인 경우: 기준액의 100%
- 1개월을 초과한 날부터 3개월 이내까지: 초과기간에 대해 기준액의 60%
- 3개월을 초과한 날부터 6개월 이내까지: 초과기간에 대해 기준액의 50%
- 6개월을 초과하는 경우: 초과기간에 대해 기준액의 45%

다만, 체재기간 60일 이하는 1개월을 실제 일수대로 계산하고, 60일 초과 시에는 1개월을 30일로 계산한다.

##### 나. 회사 외 기관에서 여비 일부를 부담하는 경우

1. 숙소 제공 시: 유학 및 연수자 체재비 정액을 국외출장 여비의 소액경비와 숙박료 구성비로 나누어 소액경비 해당 금액만 지급한다.
2. 숙식 제공 시: 위 계산방법에 따라 기타 해당 금액만 지급한다. 다만, 숙식·현지경비 등의 명목으로 지원금액이 명시된 경우에는 회사 지급 기준액과의 차액을 지급한다.

##### 다. 국비연수 또는 유학

국비연수 또는 유학인 경우 당사 지급기준액과 국비지원액의 차액을 지급한다. 다만, 국비지원기관에 다른 제한 규정이 있는 경우에는 그에 따른다.

#### 2. 지역 구분

- 아시아: 아시아주 중 중동을 제외한 지역
- 중동: 바레인, 이라크, 이란, 이스라엘, 요르단, 쿠웨이트, 레바논, 오만, 카타르, 사우디아라비아, 시리아, 터키, 아랍토후국연방, 예멘
- 북미·유럽: 미국, 캐나다 및 유럽주 전 지역
- 기타: 중·남미, 아프리카주, 오세아니아주 및 기타
````
